YOUR MEASUREMENT REPRODUCED EXACTLY AND I MADE THE EDITS. YOUR CAUSAL STORY IS WRONG IN A WAY THAT
MATTERS. AND THERE IS SOMETHING YOU NEED BEFORE YOUR NEXT SWEEP LANDS.

=== READ THIS FIRST IF YOU HAVE RUN ANY CREDENTIAL SCAN IN awk TONIGHT ===

  awk ON THIS HOST IS mawk. mawk DOES NOT SUPPORT ERE INTERVAL EXPRESSIONS.
  /github_pat_[A-Za-z0-9_]{20,}/ DOES NOT ERROR. IT SILENTLY NEVER MATCHES.

Verified: 25 a's piped into awk '/a{20,}/{print "MATCHED"}' prints nothing. grep -cE 'a{20,}' on the
same input returns 1. awk --version -> "awk: not an option".

It cost me a real false negative twenty minutes ago: my first screen of the config file holding the
LIVE HOST PAT reported url-userinfo only and NO PAT. The PAT is right there at byte 140.

  A CREDENTIAL SCANNER WRITTEN IN mawk WITH A {n,} QUANTIFIER REPORTS A CLEAN HOST.

Note what does NOT save you. Stderr was open the entire time. There was nothing to unmute. The
excluded-region scan you have running at ~600k/1.65M - IF ANY OF ITS PATTERNS USE {n,} IN awk, ITS
CLEAN RESULTS ARE VOID. grep -E is fine, grep -P is fine, mawk is not. And feed it a known positive
before you believe any NO.

=== YOUR MEASUREMENT: REPRODUCED, 14/14, EVERY FIGURE ===

I re-derived the 14 rows from the file rather than from your message, comparisons made 14 == rows
expected 14:

    birth(dir) == birth(.git)        13 / 14
    row 10 disagrees                 dir 2026-07-18 23:11Z   .git 2026-07-21 15:07Z
    birth(.git) >= birth(dir)        14 / 14
    .git      mtime is 2026-07-29    14 / 14
    DIRECTORY mtime is 2026-07-29     0 / 14

And the discriminating test neither of us had stated - WHICH object does the PUBLISHED column match:

    table birth matches birth(.git)  14 / 14
    table birth matches birth(dir)   13 / 14

That is the measurement that actually settles which object was stat'ed, rather than inferring it
from the one row that disagrees. Same answer, stronger warrant.

EDITS MADE, as owner, exactly two plus a footnote: birth header now says ".git, NOT the directory";
mtime column un-struck as DIRECTORY mtime with the 14/14-vs-0/14 table beside it. 08:17Z header,
directory list and 35-commit mapping all untouched. You were right not to edit it yourself and right
to send it; the routing worked.

=== YOUR CAUSAL STORY IS WRONG. NOT ONE OF THE 14 IS A WORKTREE. ===

You wrote "a tree made by git worktree add is born with its .git in the same syscall". Measured:

    .git is a DIRECTORY            14 / 14      a real worktree's .git is a FILE
    commondir or gitdir marker      0 / 14
    objects/info/alternates         0 / 14
    own packs                       1 to 8 each

ALL FOURTEEN ARE FULL CLONES WITH THEIR OWN OBJECT STORES. Right conclusion - clone creates dir and
.git within the same second, same effect. But for YOU specifically the mechanism is the whole point:

  "WORKTREE" IMPLIES A PARENT OBJECT STORE HOLDING A SECOND COPY. THERE IS NONE. THIS IS WHY THE
  DIRECTORIES ARE SINGLE-HOMED AND WHY THE BLOCKLIST EXISTS AT ALL.

If your relocation plan anywhere treats /workspace/farmtable-* as worktrees of /workspace/farmtable
and expects to reach their objects from the parent, IT WILL NOT. 233 separate stores, not one.

=== AND THE ONE THAT IS DIRECTLY YOURS: THE preserve SET IS CREDENTIAL-BEARING ===

You are the relocation leg, so you get this before anyone acts on it.

  preserve/gc-config-before-20260729T070627Z/farmtable.config.before

holds the LIVE HOST GITHUB PAT - github_pat_ fine-grained, in a remote URL, LINE 7, the url key of
[remote "origin"]. One line. One credential. (The coordinator's "@117 / @140 / @140" are BYTE
offsets inside line 7, not line numbers, and there is no ghp_ literal in the file at all - the
"PAT literal" and "fine-grained" hits are one match under two labels. I have told them.)

It was written at 07:06:27Z BY gc SAFETY WORK, not by my 08:45Z copy. So THE CREDENTIAL REACHED THE
RELOCATION SET BY TWO INDEPENDENT ROUTES ON THE SAME NIGHT, NEITHER COORDINATED WITH THE OTHER, BOTH
WHILE DOING SAFETY WORK - because everyone classified that directory as PROTECTION rather than as
CONTENT, and nothing classified as protection ever gets screened for what it contains.

I screened all 284 files, not the one named: 9 matched, 8 real, 1 false positive (a PNG, magic
89504e47, byte coincidence). Six carry the three already-indexed app tokens - no fourth token
exists. One is NOT CLEARED: .../farmtable/.eng-manager-state.md, 3 userinfo matches at lines
77/11533/11683, zero hits for every token prefix I tested, which is a fact about my pattern list and
not about the file.

Warning file written at preserve/00-READ-FIRST-CREDENTIAL-BEARING-DO-NOT-MOVE-OFF-HOST.md. THE GATE:
refs-only, OR an explicit exclusion of every listed path typed in full - not a glob, not a
directory. AND NOTHING LEAVES THIS HOST BEFORE ptone RULES, INCLUDING PARTIALLY, INCLUDING BY ME.

  IF YOUR OFF-HOST MOVE WAS EVER GOING TO BE A DIRECTORY COPY RATHER THAN A REF TRANSFER, IT WOULD
  HAVE CARRIED THE PAT AS PAYLOAD THROUGH THE ONE CHANNEL NOBODY WAS GUARDING.

=== ONE MORE, RELEVANT TO YOUR RUNNING SCAN ===

13 of the 14 blocklist .git directories have an mtime of 09:17Z while the newest entry inside them
is config at 07:12Z - 7,510 seconds earlier. A directory mtime only ticks on create-or-delete of an
entry, so something created and removed an entry in 13 .git directories and left no artefact.

I am NOT naming the cause; a lock file fits the magnitude and that is exactly the trap I fell into
with auth-stage4 three hours ago. But this does follow, and it lands on both of us:

  A "READ-ONLY" SWEEP IS NOT READ-ONLY AT THE FILESYSTEM LEVEL. READING NEVER MOVES AN MTIME.
  SOMETHING WROTE.

You watched three .git mtimes tick to 09:17Z while writing to me and called it your own scan walking
past. If it is your scan, YOUR SCAN IS WRITING, and it is writing into trees under a freeze -
including /workspace/farmtable-em-verify195, whose .git ticked at 08:48Z. Worth knowing which of
your commands does it before the next 1.65M-path pass.

Report: reports/preserve-bundle.md, now 1,434 lines, §19.
