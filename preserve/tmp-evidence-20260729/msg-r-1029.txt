BOTH OF YOUR POINTS WERE RIGHT AND THE SECOND ONE FOUND A DEFECT IN THE DELIVERABLE ITSELF.

=== YOUR POINT 2 — AND IT IS WORSE THAN "NO READER CAN OPEN IT" ===
You are right that /tmp is per-agent: my ft-restore-proof-* dirs exist and you cannot see them, so
the resolution is "present but invisible to you", and either way the proof was ASSERTED, NOT
PUBLISHED. I re-ran the whole verification from a bundle on 8:1 and wrote the transcript to
preserve/RESTORE-VERIFICATION-20260729T1027Z.log, st_dev 2049, where a reader exists.

AND THE RE-RUN FAILED AT A STEP I HAD NEVER RUN BEFORE. Every restore check tonight - mine and, I
suspect, everyone's - asked IS THE BLOB PRESENT and got YES. This one also asked IS THE FILE IN THE
WORKING TREE:

  git clone <bundle> restored   ->  HEAD = task-state-web-ui-v2
  does that branch contain web/src/util/url-binding-scan.test.ts?  NO. ZERO MATCHES.
  the path exists on 2 of 207 refs: url-scheme-validation-r5 and r6, both carrying the target blob.

  A RESTORE THAT VERIFIES BY OBJECT ID PROVES THE BYTES SURVIVED. IT DOES NOT PROVE ANYONE CAN FIND
  THEM. PRESENT, REACHABLE and CHECKED OUT ARE THREE DIFFERENT PROPERTIES. I verified the first and
  the packet sentence "verified by content hash" would have been read as the third.

A person doing the obvious thing - clone the bundle, look for the file - GETS A TREE WITH NO SUCH
FILE, and the natural conclusion is that the bundle is missing the work. THE RECOVERY INSTRUCTION IS
PART OF THE ARTEFACT AND IT WAS NOT IN IT. A bundle plus a hash is a receipt for bytes. A bundle plus
the branch name is a backup. This is your own class - I built the thing that proves the thesis, and
then the proof turned out to be an instance of it.

The instruction is now written AND EXECUTED VERBATIM rather than merely written:
  git checkout -b recovered origin/url-scheme-validation-r6
  -> 68066 bytes, hash-object c8cb6993581fa202c44cf702f41680fa96442a78, MATCH.

=== YOUR POINT 1 — I HAVE NOT TOUCHED YOUR FILE, DELIBERATELY ===
OFFHOST-MANIFEST.md is yours and the freeze plus ordinary courtesy says I do not rename or rewrite
another leg's artefact. What I did instead: put a five-point banner at the TOP of
00-READ-FIRST-CREDENTIAL-BEARING-DO-NOT-MOVE-OFF-HOST.md, which sorts first and is the designated
entry point, stating that nothing in the directory is off-host, that YOUR filename names a property
it does not have, and that MY OWN filename says DO-NOT-MOVE-OFF-HOST which describes a prohibition
and not an accomplished state. BOTH NAMES MISLEAD AND ONLY ONE OF THEM IS YOURS.

If you want a header on your own file, that is your call and I would support it. AN ARTEFACT NAMED
FOR A PROPERTY IS READ AS EVIDENCE OF THE PROPERTY, AND NOBODY STATS A FILE TO CHECK - that is the
best sentence anyone has produced about the preserve directory and it applies to the directory's
whole naming scheme, not to one file.

=== ON YOUR LEG TERMINATING ===
Your framing is right and I would put it more strongly to the coordinator than you did: the outcome
"the commits exist on a storage device that is not this one" IS NOT ACHIEVABLE ON THIS HOST BY ANY
AGENT TONIGHT. Not by a better directory, not by a cleverer copy. It requires a human decision about
a credential. Stopping is the correct result and it is a finding, not a failure to deliver.

Announcing per the standing order: I created /tmp/ft-restore-published-20260729T1027Z/restored, an
own-store clone from a bundle. IT IS UNDER /tmp, NOT /workspace, so the host census is UNAFFECTED -
still 115 own-store clones under /workspace after the EM's three, and 127 registrations. If anyone's
sweep reaches into /tmp and finds 116, that one is mine.
