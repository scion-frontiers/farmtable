# Task State Hotfix 179 R2 Security Audit

Date: 2026-07-27
Branch: `task-state-hotfix-179-r2`
Commit under review: `6eaae26`
Base: `582793ea1d7e8fcf9c0be28390a553abf2c7916f`
Verdict: REQUEST CHANGES

## Summary

Reviewed the narrow web fallback predicate and test harness change. The code change itself is low-risk: it is deterministic client-side eligibility logic and does not add auth, credential handling, network requests, filesystem access, command execution, dynamic HTML rendering, URL parsing, or new runtime dependencies.

Blocking issue: full `npm audit` reports one high-severity dev dependency advisory for `postcss@8.5.14` through `vite@6.4.3` (`GHSA-r28c-9q8g-f849`). Production/runtime dependency audit with `npm audit --omit=dev` is clean.

## Findings

- High: Fixable PostCSS path traversal/source map disclosure advisory remains in the web dev dependency tree.
- Critical: 0
- Medium: 0
- Low: 0

Recommended fix:

```bash
cd web
npm audit fix
npm test
npm run build
npm audit
```

## Verification

- `cd web && npm test` - pass
- `cd web && npm run build` - pass; Vite emitted the existing large chunk warning
- `cd web && npm audit --omit=dev` - pass, 0 vulnerabilities
- `cd web && npm audit` - fail, 1 high vulnerability (`postcss <=8.5.17`)
- `git diff --check` - attempted but could not run because `/workspace/.git` points to missing worktree metadata at `/workspace/farmtable/.git/worktrees/farmtable-task-state-hotfix-179-r2`

## Artifact Review

- `web/.tmp-test/` is generated by the test script and is ignored by `.gitignore`.
- `web/dist/` was regenerated by `npm run build`; observed output consists of Vite assets and copied Shoelace SVG assets.
- No scoped source file introduced obvious data exposure primitives such as `innerHTML`, `eval`, `Function`, PostCSS config, or `sourceMappingURL` handling.
