You are a senior code reviewer. Review the changes on branch `feat/f43-tree-parent-child-only` in the workspace at `/workspace/farmtable`.

## Context
This is Feature 43: The tree view component should show ONLY parent-child hierarchy lines (solid gray lines), NOT dependency/relationship lines (previously rendered as dashed indigo lines for BLOCKS/BLOCKED_BY relationships). The Inspector's Relationships tab still shows those relationships — this change is about simplifying the Tree view canvas only.

## What to Review
Run `git log --oneline -3` and `git diff HEAD~1` to see the single commit on this branch.

The change is in `web/src/components/tree/ft-tree-view.ts` — a Lit web component that renders a dagre-based tree/graph view.

## Review Criteria
Evaluate across these dimensions:
1. **Correctness** — Does the change correctly remove all dependency edge rendering without breaking hierarchy edges?
2. **Completeness** — Are all traces of dependency edge support properly removed (imports, types, CSS, layout code, render template)?
3. **No regressions** — Feature 41 (animated centering: 750ms ease-in-out pan on task selection) must still work. Check that `centerOnNode`, `animatePanTo`, `easeInOut`, `PAN_DURATION_MS`, `cancelPanAnimation` are untouched.
4. **Code quality** — Is the cleanup thorough? Any dead code remaining? Any type inconsistencies introduced?
5. **Architecture** — Is this the right approach for removing the dependency lines?

## Deliverables
Provide a structured review report with:
- Overall verdict: APPROVE or REQUEST CHANGES
- Findings listed by severity (Critical / Important / Nitpick)
- For each finding: file, line, description, suggested fix

Write your review report to `/scion-volumes/scratchpad/projects/farmtable/ui-loop/features/feature-43-tree-parent-child-only/review-round1.md`.

You MUST write the review report file and then mark the task complete.
