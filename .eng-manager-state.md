<!-- ============================================================ -->
<!-- STRAY FILE — NOT A STATE FILE. DO NOT READ THIS FOR CONTINUITY.
     DO NOT DELETE IT EITHER: it is the evidence for EM-301.

     Created 2026-07-29 08:51:44Z BY MISTAKE, by the EM, via a
     RELATIVE-PATH append after `cd` into this scratchpad tree.

     THE REAL EM STATE FILE IS, BY ABSOLUTE PATH:
         /workspace/farmtable/.eng-manager-state.md
     (756,039 bytes / 12,521 lines at the time of this error;
      durable head on line 2.)

     Cause: briefs/, reports/ and em-tooling/ live ONLY in this
     scratchpad tree, so the EM cd's here by habit — and the state
     file is the one artefact in the workflow that is NOT here.
     A relative append is therefore wrong exactly once per session,
     silently, and it CREATES rather than fails.

     The content below was re-written to the real file. It is left
     here unaltered under the durability freeze.
-->
<!-- ============================================================ -->

---

## 2026-07-29 ~09:0xZ — EM-299 / EM-300 / homonym confirmed

**EM-300 SECOND-PHRASING BANNER SWEEP — ZERO NEW HAZARDS.** Population widened per
coordinator order to 860 files (briefs 381 + reports 307 + em-tooling 172).
Three instruments, two of them failures worth recording:
- verb words alone: 6,243 lines. USELESS.
- verb + durable noun same line: 4,915 lines / **384 of 860 files**. USELESS.
  **A FILTER THAT MATCHES HALF THE CORPUS IS NOT A FILTER** — mirror of the one that
  matches nothing. "stale"/"stale citations", "tree"/"treewalk", "orphan"/"orphan-scan".
- imperative-shaped: 24 lines / 16 files. Of 12 instruction surfaces,
  `ENUMERATED 12 = BANNERED 11 + NOT 1`, and the 1 is §29 quoting its own evidence.

GAP checked separately (bare command forms carry no hazard vocabulary):
196 lines / 66 files; 18 unbannered instruction surfaces;
`ENUMERATED 18 = PROHIBITION 13 + BENIGN-BUILD-ARTEFACT 4 + CANDIDATE 1`.
**THE POLARITY TRAP, THIRD INSTANCE TONIGHT AND THE SHARPEST FORM: A FILTER AIMED AT
DESTRUCTION VOCABULARY PREFERENTIALLY FINDS THE CONTROLS THAT FORBID DESTRUCTION**,
because a prohibition must NAME what it forbids and an instruction need not. My first
classifier scored this 13-as-2; I caught it only because its own printed "OTHER" list
was visibly full of the word NO. An automated pass would have bannered 13 statements
of the freeze itself.

**EM-299 §30 SHIPPED — and its evidence is against me.** §29 cited
`farmtable-em-f23.md:26`; the string is at line **28**, displaced by the two-line freeze
banner §29 itself prescribes. **§29's EVIDENCE CITATION WAS FALSIFIED BY COMPLYING WITH
§29.** Same shape as the five stale citations that blocked xss-r7, also mine — two
independent instances, so structural. §30: **ASK FOR ANNOTATIONS BY IDENTIFIER, NEVER BY
LINE NUMBER.** Line number in an *instruction* = defect; in a *report* = fine, a commit
does not move (state the SHA). Citation fixed by quoted string, drift recorded not erased.
`_BRIEF-RULES.md` now 1,468 lines.

**HOMONYM CONFIRMED — the coordinator was right, I matched on a basename.**
- `/workspace/farmtable/.eng-manager-state.md` — 756,039 bytes, 12,521 lines, DURABLE
  HEAD first line, 1 xss-r7 mention → **MINE, this file, and correctly in the copy.**
- `/workspace/.eng-manager-state.md` — 3,275 bytes, 66 lines,
  `# Eng-Manager State — Auth Improvements Workstream`, self-declared last update
  2026-07-26 22:39Z, 0 xss-r7 mentions → **NOT MINE**, another agent's, stale ~3 days.
His path-based order was right so nothing was lost. **My own standing instructions
already disambiguate these two paths and I collapsed them anyway — second time tonight
the correct value was already in my possession** (first: the dispatch slug, both
spellings live in my own corpus). He caught it via a 231× magnitude gap between two
things I had named 90 minutes apart — the same magnitude check I ran prospectively on my
own input earlier and did not run on this.

**COORDINATOR RULES ADOPTED:** *A POPULATION WITHOUT ITS PREDICATE IS A NUMBER WEARING
RIGOUR'S CLOTHES* (mirror of mechanism-without-population, and more dangerous because a
bare mechanism looks incomplete and a bare population looks finished). *PRAISE IS THE ONE
CHANNEL WITH NO ADVERSARY IN IT.* My amendment sent: the producer offering a population
without its predicate is the defect, so the rule binds the producer first.

**FREEZE STATUS UNCHANGED:** durability freeze active and extended to deletion; agent GC
SUSPENDED (lifted only explicitly and in writing); no reclaim of the 1.2 GB; the 22 loose
files are the copy author's, not mine; `.gitignore` unchanged; no worktree registration
touched.

**NEXT:** hedge sweep over the same widened 860-file population; then relay the r7
adjudication, amend the build fence for mutation testing, create `reports/r8/`, and
dispatch the r8 fix leg.
