// CANARY, THROWAWAY. A tracked web test file that the runner does not compile,
// used to ask whether the web-side gate can see an ADDITION or only a deletion.
import { test } from 'node:test';
test('canary added file', () => {});
