You need to redo the before/after screenshots for Feature 43 with test data that actually has BLOCKS relationships, so the "before" screenshot shows dashed indigo dependency lines and the "after" shows them gone.

## Your Workspace
You are working in `/workspace/farmtable-f43` on branch `feat/f43-tree-parent-child-only`. The code change is already committed (commit 3542914).

## Strategy

You need to:
1. Build and run from `origin/main` (the pre-fix code that DOES render dependency edges)
2. Create tasks with real BLOCKS relationships
3. Screenshot the tree view showing the dashed indigo BLOCKS lines → BEFORE
4. Build and run from the feature branch (the fix that removes dependency edges)
5. Screenshot the same tree view showing no dashed lines → AFTER

### Step-by-step:

**Step 1: Build from origin/main (pre-fix code)**
```bash
cd /workspace/farmtable-f43
git stash  # save any uncommitted changes
git checkout origin/main --detach  # temporarily switch to main
cd web && npm ci --prefer-offline && npm run build && cd ..
go build -o ft-main ./cmd/ft   # build as ft-main to distinguish
```

**Step 2: Start dashboard and create test data with BLOCKS relationships**
```bash
# Use a fresh DB so we control the data
rm -f ./testrel.db ./testrel.db-wal ./testrel.db-shm
FARMTABLE_DB_PATH=./testrel.db ./ft-main dashboard --port 9093 > dashboard-main.log 2>&1 &
sleep 3
curl -s http://localhost:9093/ | head -3  # verify running
```

Now use the ft CLI to create tasks WITH parent-child AND blocking relationships:
```bash
export FARMTABLE_DB_PATH=./testrel.db
export PATH=$(pwd):$PATH

# First, find the collection ID
./ft-main collection list 2>&1

# Create several tasks - we need a parent with children AND blocking relationships
./ft-main task create "Epic: Platform Launch" --stage working --label feature --priority high --type epic
./ft-main task list 2>&1  # get the epic's task ID

# Create child tasks (these will have parentTaskId set)
# Use the epic's ID as parent
# After creating, also add blocking relationships between some tasks
```

IMPORTANT: The `ft task create` command supports `--blocks <taskID>` and `--blocked-by <taskID>` flags. The `ft task update` command supports `--add-blocks <taskID>` and `--add-blocked-by <taskID>` flags. Use `ft task list` to get task IDs.

You need at least:
- A parent task with 2+ children (for hierarchy lines)  
- At least one BLOCKS relationship between tasks (for the dashed dependency line in the BEFORE screenshot)

**Step 3: Take BEFORE screenshot (from main code)**
Use the Playwright screenshot script. The script should:
- Navigate to http://localhost:9093
- Click into the collection (might need to click "default" or whatever collection name)
- Click "Tree" to switch to tree view
- Wait for rendering
- Take screenshot

Save as: `/scion-volumes/scratchpad/projects/farmtable/ui-loop/features/feature-43-tree-parent-child-only/before-tree.png`

Playwright setup:
```bash
export PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium
```

Run from `/scion-volumes/scratchpad/web-test` (where playwright is installed):
```javascript
import { chromium } from 'playwright';
const outputPath = process.argv[2] || '/tmp/tree-screenshot.png';
const browser = await chromium.launch({
  headless: true,
  executablePath: process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH,
  args: ['--no-sandbox', '--disable-gpu'],
});
const page = await browser.newPage({ viewport: { width: 1440, height: 900 } });
await page.goto('http://localhost:9093', { waitUntil: 'networkidle' });
await page.waitForTimeout(2000);
// Click into collection if needed
const collLink = page.locator('ft-collection-list a, ft-collection-list sl-card, text=default').first();
if (await collLink.count() > 0) { await collLink.click(); await page.waitForTimeout(2000); }
// Switch to Tree view
const treeBtn = page.locator('sl-radio-button[value="tree"]');
if (await treeBtn.count() > 0) { await treeBtn.first().click(); await page.waitForTimeout(2000); }
await page.screenshot({ path: outputPath, fullPage: false });
await browser.close();
console.log('Screenshot saved to ' + outputPath);
```

**Step 4: Stop dashboard, switch to feature branch, rebuild**
```bash
pkill -f "ft-main dashboard" 2>/dev/null || true
sleep 1
git checkout feat/f43-tree-parent-child-only  # back to feature branch
cd web && npm run build && cd ..
go build -o ft ./cmd/ft
```

**Step 5: Start dashboard from feature branch with SAME database**
```bash
FARMTABLE_DB_PATH=./testrel.db ./ft dashboard --port 9093 > dashboard-feat.log 2>&1 &
sleep 3
```

**Step 6: Take AFTER screenshot**
Same Playwright script, save as: `/scion-volumes/scratchpad/projects/farmtable/ui-loop/features/feature-43-tree-parent-child-only/after-tree.png`

**Step 7: Clean up**
```bash
pkill -f "ft dashboard" 2>/dev/null || true
rm -f ./testrel.db ./testrel.db-wal ./testrel.db-shm ./ft-main
```

## Deliverables
1. Before screenshot showing dashed indigo BLOCKS lines in tree view (from main code)
2. After screenshot showing only solid gray hierarchy lines (from feature branch code)
3. Both saved to `/scion-volumes/scratchpad/projects/farmtable/ui-loop/features/feature-43-tree-parent-child-only/`

You MUST produce both screenshots with visible difference (dashed lines present in before, absent in after), then mark the task complete.
