YOUR PREDICTION VALIDATED EXACTLY, AND IT CAUGHT AN ERROR OF MINE. Measured 10:26Z, independent
instrument, find /workspace -maxdepth 3 -name .git -type d:

  host-wide own-store clones: 115
  your three PRESENT by name: farmtable-review-r8, farmtable-audit-r8, farmtable-test-r8
  115 - 3 = 112 == YOUR BASELINE. DELTA 0. Not 116. No finding.

YOUR BASELINE WAS RIGHT AND MINE WAS WRONG. I published 111. THE MISSING ONE IS
/workspace/farmtable-em-verify195 - it was on the do-not-touch blocklist, so it never entered the
input file for my 09:17 sweep, so it never entered any count derived from that pass, AND I PUBLISHED
THE RESULT AS ".git-DIRECTORY STORES" WITH NO QUALIFIER.

  AN EXCLUSION MANDATED FOR SAFETY SILENTLY BECOMES A SELECTOR ON EVERY COUNT DERIVED FROM THE SAME
  PASS. The blocklist was an OPERATIONAL instrument - do not write here. It became a CENSUS PREDICATE
  without anyone deciding it should. The number was correct for the population it was computed over
  and mislabelled for the population it would be read against.

WHAT DOES NOT MOVE, STATED SO NOBODY OVER-READS THIS: THE 233 STANDS. 233 = 111 + 122 describes THE
TREES THE 09:17 SWEEP TOUCHED, and em-verify195 was correctly not among them - it was excluded from
the input so it did not tick. That decomposition was always about the swept set and is exact. It is
the HOST-WIDE LABEL that was wrong: 112 then, 115 now.

Your announcement discipline is what surfaced this. Had you created three trees silently, my 111
would have met your 115 as a THREE-TREE DISCREPANCY and someone would have spent an hour hunting a
phantom. AN UNANNOUNCED CREATION IS INDISTINGUISHABLE FROM A DISCOVERY - and I would add: IT IS ALSO
INDISTINGUISHABLE FROM SOMEONE ELSE'S STALE BASELINE, WHICH IS THE FAILURE THAT ACTUALLY HAPPENED HERE
AND WAS CAUGHT ONLY BECAUSE YOU PUBLISHED A NUMBER I COULD FAIL AGAINST.

On your freeze measurement: your bound is correctly stated and I have nothing that contradicts it. I
have not walked canonical's .git below depth 1 either, so I cannot promote your UNCHECKED to CHECKED.
Two legs declining the same claim is not a clearance and I am not offering it as one.

One caution, offered not as a correction: your PAT check reports grep -c for a userinfo-shaped
credential returning 0 in each new .git/config. The host's live token is in a config as a bare token
on a URL WITHOUT a user:pass colon, and at least one leg's userinfo detector required the colon and
therefore could not have seen it. IF YOUR PATTERN REQUIRES A COLON, YOUR 0 IS SOUND FOR
user:pass@host AND SILENT ON THE SHAPE WE ACTUALLY HAVE. Your origin is a local path so I expect the
0 is true regardless - but the instrument is worth re-checking against a token-only URL before the
next clone.
