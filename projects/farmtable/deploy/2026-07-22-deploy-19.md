# Farm Table Deploy Log — 2026-07-22 Deploy 19

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-19`)
- Commit deployed: `cb19a2f` — `fix(web): align Ready Queue priority badges with fixed-width column (#121)`
- Previous revision: `farmtable-00024-prb` (commit `aa0feb2`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`aa0feb2` / rev `farmtable-00024-prb`)

```
cb19a2f fix(web): align Ready Queue priority badges with fixed-width column (#121)
```

Key change in this deploy:
- **Feature 47: Ready Queue Priority Badge Alignment** (PR #121):
  A small CSS fix wrapping priority badges in the Ready Queue view in a
  `.priority-cell` span with `display: inline-flex`, `flex-shrink: 0`, and
  `min-width: 6.5rem` (104px). This ensures task titles align consistently
  regardless of badge text length (e.g. "Normal" vs "No priority" vs "Urgent").

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-19.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-19.yaml \
  .
```

- Build ID: `8e746ea8-e5fb-403b-9b0c-9e4fda339f4c`
- Status: **SUCCESS**
- Started: `2026-07-22T13:39:37.403590876Z`
- Finished: `2026-07-22T13:42:56.297712Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:34f45bcea30a7d0bbb6f550daa04442d2fd675b4f79fd9c9eeecad2348f005fb`
- Note: Local log streaming exited with the known default logs bucket permission
  message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00025-nkn`**
- Traffic: **100%** to `farmtable-00025-nkn`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00025-nkn` as both `latestCreatedRevisionName` and
`latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200.

## Feature 47 Verification — Priority Badge Alignment (Playwright)

All verification performed with Playwright (Chromium headless, 1440x900 viewport)
against the live deployed URL `https://farmtable-486315127503.us-central1.run.app`.

### V1 — Default Collection Ready Queue (28 tasks) — **PASS**

Navigated to the `default` collection and switched to the Ready Queue view via
the `ft-toolbar` view selector (`sl-radio-button[value="ready-queue"]`).

The Ready Queue rendered 28 tasks. Programmatic verification via DOM inspection:

| Metric | Value |
|--------|-------|
| `.priority-cell` elements found | 28 |
| `min-width` computed value | `104px` (all cells) |
| `display` computed value | `flex` (all cells) |
| `flex-shrink` computed value | `0` (all cells) |
| All cells same width | `true` (all 104px) |
| Unique badge texts | "Normal" (60px badge), "No priority" (80px badge) |
| All task title `left` positions | 446px (identical across all rows) |
| Titles aligned despite badge width variance | **YES** |

Despite the "Normal" badge being 20px narrower than the "No priority" badge,
all task titles start at exactly the same horizontal position (446px) because
the `.priority-cell` wrapper enforces a consistent 104px minimum column width.

**Key screenshot:** `f47-03-after-switch-attempt.png`

### V2 — farmtable-deploy4-web Collection Ready Queue (4 tasks) — **PASS**

Switched to the `farmtable-deploy4-web` collection and verified the Ready Queue
view with a smaller set of 4 tasks.

| Metric | Value |
|--------|-------|
| `.priority-cell` elements found | 4 |
| All cells aligned | `true` (all 104px) |
| All titles aligned | `true` (all at 446px left) |
| Unique priorities | "Normal", "No priority" |

Confirms the fix works consistently across collections.

**Key screenshot:** `f47-05-collection2-ready-queue.png`

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-19/`:

| File | Description |
|------|-------------|
| `f47-01-initial-load.png` | Collection selector page (app loaded successfully) |
| `f47-02-collection-loaded.png` | Default collection kanban view with 28 tasks |
| `f47-03-after-switch-attempt.png` | **Key screenshot**: Default collection Ready Queue — "Normal" vs "No priority" badges with aligned titles |
| `f47-05-collection2-ready-queue.png` | **Key screenshot**: farmtable-deploy4-web Ready Queue — 4 tasks, aligned titles |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00025-nkn` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 47: CSS fix deployed | **PASS** — `.priority-cell` wrapper with `min-width: 104px` present in live DOM |
| Feature 47: Badge alignment | **PASS** — All priority cells same width (104px) despite varying badge text |
| Feature 47: Title alignment | **PASS** — All task titles at identical left position (446px) across "Normal" and "No priority" badges |
| Cross-collection check | **PASS** — Alignment confirmed on both `default` (28 tasks) and `farmtable-deploy4-web` (4 tasks) |

**Feature 47 (PR #121) is CONFIRMED WORKING on the live deployed site.**
Priority badges now occupy a fixed-width column, ensuring task titles in the
Ready Queue view align consistently regardless of badge text length.
