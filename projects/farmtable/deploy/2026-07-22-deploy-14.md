# Farm Table Deploy Log — 2026-07-22 Deploy 14

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-14`)
- Commit deployed: `b2a8123` — `feat(web): show only parent-child edges in tree view (#115)`
- Previous revision: `farmtable-00019-w8z` (commit `6cddeb4`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`6cddeb4` / rev `farmtable-00019-w8z`)

```
b2a8123 feat(web): show only parent-child edges in tree view (#115)
```

Key change in this deploy:
- **Feature 43** (PR #115): Removes dependency/relationship edge rendering (dashed
  indigo BLOCKS lines) from the tree view canvas, leaving only solid gray
  parent-child hierarchy edges. The change is surgical — all six sites where
  dependency edges were referenced (import, interface type, CSS class,
  `structureKey()` cache key, `runLayout()` graph building, edge-object mapping,
  and render template) are cleaned up consistently. Feature 41 (animated
  centering) is fully preserved.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-14.yaml` (standard `gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-14.yaml \
  .
```

- Build ID: `3c454496-0107-4a12-8336-add38f8261d4`
- Status: **SUCCESS**
- Started: `2026-07-22T11:41:10.112431072Z`
- Finished: `2026-07-22T11:45:20.356273Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:a2def11e86948bb83c63d0407f37630b4c5db4781d5c622cf27bd62e4a85a3f7`
- Note: Local log streaming exited with the known default logs bucket permission message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00020-flp`**
- Traffic: **100%** to `farmtable-00020-flp`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00020-flp` as both `latestCreatedRevisionName` and `latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200.

## Feature 43 Verification — Parent-Child Only Edges (Playwright)

All verification performed with Playwright (Chromium headless, 1440×900 viewport) against the live
deployed URL `https://farmtable-486315127503.us-central1.run.app`.

### V1 — Edge Analysis: No Dependency Edges in SVG — **PASS**

Analyzed the `ft-tree-view` component's SVG canvas via shadow DOM traversal:

| Metric | Value |
|--------|-------|
| Total SVG `<path>` elements | 5 |
| `.edge-hierarchy` paths | 5 |
| `.edge-dependency` paths | **0** |
| Paths with `stroke-dasharray` (dashed) | **0** |

All 5 edges are solid parent-child hierarchy edges. Zero dependency edges exist in the rendered SVG.

### V2 — BLOCKS Relationship Exists in Data But Not Rendered — **PASS**

**Setup**: Used Playwright to add a BLOCKS relationship via the app's gRPC-Web API (monkey-patched
`updateTask` to inject `addBlockedBy`):

- **Blocker**: "Test task from real client" (`1015aab2-d142-4c0e-ae25-8d874a1676ca`)
- **Blocked**: "smoke-test-1784467180" (`51dae6ed-7173-4da7-b7ff-f4e51682330f`)

**Verification**: After adding the relationship:
1. Switched to Tree view — still **5 hierarchy edges, 0 dependency edges, 0 dashed paths**.
2. Clicked "smoke-test-1784467180" and opened the Inspector's Relationships tab.
3. Inspector shows:
   - **BLOCKED BY**: "Test task from real client" (Ready) — relationship IS in the data.
   - **Children**: "foobar" (Working) — hierarchy relationship.
   - **Blocks**: None
4. Tree view canvas shows only solid gray parent-child lines between:
   - smoke-test-1784467180 → foobar → my-new-task
   - Ready-03 → Ready-04 → Ready-05
   - Ready-07 → Ready-08
5. **No dashed BLOCKS lines drawn** between "Test task from real client" and "smoke-test-1784467180" despite the relationship existing.

**Key screenshot**: `05-inspector-relationships.png` shows the Inspector's Relationships tab confirming
the BLOCKED BY relationship exists, while the tree view behind it shows only solid hierarchy lines.

## Feature 41 Spot-Check — Animated Centering — **PASS**

Tested animated centering by clicking nodes at different positions in the tree and measuring the
SVG `viewBox` attribute changes (the component uses `viewBox` panning, not SVG transforms):

| Action | viewBox panX | viewBox panY |
|--------|-------------|-------------|
| Initial load | -40 | -847 |
| Click "smoke-test-1784467180" | -1470 | -948 |
| Click "Ready-15" (far right) | 2216 | -948 |
| Click "my-new-task" (bottom left) | -904 | -668 |

The viewBox pans smoothly to center on each clicked node. Screenshots confirm the selected node
appears centered in the viewport after each click. The 750ms easeInOut animation duration from
Feature 41 is unchanged in the code.

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-14/`:

| File | Description |
|------|-------------|
| `00-collection-picker.png` | Initial app load (collection picker) |
| `01-kanban-board.png` | Kanban board loaded (default collection) |
| `02-tree-view-initial.png` | Tree view initial load |
| `03-tree-with-inspector.png` | Tree view with inspector open |
| `04-feature43-tree-no-blocks-edges.png` | **Key screenshot**: Tree view showing only hierarchy edges, no BLOCKS edges |
| `05-inspector-relationships.png` | **Key screenshot**: Inspector showing BLOCKED BY relationship exists while tree shows no dependency edges |
| `06-f41-smoke-test-centered.png` | Feature 41: view centered on smoke-test task |
| `07-f41-ready15-centered.png` | Feature 41: view centered on Ready-15 (far right) |
| `08-f41-my-new-task-centered.png` | Feature 41: view centered on my-new-task (bottom left) |
| `verification-summary.json` | Machine-readable verification data |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00020-flp` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| V1: Edge analysis | **PASS** — 5 hierarchy edges, 0 dependency edges, 0 dashed paths |
| V2: BLOCKS in data, not rendered | **PASS** — Inspector confirms BLOCKS relationship, tree shows no dependency edges |
| Feature 41: Animated centering | **PASS** — viewBox pans correctly to center on selected node |

**Feature 43 (show only parent-child edges in tree view) is CONFIRMED WORKING on the live deployed site.**
**Feature 41 (animated centering) is CONFIRMED NOT REGRESSED.**
