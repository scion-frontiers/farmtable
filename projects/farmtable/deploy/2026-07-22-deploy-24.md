# Farm Table Deploy Log — 2026-07-22 Deploy 24

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-24`)
- Commit deployed: `277ae61` — `feat(web): scrollable collection list landing page + new project button (F50) (#127)`
- Previous revision: `farmtable-00029-w2d` (commit `6814944`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`6814944` / rev `farmtable-00029-w2d`)

```
277ae61 feat(web): scrollable collection list landing page + new project button (F50) (#127)
```

Key change in this deploy:
- **Feature 50: Scrollable Collection List Landing Page + New Project Button** (PR #127):
  - The root collection-list landing page (no `?collection=` in URL) now wraps
    `ft-collection-list` in a `.landing` flex container with `flex: 1; overflow: auto;
    min-height: 0`, enabling the list to scroll when collections overflow the viewport.
  - A "New Project" button (primary variant, with `+` icon) is added to the landing
    page header, right-aligned next to the "Select a collection" heading.
  - Clicking "New Project" opens `ft-new-collection-dialog` — a Shoelace dialog with
    a required Name input, Cancel, and Create buttons.
  - Creating a collection calls `client.createCollection(name)`, closes the dialog,
    and dispatches `collection-select` to navigate to the new project's board.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-24.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-24.yaml \
  .
```

- Build ID: `b73f9291-b97c-45ef-bc83-d28debff006f`
- Status: **SUCCESS**
- Started: `2026-07-22T16:32:01.907342512Z`
- Finished: `2026-07-22T16:36:52.935220Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:2223f42ac2259f206e5e085726e81f6c030c2530e6adb2517feb3a057910d610`
- Note: Local log streaming exited with the known default logs bucket permission
  message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00030-9zg`**
- Traffic: **100%** to `farmtable-00030-9zg`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00030-9zg` as both `latestCreatedRevisionName` and
`latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' -L https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200 (via 302 redirect through IAP).

## Feature 50 Verification — Scrollable Landing Page + New Project Button (Playwright)

All verification performed with Playwright (Chromium headless, 1440x900 viewport)
against the live deployed URL `https://farmtable-486315127503.us-central1.run.app`.

IAP authentication was handled via a `Bearer` token obtained from
`gcloud auth print-identity-token` with the IAP OAuth client ID audience
(`486315127503-o94o6safp9v2865ddj9cupqftnst9jfn.apps.googleusercontent.com`),
passed as an `extraHTTPHeaders` `Authorization` header in the Playwright context.

### V1 — Root Landing Page Loads (no `?collection=` in URL) — **PASS**

1. Navigated to the service root URL (no `?collection=` parameter).
2. The collection-list landing page rendered with the heading "Select a collection"
   and sub-heading "Choose which collection to open."
3. Multiple collection cards are visible (default, farmtable-deploy4-web, farmtable-deploy4-cli,
   github-experiment, github-mirror, External Store Passthrough, ext-store-passthrough-design,
   deploy-16-dep-test, Scenario 1: 4-Layer Chain, and more).

**Key screenshot:** `f50-01-landing-page.png`

### V2 — Scrollable Collection List (`.landing` wrapper) — **PASS**

1. Inspected the `.landing` div in the `ft-app` shadow DOM:
   - `overflow: auto` — scroll mechanism present
   - `flex: 1 1 0%` — fills available space
   - `min-height: 0px` — allows flex shrinking for overflow
   - `clientHeight: 900` (viewport height)
   - `scrollHeight: 1292` — content overflows viewport by 392px
   - **`isScrollable: true`** — confirmed scrollable
2. The collection list has enough real collections on the live service to overflow
   the viewport, confirming the scroll mechanism works with real data.

### V3 — "New Project" Button Present — **PASS**

1. Located `sl-button[variant="primary"]` in the `ft-collection-list` shadow DOM.
2. Button text: **"New Project"** — rendered with a `+` icon prefix.
3. Positioned in the top-right of the header, right-aligned next to the heading.

**Key screenshot:** `f50-02-new-project-button.png`

### V4 — "New Project" Opens Create-Collection Dialog — **PASS**

1. Clicked the "New Project" button.
2. `ft-new-collection-dialog` opened as a Shoelace dialog.
3. Dialog state confirmed:
   - `open: true`
   - `label: "New Collection"`
4. Dialog contains a required Name input field, Cancel button, and Create (primary) button.

**Key screenshots:**
- `f50-03-create-dialog.png` — Dialog open with empty Name field
- `f50-04-dialog-filled.png` — Dialog with "Deploy-24-Test" entered in the Name field

### V5 — Creating a Project Navigates to Its Board — **PASS**

1. Entered "Deploy-24-Test" in the Name field.
2. Clicked the "Create" button.
3. Collection was created successfully via `client.createCollection()`.
4. Dialog closed automatically.
5. URL changed to: `https://farmtable-486315127503.us-central1.run.app/?collection=3b100b87-6e9c-4adc-8661-980a4effcbca`
   — `collection=` parameter present, confirming navigation.
6. Board view rendered with:
   - "Deploy-24-Test" shown in the collection selector (top-left)
   - Kanban columns: TRIAGE (0), BACKLOG (0), READY (0), WORKING (0), IN REVIEW (0)
   - Green "Live" indicator (websocket connected)
   - "+ Add Task" button available
7. Landing page is gone (`.landing` div no longer in DOM), `routeView: "board"`.

**Key screenshots:**
- `f50-05-navigated-to-board.png` — Board view immediately after project creation
- `f50-06-board-final.png` — Final board view confirming "Deploy-24-Test" collection

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-24/`:

| File | Description |
|------|-------------|
| `f50-01-landing-page.png` | Root landing page with scrollable collection list and "New Project" button |
| `f50-02-new-project-button.png` | Landing page highlighting "New Project" button (blue, top-right) |
| `f50-03-create-dialog.png` | **Key**: "New Collection" dialog open with empty Name field |
| `f50-04-dialog-filled.png` | **Key**: Dialog with "Deploy-24-Test" entered in Name field |
| `f50-05-navigated-to-board.png` | **Key**: Board view after project creation — "Deploy-24-Test" in collection selector |
| `f50-06-board-final.png` | Final Kanban board with all columns (TRIAGE through IN REVIEW) and Live indicator |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00030-9zg` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 50: Landing page loads | **PASS** — Collection list renders at root URL |
| Feature 50: Scrollable collection list | **PASS** — `.landing` wrapper with `overflow: auto`, scrollHeight (1292) > clientHeight (900) |
| Feature 50: "New Project" button present | **PASS** — Primary button with "New Project" text and + icon |
| Feature 50: Dialog opens | **PASS** — "New Collection" dialog with Name input, Cancel, Create |
| Feature 50: Create navigates to board | **PASS** — Collection created, URL gains `?collection=`, Kanban board renders |

**Feature 50 (PR #127) is CONFIRMED WORKING on the live deployed site.**
The collection-list landing page scrolls when content overflows the viewport.
The "New Project" button opens a create-collection dialog, and creating a project
successfully navigates to its empty Kanban board — verified using Playwright against
the live deployment with real IAP authentication.
