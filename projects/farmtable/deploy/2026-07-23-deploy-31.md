# Farm Table Deploy Log — 2026-07-23 Deploy 31

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-31`)
- Commit deployed: `b500753` — `fix(web): restore combined pan+zoom animation on node selection (F58) (#135)`
- Previous revision: `farmtable-00036-4lm` (commit `696cacc`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`696cacc` / rev `farmtable-00036-4lm`)

```
b500753 fix(web): restore combined pan+zoom animation on node selection (F58) (#135)
f33d68a fix(server): seed GetBottlenecks from both blocks and blocked_by edges (#134)
```

Key changes in this deploy:
- **Feature 58: Restore Combined Pan+Zoom Animation** (PR #135):
  Feature 56 had regressed the smooth combined animation — pan was snapping
  instantly while zoom animated separately. PR #135 restores the 750ms
  ease-in-out animation where pan and zoom interpolate together each frame,
  keeping the selected node centered at every intermediate zoom level. Both
  the Tree view and Dependency view are fixed.
- **PR #134: Fix GetBottlenecks server endpoint** (PR #134):
  Server-side fix to seed GetBottlenecks from both `blocks` and `blocked_by`
  edges. No user-facing UI change.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-31.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-31.yaml \
  .
```

- Build ID: `37c8289e-7d7b-418e-bf51-844303e61f4e`
- Status: **SUCCESS**
- Finished: `2026-07-23T00:18:39.582300Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:89a8f2b0432493d3930633b2be4cdb42c1fd3c6ccd6f30452785a3915196b476`
- Note: Local log streaming exited with the known default logs bucket permission
  message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00037-7cv`**
- Traffic: **100%** to `farmtable-00037-7cv`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00037-7cv` as both `latestCreatedRevisionName` and
`latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $TOKEN" -L \
  https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200

## Feature 58 Verification — Combined Pan+Zoom Animation (Playwright)

All verification performed with Playwright (Chromium headless, 1440x900 viewport)
against the live deployed URL `https://farmtable-486315127503.us-central1.run.app`.

IAP authentication handled via `Bearer` token obtained from
`gcloud auth print-identity-token` with IAP OAuth client ID audience.

**Method:** Seeded test data with relationship chains to spread nodes across the
viewport. Selected an initial node (ready-1), waited for animation to settle,
then triggered selection of a distant node (working-5). Captured screenshots at
~150ms intervals during the 750ms animation and recorded SVG viewBox values via
requestAnimationFrame hook (47 per-frame samples).

### V1 — Tree View: Mid-Animation Frame Sequence — **PASS**

Captured 8 screenshots (before + 6 during + settled) and 47 rAF viewBox samples.

| Frame | Time | viewBox (x, y, w, h) |
|-------|------|----------------------|
| 0 | before | -440, -255, 762, 591 |
| 1 | ~150ms | 639, -269, 797, 618 |
| 2 | ~300ms | 3831, -317, 922, 715 |
| 3 | ~450ms | 6501, -372, 1062, 824 |
| 4 | ~600ms | 7100, -386, 1100, 853 |
| 5-7 | ~750ms+ | 7100, -386, 1100, 853 (settled) |

**Analysis:** All four viewBox components (panX, panY, width, height) change
progressively across frames 0-4, proving combined pan+zoom interpolation.
The viewBox width expands from 762 to 1100 (zoom-out) while panX sweeps from
-440 to 7100 — both happening together, not sequentially. Animation settles
by ~600ms, consistent with the 750ms ease-in-out reaching its plateau.
The rAF data confirms 47 individual animation frames with smooth, continuous
interpolation — no instant jumps.

### V2 — Dependency View: Mid-Animation Frame Sequence — **PASS**

Captured 8 screenshots (before + 6 during + settled) and 47 rAF viewBox samples.

| Frame | Time | viewBox (x, y, w, h) |
|-------|------|----------------------|
| 0 | before | -400, -234, 762, 627 |
| 1 | ~150ms | -393, 247, 797, 656 |
| 2 | ~300ms | -385, 1664, 922, 759 |
| 3 | ~450ms | -395, 2844, 1062, 875 |
| 4 | ~600ms | -400, 3107, 1100, 906 |
| 5-7 | ~750ms+ | -400, 3107, 1100, 906 (settled) |

**Analysis:** panY sweeps from -234 to 3107 while viewBox width expands from
762 to 1100 — combined pan+zoom, not an instant snap. The rAF data shows 47
smooth animation frames. Animation settles by ~600ms. Feature 58 fix confirmed.

### V3 — Screenshot Visual Verification — **PASS**

Visual inspection of screenshots confirms:
- **tree-00-before.png**: Ready-1 selected with prominent halo, viewport at left
- **tree-02-frame-300ms.png**: Viewport mid-pan showing Ready-13-16 area (intermediate)
- **tree-04-frame-600ms.png**: Working-5 centered with prominent halo (settled)
- **dep-00-before.png**: Ready-1 selected at top of dependency chain
- **dep-02-frame-300ms.png**: Viewport mid-pan showing Ready-12-18 (intermediate)
- **dep-04-frame-600ms.png**: Working-5 centered with prominent halo (settled)

All screenshots have unique MD5 hashes during the animation window (frames 0-3),
confirming distinct visual states. 10 unique hashes out of 16 total screenshots
(frames 4+ are identical as expected — animation complete).

## Feature 56 Spot-Check — Zoom Target + Highlight Styles (Non-Regression)

### V4 — Tree View: Highlight Styles — **PASS**

Selected node computed styles (via `ft-tree-node` shadow DOM):
- `border-top-width`: **3px** (widened from default 2px)
- `border-left-width`: **5px** (accent border)
- `border-color`: `rgb(14, 165, 233)` (primary-500)
- `box-shadow`: **`rgba(0, 0, 0, 0) 0px 0px 0px 3px, rgba(99, 102, 241, 0.45) 0px 0px 0px 6px`**
  — two-layer halo with transparent gap + indigo outer ring

Node rendered width: 281.6px. As percentage of full viewport (1440px): **19.6%**
(target ~20%). Matches deploy-30's Tree view result exactly.

### V5 — Dependency View: Highlight Styles — **PASS**

Selected node computed styles (identical to Tree view):
- `border-top-width`: **3px**
- `border-left-width`: **5px**
- `border-color`: `rgb(14, 165, 233)`
- `box-shadow`: **`rgba(0, 0, 0, 0) 0px 0px 0px 3px, rgba(99, 102, 241, 0.45) 0px 0px 0px 6px`**

Node rendered width: 281.6px. As percentage of full viewport: **19.6%**.

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-23-deploy-31/`:

| File | Description |
|------|-------------|
| `tree-00-before.png` | Tree view: Ready-1 selected, before animation |
| `tree-01-frame-150ms.png` | Tree mid-animation ~150ms |
| `tree-02-frame-300ms.png` | **Key**: Tree mid-animation ~300ms (viewport mid-pan) |
| `tree-03-frame-450ms.png` | Tree mid-animation ~450ms |
| `tree-04-frame-600ms.png` | **Key**: Tree settled — Working-5 centered with halo |
| `tree-05-frame-750ms.png` | Tree settled (same as 600ms) |
| `tree-06-frame-900ms.png` | Tree settled (same as 600ms) |
| `tree-07-settled.png` | Tree final settled state |
| `dep-00-before.png` | Dependency view: Ready-1 selected, before animation |
| `dep-01-frame-150ms.png` | Dep mid-animation ~150ms |
| `dep-02-frame-300ms.png` | **Key**: Dep mid-animation ~300ms (viewport mid-pan) |
| `dep-03-frame-450ms.png` | Dep mid-animation ~450ms |
| `dep-04-frame-600ms.png` | **Key**: Dep settled — Working-5 centered with halo |
| `dep-05-frame-750ms.png` | Dep settled (same as 600ms) |
| `dep-06-frame-900ms.png` | Dep settled (same as 600ms) |
| `dep-07-settled.png` | Dep final settled state |
| `animation-data.json` | Full viewBox data: 47 rAF frames per view + F56 style checks |
| `f56-spot-check.json` | Feature 56 measurement data (zoom target + highlight styles) |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00037-7cv` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 58: Tree combined pan+zoom | **PASS** — 47 rAF frames show smooth interpolation, 4 unique mid-animation screenshots |
| Feature 58: Dep combined pan+zoom | **PASS** — 47 rAF frames show smooth interpolation, 4 unique mid-animation screenshots |
| Feature 56 non-regression: Tree highlight | **PASS** — 3px border + 5px accent + two-layer halo |
| Feature 56 non-regression: Dep highlight | **PASS** — identical styles confirmed |
| Feature 56 non-regression: Zoom target | **PASS** — 19.6% of viewport (target ~20%) |

**Feature 58 (PR #135) is CONFIRMED WORKING on the live deployed site.**
Selecting a task node in either Tree or Dependency view now animates the viewport
pan and zoom together with a smooth 750ms ease-in-out transition. The viewBox
changes progressively across 47 animation frames (recorded via requestAnimationFrame),
and mid-animation screenshots show distinct intermediate viewport positions — NOT
an instant snap. Feature 56's ~20% zoom target and prominent highlight halo are
confirmed as non-regressed.
