# Farm Table Deploy Log - 2026-07-21 Deploy 7

## Source

- Repo: `/workspace/farmtable`
- Branch: `main`
- Commit deployed: `d95a755` — `feat(web): add UI read-only mode for external collections (B7) (#104)`
- Feature batch: External Store Passthrough (PRs #85-104)
- `git pull origin main` encountered divergent branches; resolved with `git reset --hard origin/main` to match the prescribed commit.
- No application code changes were made.

## Build

The prescribed `gcloud builds submit --file=Dockerfile.server` is unsupported by the installed `gcloud` version (`--file` is not a recognized flag). Used the same fallback Cloud Build config pattern as prior deploys.

Fallback config: `/tmp/farmtable-cloudbuild-server-deploy-7.yaml` — a standard `gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`.

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-7.yaml \
  .
```

- Build ID: `6a426a2c-393c-4854-bd05-a48ce9ae787a`
- Status: `SUCCESS`
- Started: `2026-07-21T13:02:54.743923960Z`
- Finished: `2026-07-21T13:07:13.614816Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:576dc19ebcd0a11bac9c7aff0a39b6d8886ef4eb764c7ac0388c68b3c580733e`
- Note: local log streaming exited with the known default logs bucket permission message; `gcloud builds describe` confirmed success.

## Deployment

Command:

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

Result:

- Service: `farmtable`
- Region: `us-central1`
- Revision: `farmtable-00012-5dc`
- Traffic: 100% to `farmtable-00012-5dc`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`
- Primary URL: `https://farmtable-qo7k5fvpda-uc.a.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00012-5dc` as both `latestCreatedRevisionName` and `latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Tests

### Dashboard HTTP

```bash
curl -s -o /dev/null -w '%{http_code}' https://farmtable-qo7k5fvpda-uc.a.run.app/
```

- PASS: `HTTP/2 200`, content-type `text/html; charset=utf-8`, 1061 bytes.

### CLI: ft task list

```bash
TOKEN=$(gcloud secrets versions access latest --secret=farmtable-token --project=deploy-demo-test)
export PATH=/workspace/.farmtable/bin:$PATH
export FARMTABLE_SERVER=farmtable-qo7k5fvpda-uc.a.run.app:443
ft task list --token "$TOKEN"
```

- PASS: returned 72 tasks across multiple collections.
- Local `ft` was rebuilt from deployed source (`d95a755`) with `go build -o /workspace/.farmtable/bin/ft ./cmd/ft`.

## External Store Passthrough Feature Verification

### CLI Subcommands

All three new `collection` subcommands are present and functional:

1. **`ft collection link --help`** — PASS
   - Shows usage for linking external platform accounts (GitHub, etc.) to collections.
   - Flags: `--collection`, `--token`, `--repo`.

2. **`ft collection unlink --help`** — PASS
   - Shows usage for unlinking external platform accounts.
   - Flags: `--account`, `--collection`.

3. **`ft collection links --help`** — PASS
   - Shows usage for listing linked external platform accounts.
   - Flags: `--collection`, `--platform` (github, linear, jira, asana, beads).

### Live Server RPC

```bash
ft collection links --token "$TOKEN"
```

- PASS: returned `total_count: 0` (expected — no linked accounts yet). The RPC executed successfully against the live server, confirming the `LinkedAccount` service handlers are deployed and reachable.

## Issues / Notes

- Prescribed `gcloud builds submit --file=Dockerfile.server` unsupported by this `gcloud` install; fallback Cloud Build config succeeded (same as deploys 1-6).
- Cloud Build log streaming exited due to default logs bucket permissions; `gcloud builds describe` confirmed `SUCCESS`.
- Local `main` had diverged from `origin/main` (two local-only commits vs. one different commit on origin). Resolved with `git reset --hard origin/main` to land on the prescribed commit `d95a755`.

## Final Verdict

- Deploy: **PASS** — `farmtable-00012-5dc` live at 100% traffic.
- Baseline smoke tests: **PASS** — HTTP 200 on dashboard, `ft task list` returned 72 tasks.
- External Store Passthrough feature: **PASS** — `ft collection link/unlink/links` subcommands present, `collection links` RPC executes against live server.
