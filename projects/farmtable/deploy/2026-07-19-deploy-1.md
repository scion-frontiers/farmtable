# Farm Table Deploy Log - 2026-07-19 Deploy 1

## Source

- Repo: `/workspace/farmtable`
- Branch: `main`
- Commit: `66b2b32d02159b52f38466f219799d4676fc9bd9`
- `git pull origin main`: already up to date

## Commands Run

```bash
gcloud auth list
gcloud config get-value project
git pull origin main
```

The prescribed `gcloud builds submit --file=Dockerfile.server` command was attempted first, but this installed `gcloud` does not support `--file` for `builds submit`.
Used an equivalent temporary Cloud Build config outside the repo to run Docker with `-f Dockerfile.server`:

```bash
gcloud builds submit --project=deploy-demo-test --config=/tmp/farmtable-cloudbuild-server.yaml .
```

The local command exited after source upload because the service account cannot stream the default Cloud Build logs bucket, but the remote build completed successfully.

```bash
gcloud builds describe 3b883d97-8974-4e25-ac6f-e59d67c5c5f2 \
  --project=deploy-demo-test \
  --format='json(status,finishTime,images,results.images)'
```

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

Smoke test commands:

```bash
curl -i https://farmtable-qo7k5fvpda-uc.a.run.app/
TOKEN=$(gcloud secrets versions access latest --secret=farmtable-token --project=deploy-demo-test)
export PATH=/workspace/.farmtable/bin:$PATH
export FARMTABLE_SERVER=farmtable-qo7k5fvpda-uc.a.run.app:443
ft task list --token "$TOKEN"
ft task create "smoke-test-1784467180" --token "$TOKEN"
ft task list --token "$TOKEN"
```

Browser verification:

```bash
playwright-cli install-browser chrome-for-testing
playwright-cli open --browser=chromium https://farmtable-qo7k5fvpda-uc.a.run.app/
playwright-cli resize 1440 1000
playwright-cli screenshot --filename=/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-1/dashboard-kanban-features.png
playwright-cli click <smoke-test task option>
playwright-cli screenshot --filename=/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-1/inspector-editable-fields.png
playwright-cli click <Edit description>
playwright-cli screenshot --filename=/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-1/inspector-description-edit.png
```

## Build Result

- Build ID: `3b883d97-8974-4e25-ac6f-e59d67c5c5f2`
- Status: `SUCCESS`
- Finished: `2026-07-19T13:18:56.179074Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:8d25ab108e59a812d556df5a9e880104c28774d77e4d95d602daaafa2949c0d1`

## Deployment Result

- Service: `farmtable`
- Region: `us-central1`
- Revision: `farmtable-00006-rx9`
- Traffic: 100% to `farmtable-00006-rx9`
- URL: `https://farmtable-qo7k5fvpda-uc.a.run.app`
- Deploy command output also reported: `https://farmtable-486315127503.us-central1.run.app`

## Smoke Tests

- Dashboard HTTP check: PASS, `HTTP/2 200` from `https://farmtable-qo7k5fvpda-uc.a.run.app/`
- Native gRPC `ft task list --token "$TOKEN"`: PASS, returned existing task `Test task from real client`
- Native gRPC create: PASS, created `smoke-test-1784467180` with ID `51dae6ed-7173-4da7-b7ff-f4e51682330f`
- Native gRPC post-create list: PASS, returned both existing task and `smoke-test-1784467180`
- Browser gRPC-Web: PASS, Playwright network showed `ListCollections`, `ListUsers`, and `WatchTasks` returning 200; UI displayed live tasks including `smoke-test-1784467180`

## Browser Auth Gap

The previous browser gRPC-Web auth-token gap appears resolved in this deployed revision. The dashboard loaded live task data and browser gRPC-Web requests returned 200. The only observed console error was a missing `/favicon.ico` returning 404.

## Screenshots

- `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-1/dashboard-kanban-features.png`
- `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-1/inspector-editable-fields.png`
- `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-1/inspector-description-edit.png`

## Issues Encountered

- The handoff build command used `--file=Dockerfile.server`, which this `gcloud builds submit` version rejects. Resolved by using a temporary Cloud Build config that runs `docker build -f Dockerfile.server`.
- Cloud Build log streaming failed locally due to default logs bucket permissions. The build itself completed successfully and was verified with `gcloud builds describe`.
- Playwright CLI had no browser installed initially. Installed `chrome-for-testing` with `playwright-cli install-browser chrome-for-testing` to capture required screenshots.
