# Farm Table Deploy Log — 2026-07-22 Deploy 29

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-29`)
- Commit deployed: `f1a86dc` — `fix(web): suppress poll-sync flicker — refresh spinner + redundant upsert events (F55) (#132)`
- Previous revision: `farmtable-00034-svn` (commit `5ca9037`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`5ca9037` / rev `farmtable-00034-svn`)

```
f1a86dc fix(web): suppress poll-sync flicker — refresh spinner + redundant upsert events (F55) (#132)
```

Key change in this deploy:
- **Feature 55: Fix Poll-Sync Flicker** (PR #132):
  - Removed `this.isRefreshing = true` from `onPollRefreshStart` — background
    polls no longer trigger the Refresh button spinner.
  - Added `this.isRefreshing = true` to `onManualRefresh` — spinner only
    appears when the user explicitly clicks the Refresh button.
  - Added deep equality check (`JSON.stringify`) to `TaskStore.upsert()` so
    background polls with unchanged data skip `tasks-changed` events,
    preventing unnecessary re-renders.
  - Added `onPollRefreshError` handler to clear `isRefreshing` on failed
    manual refreshes (pre-existing bug fix).

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-29.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-29.yaml \
  .
```

- Build ID: `23e36e95-e912-47cb-a658-ae91efc67feb`
- Status: **SUCCESS**
- Finished: `2026-07-22T20:37:01.378283Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:a8a74d7321e403ea9166845ceb8fc8a2c08c530974e17550a84e180a257ba16f`
- Note: Local log streaming exited with the known default logs bucket permission
  message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00035-rg4`**
- Traffic: **100%** to `farmtable-00035-rg4`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00035-rg4` as both `latestCreatedRevisionName` and
`latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' -L https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200 (via 302 redirect through IAP).

## Feature 55 Verification — Poll-Sync Flicker Fix (Playwright)

All verification performed with Playwright (Chromium headless, 1440x900 viewport)
against the live deployed URL `https://farmtable-486315127503.us-central1.run.app`.

IAP authentication was handled via a `Bearer` token obtained from
`gcloud auth print-identity-token` with the IAP OAuth client ID audience
(`486315127503-o94o6safp9v2865ddj9cupqftnst9jfn.apps.googleusercontent.com`),
passed as an `extraHTTPHeaders` `Authorization` header in the Playwright context.

**Method:** Used `page.route()` to intercept gRPC-Web
`farmtable.v1.FarmTableService/ListTasks` calls with a 3-second delay, making
the in-flight spinner state reliably observable. The `default` collection was
loaded and programmatically switched to polling mode via `switchToPolling()`.

### V1 — Background Poll In-Flight: No Spinner — **PASS**

1. Switched to polling mode; set poll interval to 2 seconds.
2. Background poll triggered; `page.route()` intercepted the ListTasks request
   and held it for 3 seconds.
3. During in-flight: `isRefreshing = false`, button `loading` attribute = `false`.
4. Screenshot `f55-03-background-poll-inflight.png` is **byte-identical** to
   `f55-02-polling-idle.png` (MD5 `bc7e694c...`) — zero DOM change during
   background poll.

### V2 — Manual Refresh In-Flight: Spinner Visible — **PASS**

1. Triggered manual refresh (sets `isRefreshing = true` before calling
   `pollManager.refresh()`).
2. `page.route()` intercepted the ListTasks request and held it for 3 seconds.
3. During in-flight: `isRefreshing = true`, button `loading` attribute = `true`,
   button `disabled` attribute = `true`.
4. Screenshot `f55-05-manual-refresh-inflight.png` has a **unique** MD5
   (`d76b83c8...`) — Shoelace loading spinner visually replaces the Refresh
   button text.

### V3 — After Manual Refresh: Spinner Cleared — **PASS**

1. After the delayed ListTasks response completed: `isRefreshing = false`,
   button `loading` attribute = `false`.
2. Screenshot `f55-06-manual-refresh-complete.png` shows the Refresh button
   returned to its normal state with updated timestamp.

### MD5 Hash Proof

| Screenshot | MD5 | Description |
|---|---|---|
| `f55-02-polling-idle.png` | `bc7e694c...` | Polling idle — Refresh button normal |
| `f55-03-background-poll-inflight.png` | `bc7e694c...` | Background poll **in-flight** — **identical to idle** |
| `f55-04-background-poll-complete.png` | `bc7e694c...` | Background poll complete — identical to idle |
| `f55-05-manual-refresh-inflight.png` | `d76b83c8...` | Manual refresh **in-flight** — **UNIQUE** (spinner visible) |
| `f55-06-manual-refresh-complete.png` | `67c2e530...` | Manual refresh complete — spinner cleared, timestamp updated |

**Key proof:** Screenshots 02, 03, and 04 are byte-identical (background poll
causes zero visual change), while screenshot 05 has a unique hash showing the
Shoelace loading spinner on the Refresh button.

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-29/`:

| File | Description |
|------|-------------|
| `f55-00-app-loaded.png` | Landing page: collection selector loaded |
| `f55-01-board-loaded-streaming.png` | Default collection loaded in streaming (Live) mode |
| `f55-02-polling-idle.png` | Polling mode idle — Refresh button visible, no spinner |
| `f55-03-background-poll-inflight.png` | **Key**: Background poll **in-flight** — identical to idle (no spinner) |
| `f55-04-background-poll-complete.png` | Background poll complete — same as idle |
| `f55-05-manual-refresh-inflight.png` | **Key**: Manual refresh **in-flight** — spinner visible on button |
| `f55-06-manual-refresh-complete.png` | Manual refresh complete — spinner cleared, timestamp updated |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00035-rg4` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 55: Background poll no spinner | **PASS** — `isRefreshing=false`, `loading=false`, byte-identical to idle |
| Feature 55: Manual refresh shows spinner | **PASS** — `isRefreshing=true`, `loading=true`, unique MD5 |
| Feature 55: Spinner clears after refresh | **PASS** — `isRefreshing=false`, `loading=false` |

**Feature 55 (PR #132) is CONFIRMED WORKING on the live deployed site.**
The Refresh button spinner no longer flickers on background poll ticks. It
only appears when the user explicitly clicks the Refresh button, and correctly
clears after the refresh completes. Background polls with unchanged data also
skip redundant `tasks-changed` events. All verified using Playwright against
the live deployment with real IAP authentication and `page.route()` request
interception to reliably capture the transient in-flight state.
