# Farm Table deploy 5 — tree view infinite-growth fix

**Completed:** 2026-07-20 UTC  
**Source commit:** `6a7cafe` (`fix: add display:block to tree view SVG to stop infinite resize loop`)  
**Cloud Build:** `fb7a0125-b36f-42ad-98e8-cd18b1cc0c8e` — `SUCCESS`

## Rollout

Built `Dockerfile.server` and deployed image
`us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
to Cloud Run service `farmtable` in `us-central1`.

- New ready revision: `farmtable-00010-qdh`
- Traffic: `farmtable-00010-qdh` at **100%** (`latestRevision: true`)
- Dashboard URL check: `https://farmtable-qo7k5fvpda-uc.a.run.app/` returned **HTTP 200**.

## Live tree-view stability verification

Target:
`https://farmtable-qo7k5fvpda-uc.a.run.app/?collection=5d1e4eea-3dc7-4958-99ac-01e3372c5a0d&view=tree`

Method: Playwright Chromium, headless, 1440×900 viewport. After the tree SVG
rendered, sampled `document.documentElement.scrollHeight` and the
`.canvas-container` bounding height inside `ft-tree-view`'s shadow DOM at 0, 5,
and 10 seconds.

| Time | Document height | Canvas height | SVG display | Canvas min-height | SVG viewBox height |
|---:|---:|---:|---|---|---:|
| 0 s | 1200 px | 1056 px | `block` | `0px` | 3520 |
| 5 s | 1200 px | 1056 px | `block` | `0px` | 3520 |
| 10 s | 1200 px | 1056 px | `block` | `0px` | 3520 |

Measured live growth over 10 seconds: **0 px** for both document and canvas
(**0 px/s**).

For comparison, the pre-deploy production investigation measured 2400 px growth
over the same interval (240 px/s): document 1696 → 4096 px and canvas 1552 →
3952 px. The live deployed service is stable and the infinite-growth bug is
fixed.
