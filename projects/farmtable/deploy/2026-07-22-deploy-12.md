# Farm Table Deploy Log — 2026-07-22 Deploy 12

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-12`)
- Commit deployed: `146b3be` — `feat(web): animate tree-view centering on task selection (#113)`
- Previous revision: `farmtable-00017-wnn` (commit `8ac4bc0`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`8ac4bc0` / rev `farmtable-00017-wnn`)

```
146b3be feat(web): animate tree-view centering on task selection (#113)
```

Key feature in this deploy:
- **Feature 41** (PR #113): 750ms ease-in-out animated centering of the tree-view canvas when
  a task is selected via any trigger (Inspector Relationships click, command palette, direct
  tree-node click). Uses `requestAnimationFrame` to smoothly animate `panX`/`panY` and update
  the SVG `viewBox` over the transition period. This is a pure UX-polish feature — no
  functional/data changes.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-12.yaml` (standard `gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-12.yaml \
  .
```

- Build ID: `c11f9fd8-c1e8-4002-b1cf-6c5685e402b9`
- Status: **SUCCESS**
- Started: `2026-07-22T01:03:23.460339789Z`
- Finished: `2026-07-22T01:07:33.006289Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:24457c1b9bacd752fb006c7cc7fc6781a290b715b39483cf20b1504ece815756`
- Note: Local log streaming exited with the known default logs bucket permission message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00018-jmx`**
- Traffic: **100%** to `farmtable-00018-jmx`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`
- Primary URL: `https://farmtable-qo7k5fvpda-uc.a.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00018-jmx` as both `latestCreatedRevisionName` and `latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200.

## Feature 41 Verification — Tree-View Centering Animation (Playwright)

All verification performed with Playwright (Chromium headless, 1440x800 viewport) against the live
deployed URL `https://farmtable-486315127503.us-central1.run.app`.

The animation uses `requestAnimationFrame` to smoothly animate the SVG `viewBox` over 750ms with
ease-in-out easing. Verification captures the `viewBox` attribute at ~75–95ms intervals to confirm
multiple intermediate values exist (not a snap), proving smooth animation.

### V1 — Animation on First Node Click — **PASS**

Selected task `75df7733` (third node in tree layout, position 370,180) via `task-select` event
dispatch. Captured 12 frames across the 750ms animation window.

- **7 unique viewBox values** observed across 12 frames
- ViewBox X panned from `-40` → `-1860` (1820px of horizontal travel)
- ViewBox Y panned from `-906.5` → `-859.0` (47.5px of vertical travel)
- Progressive intermediate values confirm smooth ease-in-out:
  - Frame 0: `-40, -906.5` (start)
  - Frame 1: `-104.8, -904.8` (slow start — ease-in)
  - Frame 2: `-392.1, -897.3` (accelerating)
  - Frame 3: `-910.0, -883.8` (fast middle)
  - Frame 4: `-1455.5, -869.5` (decelerating)
  - Frame 5: `-1771.8, -861.3` (slow end — ease-out)
  - Frame 6+: `-1860, -859.0` (settled)

Inspector opened showing the "foobar" task details (Triage phase, created Jul 19, description "bon").

### V2 — Animation on Second Node Click (Different Direction) — **PASS**

Selected task `1015aab2` (first/root node, position 110,40) from the settled position of test 1.
This tests animation in the reverse direction. Captured 10 frames at 95ms intervals.

- **7 unique viewBox values** observed across 10 frames
- ViewBox X panned from `-1860` → `-1434.2` (425.8px reverse travel)
- ViewBox Y panned from `-859.0` → `-999.0` (140px vertical travel)
- Progressive intermediate values confirm smooth ease-in-out:
  - Frame 0: `-1860, -859.0` (start)
  - Frame 1: `-1844.9, -864.0` (slow start)
  - Frame 2: `-1777.6, -886.1` (accelerating)
  - Frame 3: `-1637.8, -932.0` (fast middle)
  - Frame 4: `-1505.3, -975.6` (decelerating)
  - Frame 5: `-1441.0, -996.8` (slow end)
  - Frame 6+: `-1434.2, -999.0` (settled)

### V3 — Centering Accuracy (Feature 37 Regression Check) — **PASS**

After animation 2 settled, verified centering accuracy:
- View center: `(110, 40)`
- Target node position: `(110, 40)`
- **Offset: (0.0, 0.0)** — perfect centering

No regression to Feature 37's centering accuracy.

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-12/`:

| File | Description |
|------|-------------|
| `00-collection-selector.png` | Initial app load (collection picker) |
| `01-board-loaded.png` | Board view after selecting "default" collection |
| `02-tree-view-initial.png` | Tree view initial state (19 nodes visible) |
| `03-anim1-frame-00.png` | V1: Pre-animation state |
| `03-anim1-frame-01.png` | V1: ~75ms — slow start (ease-in) |
| `03-anim1-frame-02.png` | V1: ~150ms — accelerating |
| `03-anim1-frame-03.png` | V1: ~225ms — fast middle, Inspector opens |
| `03-anim1-frame-04.png` | V1: ~300ms — decelerating |
| `03-anim1-frame-05.png` | V1: ~375ms — slow end (ease-out) |
| `03-anim1-frame-06.png` through `03-anim1-frame-10.png` | V1: Settled (animation complete) |
| `04-anim1-complete.png` | V1: Final settled state |
| `05-anim2-frame-00.png` | V2: Pre-animation (same as anim1 settled) |
| `05-anim2-frame-01.png` through `05-anim2-frame-08.png` | V2: Progressive animation frames |
| `06-anim2-complete.png` | V2: Final settled state |
| `07-inspector-state.png` | Inspector showing selected task details |
| `08-final-overview.png` | Final tree view state |
| `verification-summary.json` | Machine-readable verification data |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00018-jmx` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| V1: Animation on node click | **PASS** — 7 unique viewBoxes, smooth ease-in-out over ~375ms of captured motion |
| V2: Animation on second click | **PASS** — 7 unique viewBoxes, smooth reverse animation |
| V3: Centering accuracy | **PASS** — offset (0.0, 0.0), perfect centering |

**Feature 41 (750ms ease-in-out animated tree-view centering) is CONFIRMED WORKING on the live deployed site.**
**Feature 37 centering accuracy is CONFIRMED STILL WORKING (no regression).**
