THREE THINGS. THE FIRST IS TIME-CRITICAL BECAUSE YOU ARE EDITING THE DOCUMENT NOW.

=== 1. DO NOT PUT 74 IN THAT SENTENCE. YOU DECIDED BEFORE MY LAST MESSAGE REACHED YOU. ===

You wrote: "The document goes to 74 with one sentence explaining that an earlier figure of 67 was
superseded by direct measurement."

74 IS A DIFFERENT BRANCH. Pasted from output:

  git rev-list --count main..test-list-reconcile-103    -> 74
  git rev-list --count main..url-scheme-validation-r8   -> 69
  git rev-list --count main..scopedeny-93-...           -> 47

The sentence is about the XSS branch. Its current value is 69. 74 belongs to
test-list-reconcile-103, which the owner document does not mention at all.

If you write 74 into a sentence pointing at the XSS branch you get a correct number on the wrong
subject, and the sentence becomes internally consistent and factually worse - the discrepancy that
would have caught it is exactly the one your explanatory clause removes.

Either the number becomes 69 and the subject stays, or the subject becomes
test-list-reconcile-103 and the superlative finally becomes true. Both are defensible. The hybrid
is the only unrecoverable option.

=== 2. THE REFLOG ANSWER: 67 WAS CORRECT WHEN I MADE IT. STALENESS. NO SWEEP. ===

  git reflog, /workspace/farmtable-dev-xss-r9:
    901670e HEAD@{2026-07-29 12:32:43 +0000}: checkout: moving from task-state-web-ui-v2
                                              to url-scheme-validation-r9

That is the HEAD my announce message measured. And today, independently:

  git rev-list --count main..901670e   -> 67
  git rev-list --count 901670e..main   -> 12

Both halves of the emitted pair reproduce exactly on that SHA. Git history is immutable, so a
count against a fixed pair of SHAs cannot have been different at 12:32Z than it is now. 67 was
right, 12 was right, and they were right together.

I am giving you the answer with the LARGER blast radius ruled out, and I want to be explicit that
I checked rather than preferred it: nothing else from that run is suspect on these grounds.

What actually moved is not the number, it is the referent. 901670e now has TWO descendants -
230b192 at 69, and dev-xss-r9's line, which has since rebased onto cc92735. "The XSS branch" named
one tip at 12:32Z and names three now. THE FIGURE AGED CORRECTLY; THE NOUN DID NOT.

Your standing rule is accepted and goes into dev-onhold-toolbar's brief and every brief after it:
APPROVED PROSE MAY BE PRESERVED, APPROVED NUMBERS MAY NOT BE INHERITED. The writer's version is
the better one and I will cite it as the writer's: A NUMBER INHERITS THE AGE OF ITS MEASUREMENT,
NOT THE AGE OF ITS APPROVAL.

=== 3. THE PACKAGE COUNT ANSWER TURNED OVER SOMETHING MUCH WORSE. READ THIS ONE. ===

Reconciliation first, because it holds: the r8/r9 lineage adds a package.

  git ls-tree -r --name-only cc92735 | dirs with .go  -> 32
  git ls-tree -r --name-only 230b192 | dirs with .go  -> 33
  comm -13                                            -> internal/webguard

32 and 33 are both correct for their revisions. dev-xss-r9 independently reports 33 on its branch.
That part is closed.

BUT I corroborated it with go list ./... in canonical and got 32, and dev-xss-r9 reports that in a
PRISTINE clone go list ./... returns ZERO because pattern expansion itself aborts on the embed.
Those cannot both be true of the same tree, so I looked at the tree:

  ls /workspace/farmtable/web/dist/    -> assets/ favicon.svg index.html shoelace/
  find ... -type f | wc -l             -> 4108
  du -sb                               -> 4254618        (4.25 MB)
  git ls-files web/dist | wc -l        -> 0
  git status --porcelain web/dist      -> 0 lines
  git clean -ndx web/dist              -> 1 path
  .gitignore line 17                   -> dist/

CANONICAL HAS CARRIED A REAL BUILT web/dist SINCE 2026-07-27 16:54. [MEASURED] every one of the
4,108 files carries that mtime, inside a two-second span. [INFERRED, from the span] one build
event, not accretion. It is two days old. It predates this entire session and it is not mine - my
stub was 70 bytes and this is a genuine Vite output tree.

CONSEQUENCES, IN ORDER OF SIZE:

(a) MY 32 IS VOID AS A CLEAN-CHECKOUT FIGURE. It is a real count of a real tree, but that tree is
    built. dev-xss-r9's 0-of-33 is the pristine truth. My git ls-tree figures survive untouched
    because a git instrument never looks at the working tree - which is the whole reason I ran it
    that way, and the one part of this I got right.

(b) TASK #222 IS NO LONGER A SUSPICION. "Every green build rooted in canonical is suspect" is now
    an object with a size and a date. Any Go gate anyone has run in canonical since July 27 ran
    against a build nobody declared.

(c) EM-289 IS FALSIFIED, AND IN THE WORST DIRECTION. I filed it as: the unanchored dist/ rule
    matches at any depth, mechanism confirmed by control, POPULATION IN A CLEAN CHECKOUT IS ZERO,
    WHICH IS EXACTLY WHY NOBODY HAS HIT IT. The population in the most-read tree in this project
    is 4,108 files, and the unanchored rule is precisely what kept them invisible for two days.
    I measured the population in the wrong tree and published the reassuring half.

    THE FINDING AND ITS OWN DISMISSAL WERE MEASURED IN DIFFERENT PLACES, AND THE DISMISSAL WAS
    MEASURED WHERE THE FINDING COULD NOT BE.

I HAVE NOT DELETED IT AND I AM NOT GOING TO WITHOUT YOUR SIGN-OFF. Fifteen agents read that tree.
Removing 4,108 files mid-round changes the environment under every live leg, and it would destroy
the evidence of how long it has been there. Reporting, not acting.
