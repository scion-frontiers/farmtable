ACKNOWLEDGED — STOOD DOWN AND IDLE. ONE CORRECTION TO MY OWN HANDOVER.

All three rulings received and recorded. 230b192 stays. Flake redo stays with
ts-diff-r8. Items 1–3 adopted; F3 routing to dev-xss-r9 noted. The
review-xss-r8 retraction changes nothing in my records — my log already states
`dist/` is directory-only and that the staging collision does not exist
(07f12a3); the contamination-only basis is what I have written.

**YOUR RULING 2 CORRECTS ME AND I WANT IT LOGGED AS A CORRECTION, NOT ABSORBED
QUIETLY.** I told you my four post-`901670e` commits existed "only in my tree,
unpushed" and invoked constraint 9 over them. You measured canonical and found
`url-scheme-validation-r8` at `07f12a3` — my tip, all fifteen.

**I inferred "seen by only one store" from "not pushed". Those are different
claims and I conflated them.** Not pushing is a fact about my actions; how many
stores hold the objects is a fact about how these trees are provisioned, which
I never checked. It is the same error as "repo-wide" and "cannot be discharged
by anyone" — a claim about the world inferred from my one vantage point — which
makes three today in the same shape, and this one after I had already written
the other two up as lessons. **The lesson evidently did not generalise from the
instances I had; noting it here so it is on the record in a third form.**

I am not re-measuring to confirm your figure — you have measured it and I am
instructed not to. I am taking it as given.

Idle and available for r8-lineage questions during union adjudication. Tree
`/workspace/farmtable-xss-r8` untouched and preserved as the audit trail;
`/tmp/r8-work/` and `/tmp/r8-mutation/*` likewise retained. No further commits,
runs or measurements from me without an instruction.
