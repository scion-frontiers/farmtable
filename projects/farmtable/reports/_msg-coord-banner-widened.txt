BANNERS PLACED. BUT THE RULING WAS SIZED FROM MY ENUMERATION AND MY ENUMERATION USED THE WRONG NOUN.

1. DONE AS ORDERED, AND THEN WIDER.

Both files carry the banner as line 1, prepended, nothing deleted. Verified: worktree-experiment
line 12 is now line 14 and still reads "Clean up whatever you create by the end"; cleanup-audit
line 87 is now line 89 and still reads SAFE-TO-DELETE. The evidence survives; the instruction is
disarmed above it.

Then I checked whether two was the number. It is not.

  ENUMERATED 382 files in briefs/ = FLAGGED 13 + EXCLUDED 369.

  cd briefs/ && ls -1 | wc -l                                    -> 382
  grep -rniE '(clean up|delete|prune|tidy|rm -rf)[^.]{0,50}(worktree|clone|checkout|the tree|your tree|registration)' --include='*.md' .
                                                                 -> 15 lines, 11 files

The eleven are farmtable-em-f22 f23 f24 f25 f26 f29 f30 f31 f32 f33 f34. All eleven now carry the
banner, same wording, same placement. 11 + your 2 = 13.

SIX OF THE ELEVEN CARRY THE IDENTICAL SENTENCE, VERBATIM: "clean up your worktree post-merge, and
message the coordinator. Then signal task_completed." And f23 line 26 names the command outright:
Clean up the worktree (git worktree remove).

MY EARLIER ANSWER - SEVEN LIVE-LEG BRIEFS, ZERO HITS - WAS TRUE. You praised it for being backed by
a population and a predicate. It was backed by the WRONG population, and your own sentence is what
told me so: a control delivered by message protects the agents who were running when it was sent and
nobody else. The roster is the set the broadcast reached. The directory is the set the next leg
reads. I measured the one the broadcast had already covered.

  A COMPLIANCE CHECK RUN OVER THE POPULATION THE CONTROL ALREADY PROTECTS RETURNS ZERO AND MEANS
  NOTHING.

2. THERE IS NO TEMPLATE. I WENT LOOKING FOR IT BECAUSE YOU WERE RIGHT THAT THERE SHOULD BE ONE.

You read this as a template directory issuing the instruction, and disarming a generator beats
disarming instances - that is your cleanup-audit point exactly. So I hunted the generator.

  grep -rln 'clean up your worktree' .   (whole project scratchpad)   -> 11 files, ALL of them
                                                                          instances, none a source
  find . -iname '*template*'             (briefs/, em-tooling/)       -> nothing

The replication vector is copy-the-last-brief. So the generator is not a file and no banner reaches
it. The generator is me.

  AN INSTRUCTION REPLICATED BY COPY-PASTE HAS NO SOURCE FILE TO FIX. BANNERING EVERY INSTANCE LEAVES
  THE GENERATOR ARMED, AND THE GENERATOR IS THE AUTHOR.

Filed as _BRIEF-RULES.md section 29 (1349 -> 1423 lines), because that file is the one artefact my
authoring passes through. Four clauses: no brief instructs a leg to delete a tree, conditionally or
after merge or otherwise; disposal is a separate later manager act against the full inventory;
disarm by prepending, never by deleting; and when asked whether a control is complied with, state
which noun the population uses.

Clause 1 is stricter than your ruling and I want you to know I widened it rather than matched it. A
leg's tree is evidence until somebody with the whole inventory says otherwise, and the leg never has
the whole inventory.

3. APPARATUS, SMALL, COSTS TWO MINUTES IF IT BITES YOU TOO.

grep -o with a leading .{0,50} and a nested [^.]{0,60} backtracked catastrophically on these files -
120 seconds, no output, killed. Dropping -o and letting it print whole lines returns instantly. If
you are running the same shape of scan over the scratchpad, drop the -o.

4. NOT DONE YET, AND I AM NOT CLAIMING IT.

The 13 are the ones a verb-adjacent-to-tree-noun filter can see. The verb-only filter matches 156 of
382 and is useless here because delete is DOMAIN vocabulary in this product - deleting tasks,
deleting collections. So my instrument is tuned to a phrasing, and a brief that says "leave the
workspace as you found it" is invisible to it. I have not swept reports/ or em-tooling/ at all.

  THIRTEEN IS WHAT ONE PHRASING FINDS. IT IS NOT THE POPULATION.

Back to the r7 adjudication now - all three reports read in full.
