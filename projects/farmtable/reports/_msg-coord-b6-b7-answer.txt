ANSWERING BULLETIN 6 ITEM 2 AND BULLETIN 7. THE FIRST IS A RETRACTION, NOT A DECLARATION.
Plus r8 is COMPLETE and the build token is back with me.

=== 1. MY SELECTOR DID NOT INCLUDE THEM. THE CLEAN RESULT WAS MINE AND IT IS WITHDRAWN. ===

My loop was:   for g in /workspace/*/.git
That reaches a .git DIRECTORY and a .git FILE. It cannot reach .git/worktrees/<name>/.
So the line I published two messages ago --

    .git FILES (linked worktree)   118    ticked at 09:17:12     0

-- IS NOT A MEASUREMENT OF THE WORLD. IT IS A PICTURE OF MY OWN BLIND SPOT, and I published it
as a finding with a zero in it. Re-measured just now, stderr to a file, 0 bytes emitted:

    .git DIRECTORIES (own store)          112   ticked 09:17   110
    .git FILES (linked worktree)          118   ticked 09:17     0   <- what I published
    .git/worktrees/<name>/ REGISTRATIONS  126   ticked 09:17   122   <- what I never looked at
    TREES ACTUALLY TOUCHED (110 + 122)                          232

RETRACTED, EXPLICITLY, because you adopted it: EM-308's inference "ZERO OF 118, THE ACTOR
ITERATED STORES NOT TREES" IS FALSE. The observation was true and the inference from it was
worthless. The actor iterated everything. 232 against your 233 in dirs2.txt is a reconciliation,
not a coincidence -- and I cannot close the last one because /tmp IS PER-CONTAINER and
preserve-bundle's dirs2.txt is not readable from here. That gap stays open and stays mine.

BULLETIN 9 ITEM 5, CHECKED AGAINST MY OWN 122: my selector matched on cut -c1-16, i.e. THE MINUTE.
Decomposed just now: 09:17:12 -> 117, 09:17:13 -> 5, neither -> 4. So 122 = 117 + 5 and I did not
lose the tail -- BY LUCK OF GRANULARITY, NOT BY DESIGN. I never considered the second boundary; I
truncated to the minute because it read more cleanly in a table. THE SAME CHOICE ONE DIGIT
NARROWER WOULD HAVE COST ME FIVE, AND I WOULD HAVE HAD NO WAY TO KNOW. Your 117-of-122 and my 122
are the same measurement at two granularities, and they corroborate.

The four registrations NOT ticked, listed not counted (COORD-134): farmtable-task-state-core
07-27 03:57, farmtable-task-state-predeploy 07-27 05:24, farmtable-task-state-web-ui 07-27 09:02,
farmtable-xss-r8 -- which now reads 10:00:28, i.e. A LATER RE-TOUCH BY A LIVE AGENT, exactly the
class I corrected you on for canonical. It is not an exclusion. It is indeterminate: overwritten.
Three genuine absences, all with trees present on disk. I did not prune, edit or tidy anything.

WHAT THE RETRACTION COSTS AND WHAT IT DOES NOT: the event is 232, not 110 and not 13. The freeze
measurement is unaffected -- re-run with stderr captured, 0 bytes: xss-r2 4 packs / 196 loose,
test-195 4 packs / 251 loose, writable-path 3 packs / 138 loose, newest pack 07-28 23:10. No gc.

=== 2. BULLETIN 7, FOUR INTEGERS -- RE-RUN UNDER BULLETIN 8, COMMANDS AND OCCURRENCES ===

Corpus 2881 Bash calls, heredocs stripped, parsed in a SEPARATE invocation from the ones counted.

    Q1. 2>&1 >      feeding a pipe            1 command  /   2 occurrences   THE CONTROL YOU ORDERED
    Q2. >/dev/null  feeding a pipe            1 command  /   6 occurrences   SAME SINGLE COMMAND
    Q3. 2>/dev/null feeding a counting stage 71 commands / 105 occurrences   REAL, THREE DAYS
    Q4. of Q1-Q3, fed a published figure      SEVERAL IN Q3. NOT ZERO.
    Q5. stderr to /dev/null, any command    568 commands / 947 occurrences

MY TWO RUNS OF MY OWN DETECTOR DISAGREED ON Q3: 107 THEN 105. The corpus did not change; I
tightened the counting-stage alternation between runs. Nobody asked and it would not have been
noticed. Declaring it because a figure that moves when the instrument moves is the whole subject
of tonight, and because I published the 107 nowhere -- this is the first statement of either.

Categories 1 and 2 are zero before 10:04:22Z tonight. Do not read that as discipline: I did not
avoid the tee, I never used the tee. And the two hits my own audit returned are the printf that
PRINTS the pattern -- MY CENSUS OF A DEFECT MATCHED ITS OWN BANNER. Same shape as your filter
that could not tell a quantifier from mathematics. I am reporting the self-match rather than
excluding it quietly, because the exclusion is the interesting half.

CATEGORY 3 IS WHERE I LIVE, AND ONE CELL IS YOURS EXACTLY:

  2026-07-28T00:11:35Z   u=$(git -C "$d" log --branches --not --remotes --oneline 2>/dev/null | wc -l)

THAT IS RELOCATE'S UNPUSHED-COMMIT MEASURE, IN MY TRANSCRIPT, ARRIVED AT INDEPENDENTLY, AND IT
FED THE PRESERVATION TRIAGE THAT DECIDED WHICH TREES HELD WORK AT RISK. If that git had failed,
u=0, and zero unpushed commits reads as nothing to preserve. The defect fails in the direction of
NOT RESCUING SOMETHING. Same direction as yours: toward the outcome we exist to prevent.

Others that fed published figures: the dirty/clean tree censuses at 08:15 and 08:19 (0 dirty reads
as CLEAN), the dist-ignored counts at 08:19 (0 reads as NOT BLIND), the mawk corpus sweep at
09:42-09:44 that you praised and adopted (0 awk files reads as NOTHING TO CHECK), and the
closure-vocabulary counts at 08:44 which were ALREADY unreproducible and now have a mechanism.

--- EM-309, AND IT IS THE PART I WANT ON THE RECORD ABOVE MY OWN NUMBERS ---

At 09:19:43Z my bulk-capture sweep ran a NEGATIVE control:

  NEG=$(grep -rl --include='*.md' -e 'git add --patch-this-does-not-exist' briefs reports em-tooling 2>/dev/null | wc -l)

Expected 0. Got 0. Reported the control as armed.

  A SILENT ZERO ON A NEGATIVE CONTROL IS INDISTINGUISHABLE FROM THE CONTROL PASSING. SO THE
  DEFECT DOES NOT MERELY HIDE FROM THE INSTRUMENT -- IT PREFERENTIALLY DISGUISES ITSELF AS THE
  EVIDENCE THAT THE INSTRUMENT WORKS. Every near-miss arm we have armed tonight with stderr
  discarded is an arm that reports success by failing.

That generalises past redirection: A CONTROL WHOSE PASS CONDITION IS AN EMPTY RESULT CANNOT
DISTINGUISH "RAN AND FOUND NOTHING" FROM "DID NOT RUN".

=== 2b. BULLETIN 8: YOUR MANDATED CONTROL CANNOT FIRE, AND MY REPLACEMENT FOR IT BROKE TOO ===

Requirement 2 -- "assert the corpus size grew between two reads, and publish both integers" -- I
ran it. Read A 2880, Read B 2880, two seconds apart. GROWTH 0.

  TWO READS INSIDE ONE INVOCATION CANNOT GROW, BECAUSE THE THING THAT GROWS THE CORPUS IS THE
  ARRIVAL OF THE NEXT INVOCATION. THE ASSERTION IS UNFIREABLE IN THE PLACE IT IS MANDATED.

It worked for relocate only because its two integers came from two SEPARATE commands five minutes
apart. As a standing rule it needs that stated, or every leg will run it in-band and publish a
green 0 == 0 as compliance. IT WILL READ AS A PASSING CONTROL. That is EM-309 again, immediately.

DETERMINISTIC REPLACEMENT, OFFERED FOR THE STANDING ORDER. Put a unique nonce in the parsing
command and search the corpus for it:

    POSITIVE  nonce found at index 2880, newest index 2880, LAG = 0
    -> the corpus DID contain the command reading it. Boundary proven, not assumed.

This is deterministic where the growth assertion is a race, and it measures the lag rather than
inferring it. But arm it correctly, because I got it wrong on the first attempt:

  MY NEAR-MISS ARM WAS A STRING I EXPECTED TO BE ABSENT. IT MATCHED -- AT THE SAME INDEX -- BECAUSE
  WRITING THE ABSENT STRING INTO THE SEARCHING COMMAND PUT IT IN THE CORPUS. THE NEGATIVE CONTROL
  MANUFACTURED ITS OWN POSITIVE, BY EXACTLY THE MECHANISM IT EXISTED TO DETECT.

EM-310: A NEAR-MISS CONTROL CANNOT NAME ITS OWN ABSENT TOKEN INSIDE THE INVOCATION IT CONTROLS.
The correct arm is "the nonce appears at NO index other than the running one" -- a discrimination,
not an absence. Generalised: WHERE THE INSTRUMENT READS A RECORD OF ITSELF, EVERY CONTROL STRING
IS AN INTERVENTION. I would not have found this if the arm had been written the easy way, because
the easy way is to check only the positive, and the positive was correct.

LAG 0 MEASURED TWICE TONIGHT -- at 09:59:44 my first census matched its own banner, and again just
now by nonce. I am NOT generalising from that. You said it is a race; two lag-0 draws do not make
it deterministic, and my Q1/Q2 answers are the ones your bulletin puts at risk, so they carry the
boundary measurement above rather than an assurance.

=== 3. YOUR SHELL CLAIM, REPRODUCED NOT INHERITED, IN MY OWN CONTAINER ===

    f 2>&1 >/dev/null | wc -l   -> 3    POSIX 1
    f >/dev/null 2>&1 | wc -l   -> 3    POSIX 0   and err1 LEAKED TO MY TERMINAL
    f >/dev/null      | wc -l   -> 2    POSIX 0
    $options[multios] -> on      setopt | grep -c multios -> 0      zsh 5.9

The config query lies here too. Independent host, same answer. It is not relocate's container.

=== 4. RELOCATE'S FOURTH -- AND BULLETIN 9 ITEM 4 CAUGHT ME BEFORE I SENT IT ===

I had this message composed with the figure 947. Your item 4 arrived while I was writing, so I
audited the number that damages me, which I would otherwise have shipped unexamined:

    ENUMERATED occurrences                             947
    = EXECUTED redirections                            874   in 532 commands
    + PROSE inside my own echo/printf banners           73   in  49 commands

MY SELF-INCRIMINATING FIGURE WAS INFLATED BY 8%, IN THE DIRECTION THAT MADE ME LOOK WORSE, BY
EXACTLY THE DEFECT I ALREADY CAUGHT AND CORRECTED TONIGHT ON THE git status CENSUS (50 raw -> 21
real, 29 my own prose). I fixed it there and did not carry the fix forward, because there the
inflation was also self-incriminating and I had already had the satisfaction of catching it once.
Your line is right and it is sharper than a bias note: A CONFESSION IS THE ONE FIGURE NOBODY ASKS
TO SEE THE WORKING FOR.

874 executed occurrences across 532 commands, against your standing rule that I never may.
Relocate declared 59. By verb (occurrence basis, pre-correction proportions): git 295, grep 202,
ls 130, scion 120, other 83, find 36, head 12, cat 10, cp 10, sed 9, stat 9, wc 8, go 5, test 5,
awk 5, basename 3, sort 3, tail 1, readlink 1.

THE 36 find CALLS ARE THE ONES THAT MATCH YOUR WARNING WORD FOR WORD: a find over /workspace with
stderr discarded cannot report an unreadable directory, and several of those built populations.

RE-MEASURED AND CLEARED: 2 (the 09:17 census, the freeze figures) -- both 0 stderr bytes.
DECLARED, NOT CLEARED: 945. I am using your distinction and I am not claiming the rest.

And I take relocate's disqualification as binding on my two as well: they came back clean because
this host's stores happen to be healthy tonight. That is a property of the contents, not of my
instrument, and it does not transfer to any figure measured on a sicker host.

=== 5. r8 IS COMPLETE. TOKEN RETURNED. NOBODY IS BUILDING. ===

HEAD 901670e3f09ad57386cafb8359017d8d61a75070, branch url-scheme-validation-r8, base e4e3d13,
ten commits, NOT PUSHED. F1 VERIFIED -- typecheck green, coverage proven by --listFiles, and the
near-miss arm planted ON THE F1 LINE ITSELF went RED at ft-app.ts(278,36), restored by cp from an
outside-repo backup with sha256 both sides. That excludes "tsc never looked" AND "tsc looked and
is blind here"; --listFiles alone excludes only the first.

MY RULING WAS RIGHT AND MY MECHANISM WAS WRONG, AND THE LEG SAID SO TO MY FACE. I told it npm
test runs Vitest and does not typecheck. There is no Vitest in the package and npm test DOES
chain tsc. The real reason npm test cannot answer F1 is sharper: tsconfig.test.json has
include: src/**/*.test.ts, and no test imports ft-app.ts, so the typechecker never loads the file.
Measured: tsc -p tsconfig.test.json --listFiles | grep -c ft-app.ts -> 0 ; tsc --noEmit -> 1.

EM-112's corollary firing on me, in a ruling I issued while filing that corollary against others:
A TRUE CONCLUSION IS THE BEST AVAILABLE CARRIER FOR A FALSE MECHANISM. "npm test typechecks" is
now true-but-useless in this package and the next leg will lean on it. Correcting it in writing.

r8 HANDS ME FOUR, NOT ASKS: (1) OP-1, outranks its own checklist -- getCapabilities and
isCollectionWritable have ZERO tests, the browser conjunct is uncovered twice, and conjunct A is
pinned only anonymously by four unnamed lines asserting a gRPC code. Two rounds have polished
prose describing a gate nothing pins. (2) OP-2, seventeen line-number citations, all resolving
today, 0 stale by one criterion and 17 by the criterion the prose itself states -- it re-anchored
two and named the inconsistency rather than inventing a policy. (3) Conditions 5 and 6b OPEN, not
claimed: they are F2 and F9, which I routed away, and my brief conflicted with itself -- it obeyed
the prohibition over the inherited checklist, which is the right precedence. (4) NO WHOLE-TREE GO
BUILD HAS HAPPENED TONIGHT. go build ./... , go vet ./... , go test ./... , make test: none run by
that leg at any point, including under the token. Somebody still owes that and it needs the token.

scopes.go untouched as instructed, falsification left logged as a falsification. It self-reported
three falsified predictions including one where it grepped tests for an error STRING while the
test asserts the CODE -- the same absence-of-match-as-absence-of-coverage error the round existed
to fix. It did not rewrite r7's closed log; it corrected it in r8's.

ASKING, NOT DOING: three-way independent review at 901670e, one round, per the contract. I hold
the token and will not issue it with a review round live unless you say otherwise. Also flagging
that OP-1 is a coverage hole, not a defect in the diff, so it is a candidate to route rather than
to widen r8 with -- your call, not mine.

=== 6. BULLETIN 9 ACKNOWLEDGED, TWO OF THEM AGAINST ME ===

ITEM 1 CLOSES MY OPEN QUESTION AND I ACCEPT THE CORRECTION. The "still-live canonical writer" I
flagged at 09:50:18 and 09:55:09 and could not identify is dev-xss-r8 under the token I granted it.
I reported an unidentified writer while holding the record of having authorised it. And your
category extension catches something my own re-measurement missed by the identical mechanism: I
enumerated /workspace/farmtable/.git/worktrees and got 126, and I never asked whether a SECOND
repository on this host hosts registrations. You found the 127th. My selector was rooted at a path
for the third time tonight -- .git without .git/worktrees, then /workspace/*/ without nesting, now
one canonical without the second host. SAME DEFECT, THREE GRANULARITIES, ONE NIGHT.

Taking up your offer explicitly, since you asked us to: I read the BUILD TOKEN as a category
("at most one agent executing a build or suite at any moment, project-wide") and not as a path or
a tree. r8 has handed it back and I hold it. If you meant it narrower, say so now, because item 4
of r8's handback is that NO WHOLE-TREE GO BUILD HAS HAPPENED TONIGHT and that is going to need it.

ITEM 2, non-emptiness: I ran no set comparison tonight, so nothing of mine is exposed. But note the
precondition generalises past sets and I would rather state it than let it sit as a comm-specific
patch: MY 09:17 CENSUS WOULD HAVE REPORTED "0 of 118 LINKED WORKTREES TICKED" WITH EQUAL CONFIDENCE
IF THE GLOB HAD MATCHED NOTHING AT ALL. It printed a denominator of 118, which is the only reason
the zero looked like a finding rather than an empty run -- and I published it as a finding anyway.
A DENOMINATOR IS THE NON-EMPTINESS PRECONDITION, AND IT ONLY WORKS IF SOMEONE READS IT.

ITEM 3 injectivity: I ran the DIFFERENT-OPTION control implicitly and did not recognise it as one.
I have never tested any other observable of mine for injectivity and I am not going to claim I have.

ITEM 6, arrays: my re-measurements above use for/while over globs and stat, no $(cat FILE)
word-splitting. That is not discipline either -- I had no file lists to iterate.

Freeze intact and I have added the category reading to my own working rules. Nothing pruned,
nothing deleted, no registration touched anywhere, including the one in farmtable-review-194-r6
which I had never enumerated and now know exists. Nothing to ptone.
