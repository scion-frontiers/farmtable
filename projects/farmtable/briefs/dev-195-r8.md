# Brief — dev-195-r8: the #195 round-8 fix leg

## Your working tree

**Your working tree is `/workspace`.** Confirm with `git rev-parse --show-toplevel` and
`git worktree list`, and verify BOTH the branch and the SHA before doing anything.
You should be at `7b4f6dd`. **Do NOT create any directory named in this brief** — if a
path here does not exist, the brief is wrong and you should tell me, not build it.
(Three legs last round were sent host paths that do not exist inside a container. One of
them noted that a leg which *created* the missing directory would have worked on the wrong
tree and reported false success. That is my error, not yours; this is the corrected form.)

Base for any range: `86f30bc`. **Assert `git merge-base --is-ancestor 86f30bc 7b4f6dd`
(exit 0) before using it as a range.** Do NOT use `89306d0` — it is a sibling, not an
ancestor, and diffing against it invents a 68-line deletion.

## Why you are one leg and not three

Every item below lives in `web/src/util/markdown.ts`, `web/src/util/markdown.test.ts`, or
`.design/project-log/markdown-sanitize-cleanup-r7.md`. Round 6 of a sibling issue was split
by package and the split itself produced the gap. You own all three files. Do not split.

## What round 7 actually was, so you calibrate correctly

**The entire production delta of r7 is comments.** `markdown.ts` is +81/-13 and the
executable body is byte-identical to `86f30bc`. Three independent legs measured the
mechanisms and found them sound: 103 of 105 mutations caught, every red-on-revert the log
claims reproduced by two separate legs, all five pinned totals independently re-derived
from a static read of the source without running the suite.

**So the round's product is claims, and the blocking findings are false claims.** You are
not repairing broken machinery. You are correcting statements about machinery that works,
plus adding one missing control and closing one guard hole. Do not rewrite what measured
sound. Gold-plating here delays a real fix on a sibling branch.

---

## BLOCKING — B1. The arity sentence is false in BOTH directions

**Two legs falsified the same sentence and neither found the other's case.** If you fix
only the half you happen to read first, you will ship a new false sentence in the commit
that fixes the old one. That is the exact defect this round exists to eliminate, committed
by the fix. Read this item twice.

The sentence, in `markdown.ts:140-144` (content anchor:
`every form that survives`), repeated in `markdown.test.ts:616-627` and in the r7 log at
`:41-47`:

> *"There is no measured arity SPELLING for which [`renderMarkdown.length === 1`] is the
> falsifier … every form that survives `tsc` leaves `.length` at 1 by definition."*

**[MEASURED-BY-review-195-r7]** `(...md: string[])` → `.length` is **0**.
`(md = '')` → **0**. Both are this round's own C7-j and C7-k, both survive `tsc`.

**[MEASURED-BY-test-195-r7]** `(md, opts?: T)` → `.length` is **2**. TypeScript erases `?`,
so it compiles to a plain two-parameter function. Isolated under a control triple: with the
declaration scan blinded and both arity tables emptied, the unchanged control stayed GREEN,
`opts: T = {}` stayed GREEN, and `opts?: T` went **RED** — so `.length === 1` is the *sole*
falsifier for that spelling.

The true rule: **`Function.length` stops counting at the first DEFAULTED-OR-REST parameter,
not at the first optional one.** Defaulted and rest spellings drive it below 1; an optional
parameter leaves it above 1. It is a falsifier for four measured spellings, not zero.

**Both legs independently conclude: KEEP the assertion.** The harm the false sentence does
is that it tells a maintainer the assertion has no unique coverage, inviting deletion.

Required:
1. Correct the sentence in all three places. State both directions explicitly.
2. **[MEASURED-BY-test-195-r7]** The failure message that fires on this assertion currently
   reads *"the artifact this suite imported has diverged from the source the scan above
   read (stale build, bundler transform, or a re-export…)"*. On the `opts?: T` spelling that
   sends the reader hunting a stale build for a parameter sitting in the declaration. Add
   the optional-parameter cause to that message's list.
3. Do NOT delete or weaken `renderMarkdown.length === 1`.

## BLOCKING — B2. `fixtureTableViolation` is the one guard nobody guards

**[MEASURED-BY-test-195-r7]** All 11 fixture tables are protected from silent shrinkage by
exactly one function, `fixtureTableViolation` (`markdown.test.ts:2305`), and it is the only
rule in the file with **no positive control**. Neutering it to always return `null` is
**GREEN 75/122**. Demonstrated end-to-end three times: neutered + empty `ARITY_EVASIONS`,
+ empty `OWNERSHIP_EVASIONS`, + empty `SINK_EVASIONS` — all three **GREEN, nothing fired.**

Every other predicate reddens when neutered, because something asserts it directly at a
wrong input. This one is never called with a wrong input.

This is a **coverage hole, not a mis-attributed assertion** — the table-size pins are
correctly worded and do fire on their own. What is missing is the level-out control. Note
the docblock above the function opens *"EVERY FIXTURE TABLE IN THIS FILE IS EMPTYABLE, AND
THAT IS THE SAME DEFECT THE TABLES WERE ADDED TO FIX"*. The fix it introduced now has the
shape it was written to diagnose, one level further out again.

Required: one `check()` asserting `fixtureTableViolation('X',[1,2],2) === null`,
`fixtureTableViolation('X',[1],2) !== null`, `fixtureTableViolation('X',[1,2,3],2) !== null`
— both directions. Mirror the treatment the count pins got in
`fixture: the tree-wide count pins fire on a changed count`. **Bump
`EXPECTED_CHECK_CALL_SITES` 74 → 75 in the same commit**, and see B4 before you touch that
line.

## BLOCKING — B3. `import(...)` is under-modelled at TWO layers. Fix both.

Two legs, two different layers, same production. Neither found the other's case, and one of
them explicitly (and correctly) reasoned that its finding did not reach the other layer —
which is true, and is exactly why it is not reassurance.

**B3a — the ownership guard. [MEASURED-BY-audit-195-r7]**
R8/R9 own the CONTIGUOUS QUOTED LITERAL only. Two lines in a scanned NON-sink component:

```ts
const __p = import('dompur' + 'ify');
void __p.then((m) => (m.default as { setConfig: (c: unknown) => void })
  .setConfig({ ADD_TAGS: ['script'], ADD_ATTR: ['onerror'] }));
```

All four gates GREEN; the capture appears verbatim in the shipped bundle; Rollup reports the
same chunk and instance; Node confirms
`(await import('dompur'+'ify')).default === (await import('dompurify')).default` is `true`.
Result: `renderMarkdown` returns `<p><img src="x" onerror="alert(1)"><script>alert(2)</script></p>`
against a baseline of `<p><img src="x"></p>`.

**Fix: give the module a private DOMPurify instance, `createDOMPurify(window)`, the way
`marked` was already made private.** A pattern-matching guard cannot own a global; only
ownership can. Reproduction artifacts are preserved under
`em-tooling/audit-195-r7-artifacts/` — `sb/src/components/ft-empty-state.ts` is the PoC.

**B3b — `stripImportStatements`. [MEASURED-BY-review-195-r7]**
The W2 fix added `(?!\s*\.)` so `import.meta` no longer starts a statement match, and the
docblock added alongside it says the function *"now names all three"* productions. **It
names two.** `import(<non-literal>)` still starts a statement match, `[^;'"]` has no quote
to stop against, and the match runs forward to the next `from '…'`, blanking everything
between — the identical swallow W2 closed for `import.meta`.

Measured, one-token attribution: a laundering block appended to the real non-sink
`src/util/format.ts` hiding `export const rawHtml = unsafeHTML` behind `const dev = import(spec)`
is **GREEN 75/122 with tsc exit 0**. The identical block with `import.meta.env.DEV` is
**RED**. The identical block in a `REQUIRED_SINKS` file is **RED via R6b**.

Measured fix: `(?!\s*\.)` → `(?!\s*[.(])` keeps the clean tree GREEN 75/122 and turns the
block RED. Add the block to `INDIRECTION_EVASIONS` and bump that table pin 16 → 17.

**B3c — my ruling on the open question, because it is mine to make.**
The review leg found that `R6b` already treats a non-literal dynamic specifier as a
violation, **but only in the two `REQUIRED_SINKS` files**, and asked whether that per-file
scope is deliberate or the same oversight `W3` fixed one rule over. It correctly declined to
answer and routed the call to me.

**Ruling: oversight. Promote R6b tree-wide, the way W3 promoted R7 this round.** The reason
is B3a, which the review leg did not have: `import()` is under-modelled at *both* layers by
two independent measurements, so treating the tree-wide gap as acceptable tripwire slack is
not supportable. Apply B3a **and** B3b **and** the promotion.

Correct the "names all three" sentence regardless of what else you do.

## BLOCKING — B4. `EXPECTED_CHECK_CALL_SITES` provenance line is wrong on both numbers

**[MEASURED-BY-review-195-r7]**, positive control included (the same grep returns 68 at the
base). `markdown.test.ts:3221` reads `// Moved 69 -> 73 in round 7:` and annotates a
constant of **74**. Measured: base 68, head 74. **Six checks added, not four.** The two
omitted are both in the new `dependencyPolicy()`: the T-6 dompurify floor pin and the O3
sunset clause.

Fix to `// Moved 68 -> 74 in round 7:` and add the two `dependencyPolicy()` checks to the
enumeration. B2 then takes it to 75 — make the line say what is true after your change, and
do not let B2 and B4 land contradicting each other.

**Out of your delta, do not chase:** the round-6 line above it (`Moved 61 -> 69`) is
inconsistent with the base constant of 68 by the same one, and r7 chained off it. Nobody has
traced round 5. Leave it; note it in your log.

## BLOCKING — B5. Two more false rationales

**B5a. [MEASURED-BY-audit-195-r7]** `markdown.ts:99-107` claims *"alert(1) does not [come
back]"* and *"nothing can reach the singleton today"*. Both false — B3a is the
counterexample. This matters more than an ordinary wrong comment: those two sentences are
the **stated justification at `:108-113` for deferring the fix.** The deferral rests on
nothing. The auditor reproduced the author's exact config FIRST, confirming it, and only
then varied it — so what is refuted is the generalisation, not the original measurement.
Correct the claim and remove or re-argue the deferral, since B3a is the fix it deferred.

**B5b. [MEASURED-BY-test-195-r7]** The `EXPECTED_ASSERTIONS` docblock
(`markdown.test.ts:3239-3245`) chooses EXACT over a floor because *"a floor is satisfied by
adding two assertions somewhere new and deleting two somewhere load-bearing."* Measured: the
**exact count is satisfied by precisely that manoeuvre too** (hollow the `slot` check, revert
`slot` from `FORBID_ATTR`, add two assertions to `headings render` → **GREEN 75/122**). An
exact total catches *net* change, not *compensated* change, and neither does a floor.

**KEEP EXACT** — it is still strictly stronger than a floor, it also catches unmasked
growth. Only the rationale is wrong. Replace the paragraph with the measured statement.

The test leg offered to let me downgrade B5b to a follow-up and I am declining, for a reason
it did not give: there are four false-claim findings in this round from three legs. Fixing
three and leaving one because it reads as prose is the inconsistency, and the fix is one
paragraph.

---

## NON-BLOCKING — take them if they are cheap, say so if you skip

- **[MEASURED-BY-review-195-r7]** The O3 sunset clause fires on bare `eslint`
  (`markdown.test.ts:1274-1276`), which its own docblock says it does not. Measured RED on
  `"eslint": "^9.0.0"` alone, with the message *"typescript-eslint tooling is now declared
  (eslint)"* — false, **and it breaks a security test on an ordinary lint-adoption commit.**
  The log's positive control used `prettier`, which does not cover this. Drop the bare
  `eslint` term or reword. *(Note: the test leg measured J4 `eslint` → RED and scored it as
  the clause working as designed. Both observed the same behaviour; they disagree on whether
  it is correct. I side with the review leg — a security test that reddens when someone adds
  a linter will be disabled by the third person who hits it.)*
- **[MEASURED-BY-test-195-r7]** Retitle `dompurify declares a floor at or above the advisory
  line` to say **equals** — the predicate is string equality, so raising the floor to
  `^3.5.0` is also RED. The code is stronger than the title in the safe direction, which is
  the confusing direction to be loose in.
- `splitTopLevelDefault` returns a boolean, so it is `hasTopLevelDefault`. Measured false
  positive: `md: (x: string) => string` returns `true` (the `>` of `=>` closes depth, then
  `=` reads as a top-level default). Fail-closed, no realistic spelling, but the docblock is
  wrong and no `ARITY_LEGITIMATE` fixture covers a function-typed sole parameter.
- `markdown.test.ts:3199` typo `undeirvably` → `underivably`.
- `markdown.test.ts:263` `assertContains(out, 'a', …)` is near-vacuous — the letter `a`
  cannot be absent. Use a distinctive token.
- The marked-singleton check relies on a **comment** to stay last in `taskLists()` because
  `marked.use` has no undo. Everywhere else this file refuses to accept a comment as a
  guarantee. Move it into its own function invoked last in `run()`.

## EXPLICITLY OUT OF SCOPE — do not do these

`#204` (the AST rule), the allow-list inversion, `F-4` (the build config is unscanned and
`vite.config.ts` can inject a script into `dist/index.html` with zero changes to any scanned
file — **and adding it to `EXTRA_SCANNED_FILES` would NOT catch it**, so do not "fix" it that
way), `F-7` (`marked` has no dependency floor), CI, and CSP/Trusted Types. All are tracked.

---

## Standing bars — these are not boilerplate, each has an incident behind it

- **Positive control before any negative claim.** A harness that has never reported RED
  cannot support a GREEN.
- **A harness must ABORT on a failed prerequisite**, never continue and print green. The
  test leg's C3 abort caught an escaping bug that would otherwise have produced a false
  *negative finding* ("the U+FE0E variation selector is unpinned").
- **Exit codes from the child process, never through a pipe.**
- **Mutations content-addressed, never line-addressed. Abort if the anchor is not unique.**
  Line numbers in this brief may have drifted — the `:361` anchor in a sibling brief was
  stale by eight lines. Anchor on content and tell me if a quoted line number is wrong.
- **Verify a green mutation actually weakened the thing.** That bar is what stopped two
  equivalent mutants from being written up as coverage holes last round.
- **Predict counts BEFORE measuring**, from a static read of the source, not the runner's
  output. Your predecessors reproduced all five pinned totals this way; match that standard.
- **`npm ci`, not `npm install`** if a lockfile is present — one leg had to disclose that it
  could not verify the installed DOMPurify because it ran `install`.
- **Do not push.** Commit locally. Pushing is mine alone.
- **Costly disclosure is the trust signal.** Name what you could not verify, and name your
  own void runs. The three legs you are following disclosed their own wrong predictions and
  their own broken harnesses, and it raised my confidence in everything else they said.

## Required deliverables — you MUST produce all four

1. All blocking items B1–B5 fixed, committed locally to a branch off `7b4f6dd`.
2. `npm test`, `npx tsc --noEmit`, `npm run build` — exit codes read from the child.
   Report the check/assertion totals; they will have moved and the pins must agree.
3. A report at
   `/scion-volumes/scratchpad/projects/farmtable/reports/dev-195-r8.md` with: what you
   fixed, a mutation table with predicted-vs-actual, **which of my [MEASURED-BY-…] claims
   you re-verified yourself and which you took on trust**, what you could not verify, your
   void runs, and a section **WHERE THIS BRIEF IS WRONG** — if nothing, say so explicitly.
   Every one of the last four legs found a real error in my brief. Assume there is one.
4. A project-log entry committed in `.design/project-log/`.

**You MUST write the report file, commit the project-log entry, and then mark the task
complete.** Do not stop after the analysis.
