# dev-194-r9 — #194 round 9 fix leg

Your working tree is `/workspace`. Confirm with `git rev-parse --show-toplevel`, and verify
branch `label-write-scope-r9` and commit **`158c8ae963faa5eef032e0857ecbc40d6a7c681a`**.
**Do NOT create any directory named in this brief.**

`web/dist` is present and is gitignored — leave it. It is not optional: `go vet` dies on
`assets.go:5:12` without it `[MEASURED-BY-review-194-r8]`, and I put it there for you. Do not
delete or rebuild it.

## How to treat this brief

Tags: `[MEASURED]` = I ran it myself this session. `[MEASURED-BY-<leg>]` = someone else's
measurement, relayed, **re-measure before relying on it**. `[BELIEVED]` = neither.

**My briefs have contained at least one error in eleven consecutive rounds.** Round 8's had
four; this round the three review legs found eight between them. Two of those errors were
things I asserted confidently and one was a question that *could not be answered in Go*.
**Every round, the legs that ignored my hints found something better than the hint.**

Reporting every place this brief is wrong is a **required deliverable**, not a courtesy.

## Baseline `[MEASURED by me at 158c8ae in this exact clone]`

| check | result |
|---|---|
| `go build ./...` | exit 0 |
| `go vet ./...` | exit 1, **exactly 4** copylocks, `internal/server/server.go:1782/1892/2100/2277`, all `assignment copies lock value to ephReq: …contains sync.Mutex` |
| `git status --porcelain` | empty |

Those 4 vet findings are **pre-existing and expected**. Check the *messages and line numbers*,
not the count — same count with different messages is a new bug.

`go test ./...` was exit 0 with zero FAIL lines for all three review legs
`[MEASURED-BY-review-194-r8, test-194-r8]`.

### The flake, with its rate — this matters for your mutation work

`TestWatchTasks*` in `internal/server` fails at roughly **8% per sequential full-suite run**
`[MEASURED-BY-test-194-r8: 2 failures in 26 runs]`. A previous report claimed "0 failures in 12
sequential runs"; **that claim is wrong** and I relayed it last round, which is one of my errors.

Consequence you must design around: **at 8%, a mutation row scored from a single run has about
a 1-in-12 chance of a spurious RED.** So:

- **Score every mutant by the NAMES of the failing tests, not the count.**
- **Reject any failure outside the mutated code's blast radius** — a mutant in
  `internal/platform/github` cannot break `internal/server` streaming tests.
- **Re-run any RED row once** before you put it in a table.

There is **no CI on this project**, so nothing downstream will catch what the suite misses.

---

# What round 8 got right — do not redo this

Two legs verified the central claim independently and it holds. `RestrictLabelWriteToSnapshot`
deriving from `applyLabelDelta` is the right fix. review-194-r8 proved round-trip agreement
closed-form **and** over 524,288 triples with 0 failures, plus a positive control that goes RED;
test-194-r8 reproduced the round's mutation matrix to the digit. The six-site `stageWritePolicy`
partition is complete, compile-enforced, and every site is individually pinned (RED 80/1/3/65/64/9)
`[MEASURED-BY-test-194-r8]`. **audit-194-r8 returned APPROVE and could not break the fix after
five attacks.**

**You are not being asked to redesign anything.** Every item below is an extract-function, a
test, a property, or a comment.

---

# MUST 1 — give C-1 a backstop. This is the most important item in the round.

**Severity HIGH.** `internal/platform/github/restrict_label_write_property_test.go`.

C-1 — this round's Critical, an authorization bypass — **is pinned in exactly one file, and
nothing else in the repository notices if it regresses** `[MEASURED-BY-test-194-r8]`:

| experiment | result |
|---|---|
| neuter both oracles in `restrictProperties` (`:84`, `:132`) **+ restore the round-7 implementation** | **`go test ./...` exit 0, ZERO failing tests — the Critical ships green** |
| the same neutering **+ the pre-A-4 identity restrictor** | **RED**, `internal/server/authz_label_write_scope_test.go:2357` and `:2419` |

That contrast is the whole finding, and note *why it is good evidence*: it is a positive control
drawn from a **different axis** than the thing being searched. The older A-4 defect survives
destruction of the property file because it has two independent **server-layer** pins. C-1 has
none.

**Do:** add a server-layer C-1 pin beside the two A-4 ones. The shape suggested, which you should
feel free to improve: a `task:write`-only caller issuing
`add_labels=[ft:stage/completed], remove_labels=[ft:stage/completed]` against a snapshot that
lacks the label must not end up with the terminal label on the issue.

**Verify it is real:** restore the round-7 implementation and confirm your new test goes RED
*from `internal/server`* — not merely that the suite is RED, which the property file already
guarantees. **Name the failing tests.**

# MUST 2 — the probe guarding P2 is a copy of P2

**Severity HIGH. Found independently by two legs** (review-194-r8 R1, test-194-r8 F-2), which is
why it is second and not fourth.

`restrict_label_write_property_test.go:396-455`,
`TestRestrictLabelWriteToSnapshot_PropertiesRejectTheIdentityRestrictor`.

Its docblock promises: *"If this test ever fails, P2 has stopped discriminating and the A-4 class
is unpinned again."* **The test never calls `RestrictLabelWriteToSnapshot` and never calls
`restrictProperties`.** Line 411 does `gotAdd, gotRemove := row.add, row.remove` and lines 413-435
hand-reimplement P2's predicate, then check the copy against itself.

Measured `[MEASURED-BY-test-194-r8]`: disable P2 in `restrictProperties` → **suite GREEN, probe
silent**. Disable P2 *and* revert production to the identity restrictor → **probe still passes**.
So the promised contrapositive is false in both directions.

Independently `[MEASURED-BY-review-194-r8]`: three mutants of the P2 *definition* — delete the
C-1 arm, delete the A-4 arm, gut P2 entirely — **all three GREEN, `go test ./...` exit 0**. P2 can
be deleted outright and the repo stays green.

This violates the rule the file itself argues for, 300 lines earlier: *"any hand-rolled model of
`applyLabelDelta` here would be free to drift in the same direction the production code drifted
and would report agreement anyway."* **When a control's contract is "mirrors function F", the
oracle must BE F, never a reimplementation of F.**

**Do:** extract P2's body into a shared helper and call it from both places —
`func p2Violations(snapshot, addLabels, removeLabels, gotAdd, gotRemove []string) []string`.
`restrictProperties` calls it with the production restrictor's output; the probe calls it with
`gotAdd, gotRemove := row.add, row.remove`.

**Acceptance:** all three P2-definition mutants above must go **RED** after your change. Measure
each one, one at a time, and report the failing test names.

# MUST 3 — the snapshot-spelling fix (F-2 closure) has zero coverage

**Severity MEDIUM. Two legs, independently** (review R2, test F-5). `passthrough.go:1199-1225`.

Round 8's report leads with "removals are now emitted in the snapshot's spelling, not the
caller's — this closes F-2 as a side effect". Revert it to emit the caller's spelling and the
**suite is GREEN, 0 failing tests** `[MEASURED-BY both legs, independently]`.

**Why no amount of added inputs would have caught it** — this is the instructive part. Both P1 and
P2 compare through `labelMatchKey`, which is `strings.ToLower(strings.TrimSpace(raw))`. Caller
spelling and snapshot spelling differ **only** in case and padding — exactly the two things that
oracle normalises away. The failure is outside the oracle's range *by construction*. The 8192-triple
sweep was answering a different question. **Ask what your oracle can discriminate before asking
what your inputs vary.**

**The harm is real, not theoretical.** `labelNameToID` (`passthrough.go:198-203`) looks up
`s.labelIndex[strings.ToLower(name)]` and the index is built with `ToLower` and **no `TrimSpace`**.
A padded caller spelling resolves to nothing, `labelNamesToIDs` silently drops it, `writeLabelSwap`
sees an empty ID list and returns `nil` — so a **priced** removal does not land while `UpdateTask`
reports success.

**Do both:**
1. Add **P3** to `restrictProperties`: every entry of `gotRemove` must be **verbatim** an element
   of `t.Labels`. `[MEASURED-BY-review-194-r8]` this holds today across all 524,288 triples
   including padded and upper-cased snapshots, so it is safe to assert.
2. Add a named row whose caller spelling is padded relative to the snapshot's, e.g.
   `snapshot: ["ft:stage/wont_fix"], remove: ["  FT:Stage/Wont_Fix "]`.

**ORDERING HAZARD, read this before you start:** test-194-r8's killing probe for this **omitted
`ensureLabelIndex` on its first attempt and went RED against the correct tree** — they caught it
only because the shipped code failed too, and they wrote it down. If you write a probe that
resolves label IDs, call `ensureLabelIndex` first. **A probe that fails on HEAD is measuring the
probe.**

**MUST 2 and MUST 3 both edit `restrictProperties`.** Do MUST 2 first (it changes the function's
shape), then MUST 3.

# MUST 4 — a comment documents a hazard that cannot occur

**Severity LOW as code, but it is this workstream's declared defect class, so it blocks.**
Two legs (review R3, test F-6). `passthrough.go:1206-1220`.

The `removeKeys` "safety belt" comment claims *"two entries sharing a match key would make
`applyLabelDelta`'s dedup drop one, which without this test would emit a removal the caller never
asked for."* **That situation cannot occur.** `applyLabelDelta` keeps the **first** occurrence of
each key, so a duplicated snapshot key is always in `afterKeys`; therefore
`keys(S) \ keys(after) ⊆ keys(R)` unconditionally and `!removeKeys[key]` is never true there.

Proved analytically by both legs, plus: 524,288 triples with three spellings of one key in the
vocabulary, **0 diffs** `[MEASURED-BY-review-194-r8]`; an independent 32,768-triple duplicate-key
sweep, both shipped and mutant **GREEN** `[MEASURED-BY-test-194-r8]`; deleting the clause leaves
the suite **GREEN**.

**I relayed that false rationale to the review leg last round as an established fact. That is my
error, not the developer's** — but the rationale is still false and must not ship.

**Also false and must be withdrawn:** round 8's report claims the belt is *"covered by a named
row"*. No named row and no sweep vocabulary contains a snapshot with two entries sharing a match
key `[MEASURED-BY both legs]`.

**Do:** the clause is not worthless — it is a legitimate hedge against a *future* `applyLabelDelta`
that drops a label for some reason other than the remove list. Either keep it and **replace the
paragraph with the invariant proof plus that honest rationale**, or **delete the clause and the
`removeKeys` map** and state the invariant. Either is acceptable; leaving the current text is not.

If you keep it, note it also supplies the qualification the docblock at `:1147` currently lacks —
*"NO FUTURE CHANGE TO applyLabelDelta CAN DESYNCHRONISE THEM"* is slightly too strong, and the belt
exists precisely because it is.

---

# DO NOT START: the `checkLifecycleKeyCollisions` item is deferred, and here is why

All three legs independently landed on this function and **their prescribed fixes contradict each
other.** I am waiting on a ruling. **Do not touch `checkLifecycleKeyCollisions` this round**, and
do not add test-194-r8's `TestLifecycleKeyCollision_DisabledMappingOwnsNoStageLabel` as written.

The conflict, so you understand the deferral rather than just complying:

- review R4 and audit M-1 both say: seed `owned` from `m.labelToStage` (a ~4-line merge).
- test F-3 proposes a test asserting `Validate()` returns **nil** at `enabled: false` with
  `types: {duplicate: chore}`.
- **`[MEASURED by me]`** `labels.go:134-136` seeds `m.labelToStage[strings.ToLower(s.String())]`
  for all ten stages — the **bare** names — **regardless of `enabled`**; `labels.go:677-678` says
  so in its own words. So the R4/M-1 merge makes `Validate()` **reject** precisely the input F-3
  asserts it must **accept**.

Underneath it is an undecided semantic: `TerminalLabelStage` **does** guard `!m.enabled`
(`labels.go:682-685`) and its comment states the principle — *"scanning anyway would make a
disabled mapper start honouring labels it is configured to ignore"* — while `authorizationStage`
(`terminal_label_stages.go:46-52`) and `assertStageWriteAllowed` **do not** `[MEASURED by me: both
bodies read; grep for "enabled" in passthrough.go returns nothing]`. That errs *restrictive*, so
it is not a vulnerability — but it means F-3's premise is false and the two fixes cannot both land.

If the ruling arrives while you are working, I will send it. **If you find a reason any of MUST 1-4
depends on this, stop and tell me rather than deciding it yourself.**

---

# SHOULD — do these only if MUST 1-4 land cleanly

- **S1** `stage_write_policy_test.go:33` points at `TestValidate_RejectsATypesKeyThatNormalisesOntoAStage`,
  which does not exist. Real name: `TestValidate_RejectsAPrioritiesOrTypesKeyThatCapturesALifecycleLabel`
  (`lifecycle_key_collision_test.go:34`). One-line fix.
- **S2** `cmd/farmtable-server/main.go:89` — `Describe` has seven mutants pinning the string it
  builds and **zero** pinning that it is ever logged; deleting the `log.Println` line survives
  (review O2, test F-7, audit L-1 area). Extract the two lines into a helper a test can drive.
- **S3** `stageWritePolicy` is a bare named `bool`, so `writeLabelSwap(…, true)` compiles and
  bypasses the contract (review N1, audit L-2). A one-field struct or an unexported enum type makes
  the named constant the only spelling. **The zero value is `stageWriteForbidden` and that is
  correct and deliberate — preserve that property and say how you preserved it.**
- **S4** `allStages` (`labels.go:66`) is hand-maintained and feeds *both* defence layers, so a
  stage added to the ent enum but missing there is invisible to both — a silent fail-open (review
  O1). One test asserting every value `task.StageValidator` accepts appears in `allStages`.
  Related: `StageToLabel`'s fallback (`labels.go:325`) is **dead code** — replacing it with a panic
  leaves the suite exit 0 `[MEASURED-BY-review-194-r8]`.

---

# Corrections to round 8's own report — fix these where they appear in the log

- **"8 commits off `1d4442f`"** — there are **9** `[MEASURED-BY both legs]`.
- **The vet table** lists `GetBottlenecks` base as `~2277` while concluding "uniform +45". The base
  at `1d4442f` is **2232**; the conclusion is right and the row is wrong `[MEASURED-BY-review-194-r8]`.
- **M-P1 is 4 failing tests, not 3** — `TestLifecycleKeyCollision_IsTheHarmTheCheckClaims` postdates
  that row `[MEASURED-BY both legs]`.
- **M-C1b / M-C1c are not mutants of `RestrictLabelWriteToSnapshot`** — that function has neither a
  cross-list equality test nor a raw-string comparison. They are mutants of unspecified alternative
  implementations, so **half the headline matrix cannot be re-derived by a reader**
  `[MEASURED-BY-test-194-r8]`. Any mutation row you table must carry its exact diff or a unique
  anchor string.
- **The `req.Type` `UpdateTask` call site (`server.go:739`) is absent from the matrix.** It *is*
  pinned (RED 4) `[MEASURED-BY-test-194-r8]`, so there is no hole — but the report's own narrative
  is about a validator wired into the wrong two of three sites, and an untabled third site is that
  same shape.

---

# Verification bars — restated, not boilerplate

- **Exit codes come from the child process, never through a pipe.** review-194-r8's first build
  measurement was wrong for exactly this reason (`| tail`) and they caught it on re-run.
- **A mutant that does not compile measured nothing.** Record `go build` exit 0 per mutant.
- **Apply mutants one at a time**, assert the anchor matches **exactly once**, revert, and assert
  `git diff --quiet` after each.
- **Predict counts BEFORE measuring** and report the prediction next to the result. Recorded misses
  are valuable — both review legs recorded theirs.
- **A green control is a finding, not a pass.** Write it down.
- **A negative claim needs a positive control from a DIFFERENT axis than the one you searched.**
  This round produced a textbook counter-example: a 360-cell sweep "proving" a mutant unkillable,
  which held `enabled` constant at true the whole time and was killed by varying it. **Scale on the
  wrong axis is not evidence.** When you claim something cannot be tested, **name the axes you held
  constant** — that list is the finding.

# Deliverables — you are not done until all five exist

1. **Commits on `label-write-scope-r9`.** Clear messages. **Never push.**
2. **A report at `/scion-volumes/scratchpad/projects/farmtable/reports/dev-194-r9.md`** — per MUST
   item: what you changed, the mutant you used to prove it now fails, the **names** of the failing
   tests, and your prediction alongside the result.
3. **A project log entry** in `.design/project-log/`, **committed**.
4. **An explicit list of every place this brief was wrong.** If nothing, say so and say what you
   checked.
5. **Final gates**, reported with child-process exit codes: `go build ./...` = 0,
   `go test ./...` = 0 with zero FAIL lines, `go vet ./...` = 1 with the **same 4** copylocks
   messages and line numbers as the baseline, `git status --porcelain` empty.

**You MUST produce all five deliverables and then mark the task complete.**
