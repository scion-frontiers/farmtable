[
 {
  "id": "N1 add a new src/*.ts file carrying an innerHTML sink",
  "expect": "RED",
  "creates": [
   {
    "file": "src/legacy-widget.ts",
    "content": "export function w(el: HTMLElement, s: string): void { el.innerHTML = s; }\n"
   }
  ]
 },
 {
  "id": "N2 same, with EXPECTED_SOURCE_FILES bumped (isolates the sink tripwire)",
  "expect": "RED",
  "creates": [
   {
    "file": "src/legacy-widget.ts",
    "content": "export function w(el: HTMLElement, s: string): void { el.innerHTML = s; }\n"
   }
  ],
  "edits": [
   {
    "file": "src/util/markdown.test.ts",
    "anchor": "const EXPECTED_SOURCE_FILES = 51;",
    "repl": "const EXPECTED_SOURCE_FILES = 52;"
   }
  ]
 },
 {
  "id": "N3 NEGATIVE CONTROL: add a benign src file + bump the count",
  "expect": "GREEN",
  "creates": [
   {
    "file": "src/benign-widget.ts",
    "content": "export function w(s: string): string { return s.toUpperCase(); }\n"
   }
  ],
  "edits": [
   {
    "file": "src/util/markdown.test.ts",
    "anchor": "const EXPECTED_SOURCE_FILES = 51;",
    "repl": "const EXPECTED_SOURCE_FILES = 52;"
   }
  ]
 },
 {
  "id": "N4 add src/legacy-widget.html with an inline innerHTML sink (.html off INERT_EXTENSIONS)",
  "expect": "RED",
  "creates": [
   {
    "file": "src/legacy-widget.html",
    "content": "<div><script>document.body.innerHTML = location.hash;</script></div>\n"
   }
  ]
 },
 {
  "id": "N5 delete a scanned source file",
  "expect": "RED",
  "deletes": [
   "src/capabilities.ts"
  ]
 },
 {
  "id": "N6 R6: park an aliased re-export in a *.test.ts file and import it at the sink",
  "expect": "RED",
  "creates": [
   {
    "file": "src/components/inspector/helper.test.ts",
    "content": "export { unsafeHTML as rawHtml } from 'lit/directives/unsafe-html.js';\n"
   }
  ],
  "edits": [
   {
    "file": "src/components/inspector/ft-inspector-desc.ts",
    "anchor": "import { unsafeHTML } from 'lit/directives/unsafe-html.js';\nimport { renderMarkdown } from '../../util/markdown.js';",
    "repl": "import { unsafeHTML } from 'lit/directives/unsafe-html.js';\nimport { renderMarkdown } from '../../util/markdown.js';\nimport { rawHtml } from './helper.test.js';"
   },
   {
    "file": "src/components/inspector/ft-inspector-desc.ts",
    "anchor": "${unsafeHTML(renderMarkdown(this.description))}",
    "repl": "${rawHtml(this.description)}"
   }
  ]
 }
]