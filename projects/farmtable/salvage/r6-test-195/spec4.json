[
 {"label":"A21+C7b decl-scan ablated + (md, ...rest)","edits":[
   {"file":"web/src/util/markdown.test.ts","find":"    const src = readFileSync(join(findWebRoot(), 'src', 'util', 'markdown.ts'), 'utf8');\n    const decl = /export function renderMarkdown\\s*\\(([^)]*)\\)/.exec(src);\n    if (decl === null) {\n      throw new Error('could not find the renderMarkdown declaration in src/util/markdown.ts');\n    }\n    if (/[,=]/.test(decl[1])) {\n      throw new Error(\n        `renderMarkdown declares more than one parameter: (${decl[1].trim()}) — a defaulted ` +\n          'second parameter keeps Function.length at 1, so the declaration is checked too',\n      );\n    }\n","replace":""},
   {"file":"web/src/util/markdown.ts","find":"export function renderMarkdown(md: string): string {\n  if (typeof md !== 'string') return '';","replace":"export function renderMarkdown(md: string, ...rest: unknown[]): string {\n  void rest;\n  if (typeof md !== 'string') return '';"}
 ]},
 {"label":"A21+C7c decl-scan ablated + (md, {inline} = {})","edits":[
   {"file":"web/src/util/markdown.test.ts","find":"    const src = readFileSync(join(findWebRoot(), 'src', 'util', 'markdown.ts'), 'utf8');\n    const decl = /export function renderMarkdown\\s*\\(([^)]*)\\)/.exec(src);\n    if (decl === null) {\n      throw new Error('could not find the renderMarkdown declaration in src/util/markdown.ts');\n    }\n    if (/[,=]/.test(decl[1])) {\n      throw new Error(\n        `renderMarkdown declares more than one parameter: (${decl[1].trim()}) — a defaulted ` +\n          'second parameter keeps Function.length at 1, so the declaration is checked too',\n      );\n    }\n","replace":""},
   {"file":"web/src/util/markdown.ts","find":"export function renderMarkdown(md: string): string {\n  if (typeof md !== 'string') return '';","replace":"export function renderMarkdown(md: string, { inline }: { inline?: boolean } = {}): string {\n  void inline;\n  if (typeof md !== 'string') return '';"}
 ]},
 {"label":"A21+C7d decl-scan ablated + REQUIRED second parameter","edits":[
   {"file":"web/src/util/markdown.test.ts","find":"    const src = readFileSync(join(findWebRoot(), 'src', 'util', 'markdown.ts'), 'utf8');\n    const decl = /export function renderMarkdown\\s*\\(([^)]*)\\)/.exec(src);\n    if (decl === null) {\n      throw new Error('could not find the renderMarkdown declaration in src/util/markdown.ts');\n    }\n    if (/[,=]/.test(decl[1])) {\n      throw new Error(\n        `renderMarkdown declares more than one parameter: (${decl[1].trim()}) — a defaulted ` +\n          'second parameter keeps Function.length at 1, so the declaration is checked too',\n      );\n    }\n","replace":""},
   {"file":"web/src/util/markdown.ts","find":"export function renderMarkdown(md: string): string {\n  if (typeof md !== 'string') return '';","replace":"export function renderMarkdown(md: string, opts: Record<string, unknown>): string {\n  void opts;\n  if (typeof md !== 'string') return '';"}
 ]},
 {"label":"A21+A22 BOTH halves of the arity pin ablated + (md, opts = {}) [control: must be GREEN]","edits":[
   {"file":"web/src/util/markdown.test.ts","find":"    assertEqual(\n      String(renderMarkdown.length),\n      '1',\n      'renderMarkdown no longer takes exactly one argument — a second parameter is a ' +\n        'configuration channel into the sanitizer that no rule in the sink guard can see',\n    );\n","replace":""},
   {"file":"web/src/util/markdown.test.ts","find":"    const src = readFileSync(join(findWebRoot(), 'src', 'util', 'markdown.ts'), 'utf8');\n    const decl = /export function renderMarkdown\\s*\\(([^)]*)\\)/.exec(src);\n    if (decl === null) {\n      throw new Error('could not find the renderMarkdown declaration in src/util/markdown.ts');\n    }\n    if (/[,=]/.test(decl[1])) {\n      throw new Error(\n        `renderMarkdown declares more than one parameter: (${decl[1].trim()}) — a defaulted ` +\n          'second parameter keeps Function.length at 1, so the declaration is checked too',\n      );\n    }\n","replace":""},
   {"file":"web/src/util/markdown.ts","find":"export function renderMarkdown(md: string): string {\n  if (typeof md !== 'string') return '';","replace":"export function renderMarkdown(md: string, opts: Record<string, unknown> = {}): string {\n  void opts;\n  if (typeof md !== 'string') return '';"}
 ]},

 {"label":"CFG-1 ADD_ATTR onerror","edits":[{"file":"web/src/util/markdown.ts","find":"    FORBID_TAGS,\n    FORBID_ATTR,\n  });","replace":"    FORBID_TAGS,\n    FORBID_ATTR,\n    ADD_ATTR: ['onerror'],\n  });"}]},
 {"label":"CFG-2 ALLOWED_URI_REGEXP widened to /^.+$/","edits":[{"file":"web/src/util/markdown.ts","find":"    FORBID_TAGS,\n    FORBID_ATTR,\n  });","replace":"    FORBID_TAGS,\n    FORBID_ATTR,\n    ALLOWED_URI_REGEXP: /^.+$/,\n  });"}]},
 {"label":"CFG-3 SANITIZE_DOM false","edits":[{"file":"web/src/util/markdown.ts","find":"    FORBID_TAGS,\n    FORBID_ATTR,\n  });","replace":"    FORBID_TAGS,\n    FORBID_ATTR,\n    SANITIZE_DOM: false,\n  });"}]},
 {"label":"CFG-4 ADD_TAGS iframe","edits":[{"file":"web/src/util/markdown.ts","find":"    FORBID_TAGS,\n    FORBID_ATTR,\n  });","replace":"    FORBID_TAGS,\n    FORBID_ATTR,\n    ADD_TAGS: ['iframe'],\n  });"}]},
 {"label":"CFG-5 config object dropped entirely (bare sanitize)","edits":[{"file":"web/src/util/markdown.ts","find":"  return DOMPurify.sanitize(parser.parse(md) as string, {\n    FORBID_TAGS,\n    FORBID_ATTR,\n  });","replace":"  return DOMPurify.sanitize(parser.parse(md) as string);"}]},
 {"label":"CFG-6 renderMarkdown returns unsanitized parser output","edits":[{"file":"web/src/util/markdown.ts","find":"  return DOMPurify.sanitize(parser.parse(md) as string, {\n    FORBID_TAGS,\n    FORBID_ATTR,\n  });","replace":"  return parser.parse(md) as string;"}]},

 {"label":"S1 second RAW unsafeHTML call added to desc.ts (two sinks, one file)","edits":[{"file":"web/src/components/inspector/ft-inspector-desc.ts","find":"${unsafeHTML(renderMarkdown(this.description))}","replace":"${unsafeHTML(renderMarkdown(this.description))}${unsafeHTML(this.description)}"}]},
 {"label":"S2 sink swapped for a nested arrow IIFE, still sanitized [false-positive control]","tsc":true,"edits":[{"file":"web/src/components/inspector/ft-inspector-desc.ts","find":"${unsafeHTML(renderMarkdown(this.description))}","replace":"${(() => unsafeHTML(renderMarkdown(this.description)))()}"}]},
 {"label":"S3 sanitizer output routed through a local variable first","tsc":true,"edits":[{"file":"web/src/components/inspector/ft-inspector-desc.ts","find":"${unsafeHTML(renderMarkdown(this.description))}","replace":"${unsafeHTML(renderMarkdown(String(this.description)))}"}]}
]
