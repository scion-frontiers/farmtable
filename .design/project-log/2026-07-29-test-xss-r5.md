# 2026-07-29 — Test review leg, XSS R5 three-way independent review

**SHA under review:** `d305391ee6dc473f5e7bf202167221e15cf52e10` (branch `url-scheme-validation-r5`,
base `e6bda71`, 13 commits).
**Working tree:** `/workspace/farmtable-xss-r5-test`, fresh `git clone --no-hardlinks` of
`/workspace/farmtable-dev-xss-r5`. ROOT recorded, **DIST=ABSENT** (no inherited `web/dist`).
**Full report:** `reports/xss-r5-test.md` on the scratchpad volume — untracked by design, not committed.

## Verdict

**REQUEST CHANGES.** No unsanitized path and no live XSS found. Blocking on four places where the
round asserts a property in prose that no instrument enforces.

## Angle

Not coverage percentages. The question was: **can the tests in this round fail, and do they fail for
the reason they claim?** For every property asserted, name the test that goes red.

## Method

Eight mutants, each with a **prediction pre-registered in `reports/_run-queue-log.md` before the run**.
All single-package `go test ./internal/<pkg>/ -run '^(...)$' -count=1` — **no build token was ever
needed**. Baseline `md5sum` taken before the first mutation; every mutant restored via
`git checkout --` and the restore verified by `md5sum -c`. Final state: working tree clean, all seven
checksums OK, `url-scheme-validation-r5` still at `d305391`.

**8 predictions, 8 correct. 4 survived, 4 died.**

## What the mutants showed

**Died (coverage is real):**
- Gutting `sanitizeRemoteData` → 4 tests red.
- Removing the `map[string]any` recursion arm — the round's headline feature → 4 tests red.
- `taskToCreateParams` assigning `RemoteData` → `TestEphemeralGraphRouteDropsRemoteData` red with
  the exact message it promises. **This is the best test in the diff:** real code, two controls that
  could genuinely have failed, pins the value rather than the spelling of an absent line.
- A *behaviour-preserving* extraction out of `taskToProto` → registry membership red, naming
  `taskToProto` and OUTBOUND. The per-direction membership set works as advertised.

**Survived (the blocking findings):**
- **B-1** Deleting `"labels": issueLabels(issue)` from `issueBuildRemoteData` — green in
  `internal/server` *and* in `internal/platform/github`. The carrier pins assert over hand-written
  literals and **never call the builder**; no test in the repository does.
- **B-2** Deleting the `log.Printf` from `structOrNilLoggingErr` — green. Commit `6ee08bf`'s whole
  deliverable ("it is audible") has no instrument, even though one test already prints the line.
- **B-3** Deleting a `*`-starred row from the shape table (6 → 5 against a floor of 5), and then the
  count-neutral version: replacing the `index target` row with a duplicate of another starred row.
  Both green. **The file spends forty lines memorialising a floor of 4 against a population of 6
  — "we were missed by a unit" — and then adds five new floors with 1–2 units of slack.**

## B-4 — a new false citation, shipped beside the one that was repaired

`collectionToProto`'s new exemption says *"Every writer of `Collection.RemoteData` ... (entstore.go:408,
:898, :2117)"*. Resolved at this SHA: `:408` is `CreateTask`, `:898` is `doUpdateTask` — both **task**
functions — and the two dedicated collection writers, `CreateCollection` (`:1366`) and
`UpdateCollection` (`:1399`), are **omitted**. `entstore.go` is not in this diff, so this is
misattribution, not line rot.

The **conclusion holds** — no caller sets `RemoteData` on those param structs, so the log genuinely
cannot fire — but the evidence for it is false, and it is the sole reason there is no test on the
collection half.

## Negative result worth keeping

I hypothesised the recursion might be as unpinned as the carriers, **pre-registered the falsifier,
and it fired**: removing the recursion arm killed four tests. The author did the hard half correctly.
Recording this because a report that only lists what is broken would have implied otherwise, and the
asymmetry — solid centre, unpinned periphery — is the actual shape of this round.

## Process finding

**The dispatch said "READ THE BRIEF FIRST AND IN FULL"; the brief said "DO NOT READ SECTION 4 UNTIL
PASS 1 IS DONE."** Both cannot be obeyed. I obeyed the dispatch and so read the six disclosed
weaknesses before looking at the diff, which contaminated exactly the PASS-1/PASS-2 attribution the
tags exist to measure. Tagged conservatively and disclosed in the report rather than papered over.
Suggested fix: dispatches should say *"read sections 0–3 and stop."*
