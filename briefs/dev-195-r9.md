# dev-195-r9 — #195 blocking fix round 9

Your clone is at `/workspace`, branch `markdown-sanitize-r8`, HEAD `3f6a695`, clean, with
`web/node_modules` already installed. Work on a new branch `markdown-sanitize-r9` off `3f6a695`.

Baseline at HEAD `[MEASURED-BY-test-195-r8]`: `npm test` green at **78 files / 123 tests**,
`tsc` exit 0, build exit 0. Confirm this before you change anything; if your baseline differs,
stop and report rather than proceeding.

## Claim tagging, and my track record

Claims are tagged. **`[MEASURED-BY-<leg>]` is somebody else's measurement relayed by me.**
Re-measure before relying on anything.

**Last round my brief contained four errors and one of my hints actively misdirected two
legs.** I told them to look at a `)` inside a *string* literal; the live vector is a
*template* literal, which the same function deliberately preserves. Both legs found the bug
by ignoring me. **Treat this brief as a starting point, not an authority, and report every
place it is wrong — that is a required deliverable.**

## Context

Round 8 was reviewed by three independent legs; all three reported REQUEST CHANGES or worse.
Read the reports rather than relying on my summary:
- `/scion-volumes/scratchpad/projects/farmtable/reports/review-195-r8.md`
- `/scion-volumes/scratchpad/projects/farmtable/reports/audit-195-r8.md`
- `/scion-volumes/scratchpad/projects/farmtable/reports/test-195-r8.md`

---

# MUST — two blockers

## 1. The arity pin is bypassed again — THIRD consecutive round. Take the CLASS fix.

Severity: **HIGH and merge-blocking** (my ruling; review said Critical, audit said Medium,
test said Blocker — reasoning is in the adjudication, you do not need to relitigate it).

`[MEASURED-BY-review-195-r8 and audit-195-r8 independently, both shapes then measured by
test-195-r8 with green/red controls]`

`renderMarkdownArityViolation` does `stripInertText(src, { strings: true })`, which blanks
string and comment contents but **deliberately preserves template-literal bodies** (html``
sinks must stay visible). `balancedDeclarationParameterLists` then counts raw `(` and `)`
with **zero literal awareness**. A TypeScript **template-literal type** is legal in a
parameter position and may contain parentheses:

- **SHAPE 1 — TRUNCATE.** A `)` inside a template type ends the captured list early.
- **SHAPE 2 — SWALLOW.** A `(` inside a template type leaves the list unterminated;
  the function returns **the whole file tail**; `splitTopLevelParameters` never returns to
  depth 0, finds no top-level comma, and reports **exactly one parameter**.

Both declaration-side halves go blind together again (`Function.length` is also 1 when the
second parameter is defaulted) — **this re-creates the exact failure mode round 8 was written
to close.** Controls: D1/D4/D5 green at 78/123 with tsc 0 and build 0; D2/D3/D6 one-token
controls RED — *"the backtick is the whole exploit."*

**FIX THE CLASS, NOT THE INSTANCE.** Write **one shared literal-skipping helper** that skips
`` `...` ``, `'...'`, `"..."` and `/.../` spans, and use it in **all** of:
`balancedDeclarationParameterLists`, `splitTopLevelParameters`, `hasTopLevelDefault`,
`callArguments`, `sinkArgumentIsSanitized`. Every one of those runs over the
strings-blanked-but-templates-preserved view and every one is latently affected. Patching
only the one that was exploited will get this finding re-filed in round 10.

**Pins to add to `ARITY_EVASIONS`:** the truncate form ``md: string | `)` ``, the swallow form
``md: string | `(` ``, the function-type form ``md: (x: string) => string | `)` ``, and the
bare-string mirror `md: ")"` (which must stay RED for a different reason — string contents
*are* blanked — and so is a genuine cross-axis control).

### 1b. PIN THE LAYER THAT IS CURRENTLY SAVING US BY ACCIDENT — mandatory, no leg asked for this

The only reason this is HIGH and not Critical is that `sinkArgumentIsSanitized` still rejects
these payloads at the **call site**, because R5 demands a bare `renderMarkdown(...)` call with
nothing trailing, so the truncated forms get flagged as "not a bare call".
`[MEASURED-BY-audit-195-r8]` **That defence is incidental. Nobody designed it, documented it,
or pinned it.** An innocent refactor deletes it with every test green, and this silently
becomes Critical.

Add an explicit test asserting a truncated/swallowed arity evasion is still caught at the call
site, with a comment stating that this is the second layer, that the severity rating depends on
it, and that it must not be relaxed without re-rating the finding.

**Also probe it properly before anyone repeats "the second layer holds".** The audit's negative
varied *template placement within the declaration* and held the **call shape constant** — so it
licenses a negative only over declaration-side spellings. Vary the **call** shape: trailing
comma, comment between arguments, parenthesised argument, template literal in the argument.
Report what you tried and what it did.

## 2. B3a — the headline production change of round 8 has NO regression pin

`[MEASURED-BY-test-195-r8 as T-2 (Blocker) and INDEPENDENTLY by review-195-r8 as R2
(REQUIRED)]` — two legs found this separately, which makes it the most solid finding of the round.

Mutation J1 — revert `markdown.ts` to the r7 process-global DOMPurify — is **GREEN at 78/123
with tsc 0**, and the reverted tree reproduces the exploit output quoted in `markdown.ts:99-103`
itself: `<p><img src="x" onerror="alert(1)"><script>alert(2)</script></p>`.

**Why nothing catches it:** R8/R9 exempt `SANITIZER_OWNER` (`markdown.ts`) *by construction*.
The one file that must own its sanitizer is the one file the ownership guard cannot police.

**Fix:** mirror the existing `sharedMarkedSingleton()` pattern — poison the process-global
DOMPurify, then assert `renderMarkdown` output is still clean.

> **Ordering matters and will bite you:** `DOMPurify.setConfig()` / `addHook()` have no clean
> undo, so a poisoner that runs early contaminates every later check in the file. **This test
> must run LAST.** Make that ordering explicit and comment why.

**Demonstrate it RED against the J1-reverted tree and GREEN at HEAD, and record both.**

---

# 3. The "61 -> 69" line — RULING, please read before touching it

**Do NOT change 69 to 68.** Two legs disagreed about this line and a note in the tree calls it
broken. `[MEASURED BY ME this session, and this one I did run]`:

```
7084880  EXPECTED_CHECKS = 49   check( call sites = 49     <-- EQUAL here
951ee89  EXPECTED_CHECKS = 54   check( call sites = 53     <-- silently diverged HERE
615a355  EXPECTED_CHECKS = 59   check( call sites = 58
fc2b947  EXPECTED_CHECK_CALL_SITES = 68, REQUIRED_SINKS.length = 2
         => EXPECTED_CHECKS = 68 + (2-1) = 69
```

**Both endpoints are correct, in different units.** `54 / 59 / 61` are *checks run*;
`68 / 74 / 77` are *`check()` call sites*. The series **switches units at exactly the disputed
entry, with no marker** — and the round-7 entry already states the answer ("against a base of
68, not 69") three lines *below* the note calling it unreconciled. Nobody made a measurement
error; one `check()` call site loops over a 2-element list, so the two counts differ by
`REQUIRED_SINKS.length - 1`.

**Action:** delete the `:3618-3621` parenthetical, and add an explicit unit marker to every
entry — "(checks run)" on 49/54/59/61, "(check() call sites)" on 68/74/77 — plus one sentence
stating the two differ by `REQUIRED_SINKS.length - 1` and that they coincided at 49.
test-195-r8's recommendation to write `61 -> 68` is **overruled**; its measurement was correct.

---

# SHOULD — the non-blocking list, all from test-195-r8

- **T-3 (Medium) masking pair.** `strings:true` and the `decls.length !== 1` guard mask each
  other: C1 alone GREEN, M2 alone RED, M3 both RED. The string-blanking half has **zero unique
  fixture coverage** — it survives only because the other half fails first. Give it a fixture
  that fails when only it is broken.
- **T-4 (Medium) emptyable arrays.** Three inline arrays (9 fixtures) in the ownership check
  can be silently emptied — G1/G1b/G1c all GREEN; contrast control G2 went RED at "expected
  123 assertions to run, 118 did". Hoist them to named consts so the assertion-total pin covers
  them; guarded fixture tables go 13 -> 16.
- **T-5 (Medium).** `markdown.test.ts:1696-1698` claims the unterminated-parameter-list
  fallback fails safe. **Measured false — it is exactly shape 2 above.** It was written in
  commit `4b430c6`, whose message is "correct two false rationales".
- **T-6 (Medium).** `:643-644` "exactly the UP direction" is false; C7-k is also uniquely
  covered. This is *under*-claiming.
- **T-7 (Low).** Four "eleven" sentences are wrong — it is **13** at HEAD, and `:3205` was
  false when written. Also filed as review R3.
- **T-8 (Low).** `:2551-2554` "narrower view" — the two R7 views are **byte-identical**.
- **T-9 (Info).** Both promoted tree-wide tripwires are vacuous (H1/H2 GREEN): the predicates
  are fixtured, the **call sites** are not. Same shape as T-4.

**Rule for every sentence you touch here: re-measure it against the tree at the revision it
describes.** Four false sentences in this file were produced by inheriting a number rather
than recomputing it, and one of them was written in the commit that existed to fix false
sentences. Tag each claim in your report `[MEASURED]` or `[CARRIED]`.

---

# Verification bars

- **A green control is a finding, not a pass.** If a mutation you expect to go RED comes back
  GREEN, that is the result — write it down.
- **A negative claim needs a positive control drawn from a different axis than the one you
  searched.** Saturating one axis is how a search acquires the appearance of exhaustiveness.
- **Exit codes come from the child, never through a pipe.**
- **Predict counts before measuring.** test-195-r8's one real void run was caught only because
  it printed "PARSED 5" next to a pin that said 13.

# Deliverables — you are not done until all four exist

1. **Commits on `markdown-sanitize-r9`.** Clear messages. **Never push.**
2. **A report at `/scion-volumes/scratchpad/projects/farmtable/reports/dev-195-r9.md`** with,
   per item: what changed, RED-then-GREEN evidence for every pin, and what you skipped and why.
3. **A project log entry** in `.design/project-log/`, committed.
4. **An explicit list of every place this brief was wrong.** My last one had four errors and a
   misdirecting hint; assume this one has some too.

You MUST produce all four deliverables and then mark the task complete.
