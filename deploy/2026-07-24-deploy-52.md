# Farm Table Deploy Log — 2026-07-24 Deploy 52

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-52`)
- Commit deployed: `758de9f7b26003b8f86577e89109b77c8c590206` — `fix(dependency): exclude cross-edges in Solo mode (#159)`
- Previous revision: `farmtable-00058-l6f` (commit `42ced66`, deploy-51)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`42ced66` / rev `farmtable-00058-l6f`)

```
758de9f fix(dependency): exclude cross-edges in Solo mode (#159)
```

Key changes in this deploy:

1. **Solo Cross-Edge Fix (PR #159)**: In Solo Dependency View, a spurious dashed-blue edge
   sometimes appeared connecting two visible nodes that are NOT the selected task (e.g. a
   legitimate but incidental relationship between two chain members). This was DISTINCT from
   the earlier Feature 66 fix (which addressed node inclusion, not edge inclusion) — Feature
   66 actually unmasked this latent bug by making downstream nodes visible for the first time.
   The fix adds a small, targeted guard: when Solo mode (`isolateMode`) is active, skip
   rendering edges whose `classifyEdge()` returns null (cross-edges not part of the selected
   task's direct chain). Only 4 lines added in `ft-dependency-view.ts`.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-52.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-52.yaml \
  .
```

- Build ID: `4c553595-19ce-442f-bfc8-04a2662741dc`
- Status: **SUCCESS**
- Duration: ~3.5 minutes (23:07:22 — 23:10:56 UTC)
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Image digest: `sha256:a2f8406e8a588e6815af5182671f31f144c6d8868bca19174f19242f547ba2fa`

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --max-instances=4 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00059-xc2`**
- Traffic: **100%** to `farmtable-00059-xc2`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName,spec.template.metadata.annotations)'
```

Output:
```json
{
  "spec": {
    "template": {
      "metadata": {
        "annotations": {
          "autoscaling.knative.dev/maxScale": "4",
          "run.googleapis.com/client-name": "gcloud",
          "run.googleapis.com/client-version": "575.0.0",
          "run.googleapis.com/cloudsql-instances": "deploy-demo-test:us-central1:scion-postgres-test",
          "run.googleapis.com/startup-cpu-boost": "true"
        }
      }
    }
  },
  "status": {
    "latestCreatedRevisionName": "farmtable-00059-xc2",
    "latestReadyRevisionName": "farmtable-00059-xc2",
    "traffic": [
      {
        "latestRevision": true,
        "percent": 100,
        "revisionName": "farmtable-00059-xc2"
      }
    ],
    "url": "https://farmtable-qo7k5fvpda-uc.a.run.app"
  }
}
```

Additional verification: `curl` returns HTTP 302 (IAP redirect) confirming service is live
and IAP is enforcing.

## LIVE Solo Cross-Edge Fix Verification

### Test Setup

- Automated Playwright verification script: `verify-deploy52.mjs`
- Viewport: 1920x1080
- **EXACT original repro used** (from the brief):
  - Collection: `default` (`1e0f02d1-99cd-46bc-a739-bac0fde60710`)
  - Task: `717ab19c-e86f-4c51-8126-fc16a8f81ef7` (D16-Run-Tests)
  - URL: `?collection=1e0f02d1...&view=dependencies&task=717ab19c...&solo=1`

### Check 4(a): Solo mode shows ONLY direct chain edges (no cross-edges)

**Result: PASS**
- Used the **EXACT original repro** from the brief
- `isolateMode`: **true**
- Selected task: `717ab19c-e86f-4c51-8126-fc16a8f81ef7` (D16-Run-Tests)
- Layout nodes: **7**, rendered nodes: **5**
- Rendered edges: **7** — all are direct chain edges
  - Orange (blocking): **2** — edges from blockers (Ready-03, Ready-15 → D16-Run-Tests)
  - Purple (blocked): **5** — edges to what it blocks (D16-Run-Tests → Deploy to production, etc.)
  - Dashed/cross: **0** — the previously-spurious cross-edge is GONE
  - Other: **0**
- Screenshot (`4a-solo-crossedge-fix.png`) visually confirms: D16-Run-Tests is selected,
  orange edges from Ready-03 and Ready-15, purple edge to Deploy to production. **No
  dashed-blue cross-edge between Ready-15 → Deploy to production.**

### Check 4(b): Correct repro task confirmed

**Result: PASS**
- Selected task ID matches exactly: `717ab19c-e86f-4c51-8126-fc16a8f81ef7`

### Check 5: Non-Solo Dependency View renders ALL edges

**Result: PASS**
- Same collection without Solo mode
- `isolateMode`: **false**
- Layout nodes: **36**, rendered edges: **27** (all edges, including cross-edges)
- Non-Solo has **27 edges** vs. Solo's **7 edges** — cross-edges correctly appear when Solo is off
- Screenshot (`5-non-solo-all-edges.png`) shows full dependency graph with all relationships

## Regression Checks (Check 6)

### 6a: Perf Phase 2 Viewport Culling (deploy-49)

**Result: PASS**
- Collection: `default` — 36 tasks with dependencies
- Layout nodes: 36, DOM nodes: 36 (all visible at current scale)
- Layout edges: 27, DOM edges: 27
- Culling correct (DOM ≤ layout): true
- Scale: 0.396

### 6b: Feature 67 — LR Default + Layout Toggle (deploy-50)

**Result: PASS (3 sub-checks)**
- Default orientation: `LR` — no `?layoutdir=` URL param (PASS)
- Toggle to TB: orientation `TB`, URL shows `?layoutdir=TB` (PASS)
- Toggle back to LR: orientation `LR`, URL param removed (PASS)
- Orientation button icon: `arrow-clockwise`

### 6c: CLOSED-Task Solo Fix Regression (deploy-51)

**Result: PASS**
- Collection: `jibo` (`7e76c29c-5981-4e32-98b2-fa2bdd5ad9b7`)
- Task: `9f7731a8-a23d-493d-86eb-2ac5d39f5e7a` (CLOSED, phase 4)
- `hasNoDepsMessage`: **false** (fix still working)
- Layout nodes: **6**, Layout edges: **5**
- `closedTasksInLayout`: 1 (the explicitly-selected CLOSED task)
- **CLOSED-task Solo fix from deploy-51 is not regressed.**

### 6d: Dashboard

**Result: PASS**
- Dashboard loads correctly: `currentView='dashboard'`

### 6e: Console Errors

**Result: PASS**
- Total console errors: 2 (auth/network noise)
- Relevant errors: **0**

## Evidence Files

| File | Description |
|------|-------------|
| `verification-results.json` | JSON summary of all 10 verification checks |
| `console-errors.json` | Console errors captured during testing |
| `4a-solo-crossedge-fix.png` | Solo mode: only direct chain edges, no cross-edges (EXACT original repro) |
| `5-non-solo-all-edges.png` | Non-Solo: all 27 edges including cross-edges render normally |
| `6a-dependency-culling.png` | Dependency View regression (viewport culling working) |
| `6b-tree-default-lr.png` | Tree View default LR orientation regression |
| `6c-closed-task-solo-regression.png` | CLOSED-task Solo fix still works (deploy-51 regression) |
| `6d-dashboard.png` | Dashboard regression check |
| `verify-deploy52.mjs` | Playwright verification script |

## Final Verdict

| Check | Result | Detail |
|-------|--------|--------|
| Deploy | **PASS** | `farmtable-00059-xc2` live at 100% traffic, commit `758de9f` |
| max-instances=4 | **PASS** | `autoscaling.knative.dev/maxScale: "4"` confirmed |
| Baseline IAP | **PASS** | 302 without token, IAP enforcing |
| (4a) Solo shows only direct chain edges | **PASS** | 7 edges: 2 orange + 5 purple, 0 dashed/cross (exact repro) |
| (4b) Correct repro task used | **PASS** | `717ab19c` = D16-Run-Tests confirmed |
| (5) Non-Solo renders ALL edges | **PASS** | 27 edges including cross-edges; isolateMode=false |
| (6a) Dependency View culling (deploy-49) | **PASS** | 36 nodes, 27 edges, culling correct |
| (6b) Tree default LR (deploy-50) | **PASS** | Orientation LR, no layoutdir param |
| (6b) Tree toggle TB/LR (deploy-50) | **PASS** | TB ↔ LR toggle works, URL params correct |
| (6c) CLOSED-task Solo (deploy-51) | **PASS** | hasNoDepsMessage=false, 6 nodes, 5 edges |
| (6d) Dashboard | **PASS** | Loads correctly |
| (6e) Console errors | **PASS** | 0 relevant errors |

**Overall: ALL 10 CHECKS PASSED — Deploy 52 verified. Solo cross-edge fix confirmed working against EXACT original repro.**
