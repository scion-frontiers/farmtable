# Farm Table Deploy Log — 2026-07-23 Deploy 36

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-36`)
- Commit deployed: `8665535` — `docs: add project log for auth wiring integration`
- Previous revision: `farmtable-00041-jh7` (commit `5b05b01`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`5b05b01` / rev `farmtable-00041-jh7`)

```
8665535 docs: add project log for auth wiring integration
7bf0ae5 feat(auth): wire stage 5/6 auth components into server startup
36235c3 fix: resolve generateState duplicate after merging stages 5 and 6
5945891 Merge branch 'auth/stage6-credential-improvements'
e455aea Merge branch 'auth/stage5-oauth-iap-proxy'
36d98ae docs: add project log for stage 5 review fixes
6312706 fix(auth): address stage 5 review findings — thread safety, open redirect, PKCE
bc37487 docs: add project log for stage 6 review fixes
cea72a3 fix(auth): address stage 6 review findings — thread safety, encryption integration, state expiry
85d345b docs: add project log for auth stage 5 OAuth/IAP proxy
2955706 feat(auth): add OAuth/IAP proxy authentication (stage 5)
e32dad9 feat(auth): add encrypted credential storage, OAuth flows, token refresh, and monitoring
c6519ab fix(decomposer): remove per-subtask terminal pre-judgment from prompt (#139)
```

Key changes in this deploy:
- **Auth Stage 5: OAuth + IAP proxy auth modes** (commits `2955706`, `6312706`, `e455aea`):
  - New OAuth and IAP-proxy authentication modes added (opt-in only via
    `FARMTABLE_AUTH_MODE=oauth` or `=proxy` — NOT enabled in this deploy).
  - PKCE support, open redirect prevention, thread-safe state management.
- **Auth Stage 6: Credential improvements** (commits `e32dad9`, `cea72a3`, `5945891`):
  - Encrypted credential storage for OAuth tokens.
  - TokenRefresher: background goroutine for proactive token refresh.
  - CredentialMonitor: background goroutine for credential health monitoring.
- **Auth wiring** (commits `36235c3`, `7bf0ae5`, `8665535`):
  - Wired Stage 5/6 components into server startup.
  - With `FARMTABLE_AUTH_MODE` unset (default = `token`), behavior is identical
    to pre-wiring — no new routes, no new middleware, token-interceptor unchanged.
  - TokenRefresher and CredentialMonitor goroutines start but are effectively
    no-ops in token mode (no OAuth credentials to manage).
- **Decomposer fix** (commit `c6519ab`): Remove per-subtask terminal pre-judgment.

**IMPORTANT**: `FARMTABLE_AUTH_MODE` was NOT set — deploy uses default `token` mode.
OAuth/proxy modes are NOT enabled. Known session-to-bearer bridging gap in those
modes; they are out of scope for this deploy.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-36.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-36.yaml \
  .
```

- Build ID: `47717a6a-841b-4d2d-b797-1f5710eee6d7`
- Status: **SUCCESS**
- Finished: `2026-07-23T05:24:06.832257Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:5670bbfdc11d10d4fe2fd48bd16b7ffa343f26356288ce417b9084ef9ed2193d`

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00042-x2d`**
- Traffic: **100%** to `farmtable-00042-x2d`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output:
```json
{
  "status": {
    "latestCreatedRevisionName": "farmtable-00042-x2d",
    "latestReadyRevisionName": "farmtable-00042-x2d",
    "traffic": [
      {
        "latestRevision": true,
        "percent": 100,
        "revisionName": "farmtable-00042-x2d"
      }
    ],
    "url": "https://farmtable-qo7k5fvpda-uc.a.run.app"
  }
}
```

Env vars confirmed: only `FARMTABLE_DB_URL`, `FARMTABLE_DB_DIALECT`,
`FARMTABLE_DB_PASSWORD` (secret), and `FARMTABLE_TOKEN` (secret) are set.
**No** `FARMTABLE_AUTH_MODE`, `FARMTABLE_IAP_AUDIENCE`, `FARMTABLE_ALLOWED_DOMAINS`,
`FARMTABLE_BASE_URL`, or `FARMTABLE_ENCRYPTION_KEY`.

## Baseline IAP Check

```bash
curl -s -w '\n---HTTP_CODE:%{http_code}---' \
  https://farmtable-486315127503.us-central1.run.app/
```

- **302** with body "Invalid IAP credentials: empty token" — IAP is active and enforcing.
  **Result: ✅ PASS**

## Verification Checks

### Check (a): Service starts cleanly — Cloud Run logs

Checked Cloud Run logs for the first 5+ minutes after deploy for panics,
crash-loops, or repeated error spam from `TokenRefresher` or `CredentialMonitor`.

- Startup log: `Token authentication enabled (store-backed)` — confirms
  AuthMode=token is active.
- Startup probe: `Default STARTUP TCP probe succeeded after 1 attempt` — healthy.
- No FARMTABLE_SESSION_KEY warning: expected (session key not configured).
- Searched logs for `TokenRefresher|CredentialMonitor|panic|fatal|crash|OOM`:
  **zero results**.
- WARNING-level logs: only HTTP 401 responses to `GET /api/auth/session` from our
  Playwright tests (expected: checking session before login, after logout).
- No ERROR-level logs.

**Result: ✅ PASS** — Service starts cleanly, no destabilization from new goroutines.

### Check (b): ft CLI with dual-header IAP auth — RBAC still works

All checks executed via a custom Go verification program (`verify-deploy36.go`)
connecting to the deployed gRPC service and directly to Postgres for scoped token
creation.

**b/a1 — Read RPC (ListTasks) with existing default token:**
- Returned **50 tasks** successfully.
- **Result: ✅ PASS** — Backward compat read works.

**b/a2 — Mutating RPC (CreateTask) with existing default token:**
- Task created successfully, then cleaned up.
- **Result: ✅ PASS** — Backward compat write works.

**b/a3 — ListCollections with existing default token:**
- Returned **21 collections**.
- **Result: ✅ PASS** — Backward compat collection access works.

**b.i — Scoped read in allowed collection (task:read + collection:read):**
- ListTasks returned **34 tasks** in allowed collection.
- **Result: ✅ PASS** — Scoped read works.

**b.ii — Write blocked (no task:write scope):**
- CreateTask rejected: `PermissionDenied "missing required scope \"task:write\""`.
- **Result: ✅ PASS** — Scope enforcement works.

**b.iii — Cross-collection blocked:**
- ListTasks rejected: `PermissionDenied "token not authorized for collection 2c78db91-d17f-4d8b-9d62-29a7b1d409fd"`.
- **Result: ✅ PASS** — Collection restriction works.

**b/e1 — ListTasks via dual-header (IAP + ft token):**
- Returned **50 tasks**.
- **Result: ✅ PASS** — Dual-header read works.

**b/e2 — CreateTask via dual-header:**
- Task created and cleaned up.
- **Result: ✅ PASS** — Dual-header mutating works.

### Check (c): Web Dashboard — Session-based login (Playwright)

**c1 — Load dashboard without ?token= URL param:**
- HTTP 200 returned, app shell rendered.
- **Result: ✅ PASS** — App loads cleanly.

**c2 — Login via POST `/api/auth/session`:**
- Response: `200 {"userId":"a948da6d-10cb-4d5c-8167-b1a35de52946"}`.
- **Result: ✅ PASS** — Session login works.

**c3 — Session cookie set:**
- `farmtable_session` cookie present: `httpOnly=true`, `secure=true`, `sameSite=Lax`.
- **Result: ✅ PASS** — Session cookie correctly set.

**c4 — GET `/api/auth/session`:**
- Returns `200 {"userId":"a948da6d-10cb-4d5c-8167-b1a35de52946"}`.
- **Result: ✅ PASS** — Session readable.

**c5 — Dashboard loads data with session cookie (post-wiring):**
- Shadow DOM `ft-collection-list` rendered with collection data ("default",
  "farmtable-deploy4-web", GitHub mirrors, etc.).
- gRPC-web `ListCollections` request captured: **HTTP 200**.
- SessionToBearerMiddleware correctly bridged session cookie to gRPC auth.
- Screenshot saved: `c5-dashboard-detailed.png`.
- **Result: ✅ PASS** — Stage 5/6 wiring did NOT break session auth.

**c6 — Logout via DELETE `/api/auth/session`:**
- Returns `200 {"status":"ok"}`.
- **Result: ✅ PASS** — Logout works.

**c7 — Session invalidated after logout:**
- GET returns **401**.
- **Result: ✅ PASS** — Session properly cleared.

### Check (d): GetVersion/GetStatus unauthenticated (exempt RPCs)

**d1 — GetVersion (IAP only, no ft token):**
- Response: `server_version="dev"`.
- **Result: ✅ PASS** — GetVersion exempt RPC works.

**d2 — GetStatus (IAP only, no ft token):**
- Response: `server_mode="dev"`, `uptime_seconds=204`.
- **Result: ✅ PASS** — GetStatus exempt RPC works.

### Check (e): Cloud Run revision stability (no restarts/OOM/crash-loop)

- Revision `farmtable-00042-x2d` conditions: all **True** (Ready, Active,
  ContainerHealthy, ContainerReady, ResourcesAvailable).
- Instance lifecycle: 2 instances started (1 for deployment rollout, 1 for
  autoscaling) — normal behavior, no crash-loop restarts.
- No OOM events, no kills, no unexpected shutdowns.
- Service deployed at `05:24:33Z`, stable through `05:30+Z` (6+ minutes).
- TokenRefresher/CredentialMonitor goroutines running silently — no errors.

**Result: ✅ PASS** — Revision stable, no crash-loops, no OOM.

## Evidence

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-23-deploy-36/`:

| File | Description |
|------|-------------|
| `rbac-verification-results.json` | Full JSON results for checks b, d |
| `web-dashboard-results.json` | Full JSON results for check c (c1-c7) |
| `c1-dashboard-no-token.png` | Dashboard screenshot without token param |
| `c5-dashboard-detailed.png` | Dashboard screenshot after session auth, showing collections |
| `verify-deploy36.go` | Go verification program source (checks b, d) |
| `verify-web-dashboard.mjs` | Playwright verification script (check c) |

## Final Verdict

| Check | Result | Detail |
|-------|--------|--------|
| Deploy | **✅ PASS** | `farmtable-00042-x2d` live at 100% traffic, commit `8665535` |
| Baseline IAP | **✅ PASS** | 302 without token, 200 with valid IAP token |
| (a) Service starts cleanly | **✅ PASS** | No panics, crash-loops, or TokenRefresher/CredentialMonitor errors in logs |
| (b) ft CLI RBAC — backward compat | **✅ PASS** | Read (50 tasks), write, collection list (21) all succeed with existing token |
| (b) ft CLI RBAC — scope enforcement | **✅ PASS** | task:write blocked, cross-collection blocked, scoped read works |
| (b) ft CLI RBAC — dual-header auth | **✅ PASS** | Read and mutating RPCs work via IAP + x-farmtable-token |
| (c) Web dashboard session auth | **✅ PASS** | Login, cookie, data loading (gRPC-web), logout, invalidation all work |
| (d) Unauthenticated RPCs | **✅ PASS** | GetVersion and GetStatus work with IAP only (no ft token) |
| (e) Revision stability | **✅ PASS** | No restarts, OOM, or crash-loops over 6+ minutes. All conditions True. |

**ALL CHECKS PASSED.**

Auth Stage 5/6 wiring (commit `8665535`) is confirmed live on revision
`farmtable-00042-x2d` with no regressions:
- **Default AuthMode=token unchanged**: No new routes, no new middleware.
- **Background goroutines stable**: TokenRefresher and CredentialMonitor running
  without errors or resource issues.
- **RBAC enforcement intact**: Scope and collection enforcement working exactly
  as in deploy-35.
- **Session auth intact**: Web dashboard session-to-bearer middleware unaffected.
- **Exempt RPCs intact**: GetVersion/GetStatus work without ft token.
- **Dual-header pattern working**: ft CLI / decomposer IAP+token fully functional.
