# Farm Table Deploy Log - 2026-07-20 Deploy 4

## Source

- Repo: `/workspace/farmtable`
- Branch: `main`
- Commit deployed: `691a77b42e3a765c93a4800c262ab378eb814c93`
- `git fetch origin main feat/export-import-web-ui`: updated `origin/main` from `a678aea` to `691a77b`
- `git switch main`
- `git pull origin main`: fast-forwarded local `main` from `a678aea` to `691a77b`
- Pre-existing untracked workspace files were left untouched.
- No application code changes were made.

## Build

The prescribed command was attempted:

```bash
gcloud builds submit \
  --tag us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --project=deploy-demo-test \
  --file=Dockerfile.server \
  .
```

This installed `gcloud` still rejects `--file`:

```text
ERROR: (gcloud.builds.submit) unrecognized arguments: --file=Dockerfile.server (did you mean '--flatten'?)
```

Used the same fallback pattern as prior deploys: temporary Cloud Build config outside
the repo running Docker with `-f Dockerfile.server`.

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-4.yaml \
  .
```

- Build ID: `7baf902f-d755-4644-82f0-bccaa77dd0d9`
- Status: `SUCCESS`
- Started: `2026-07-20T05:50:44.175282333Z`
- Finished: `2026-07-20T05:53:45.219726Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:5628af9f25fb304ece36b661e77502fc68c34ec56e2e82c4e657ec9afb4a5a9b`
- Note: local log streaming exited with the known default logs bucket permission message; `gcloud builds describe` confirmed success.

## Deployment

Command:

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

Result:

- Service: `farmtable`
- Region: `us-central1`
- Revision: `farmtable-00009-826`
- Traffic: 100% to `farmtable-00009-826`
- Primary URL: `https://farmtable-qo7k5fvpda-uc.a.run.app`
- Deploy output URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

## Baseline Smoke Tests

Dashboard HTTP:

```bash
curl -i https://farmtable-qo7k5fvpda-uc.a.run.app/
```

- PASS: `HTTP/2 200`, page title asset payload returned.

CLI:

```bash
TOKEN=$(gcloud secrets versions access latest --secret=farmtable-token --project=deploy-demo-test)
export PATH=/workspace/.farmtable/bin:$PATH
export FARMTABLE_SERVER=farmtable-qo7k5fvpda-uc.a.run.app:443
ft task list --token "$TOKEN"
```

- PASS: returned 4 tasks from existing `default` collection `1e0f02d1-99cd-46bc-a739-bac0fde60710`.
- Local `ft` was rebuilt from deployed source with `go build -o /workspace/.farmtable/bin/ft ./cmd/ft` so export/import commands were available.

Existing protected collections were not modified or deleted:

- `default`: `1e0f02d1-99cd-46bc-a739-bac0fde60710`
- GitHub-platform test collection: `466c2baa-334e-439c-b9f9-abbe89eb8aae`

## CLI Export/Import Round Trip

Export command:

```bash
ft collection export default \
  --out /tmp/default-export-deploy-4.json \
  --token "$TOKEN"
```

Result:

- PASS
- Runtime: approximately `0.151s`
- File: `/tmp/default-export-deploy-4.json`
- Size: `3188` bytes
- Mode: `0600`
- md5: `f9a59358554ebe0bf8238eed30e5e599`
- JSON sections:
  - `format_version`: `1`
  - `generator`: `farmtable`
  - `collection.name`: `default`
  - `users`: `0`
  - `tasks`: `4`
  - `comments`: `0`
  - `relationships`: `0`
  - `changes`: `0`

Import command:

```bash
ft collection import /tmp/default-export-deploy-4.json \
  --name farmtable-deploy4-cli-20260720055552 \
  --token "$TOKEN" \
  -o json
```

Result:

- PASS
- Runtime: approximately `0.112s`
- New collection ID: `b05c411c-283a-4c7e-a3a6-01673676a317`
- Stats: `tasks=4`, `comments=0`, `relationships=0`, `changes=0`, `users_created=0`, `users_matched=0`
- Warnings: `null`

Verification commands:

```bash
ft collection list --token "$TOKEN" -o json
ft task list --collection 1e0f02d1-99cd-46bc-a739-bac0fde60710 --full --token "$TOKEN" -o json
ft task list --collection b05c411c-283a-4c7e-a3a6-01673676a317 --full --token "$TOKEN" -o json
```

Task count matched source and imported collection: `4` / `4`.

Spot checks matched:

- `Test task from real client`: source and import both `priority=NORMAL`, `stage=backlog`, `phase=OPEN`, `labels=null`
- `my-new-task`: source and import both `stage=in_review`, `phase=IN_PROGRESS`, `labels=["bar","foo"]`

Note: `ft task list --collection default` returned `invalid collection_id: invalid UUID length: 7`; reran with the default collection UUID. This is a CLI scoping behavior note, not an export/import failure.

CLI round-trip verdict: PASS.

## Web UI Export/Import Round Trip

Browser automation:

```bash
playwright-cli open --browser=chromium https://farmtable-qo7k5fvpda-uc.a.run.app/
```

The live dashboard opened on the collection selector. Selected `default Farm Table`, which navigated to:

```text
https://farmtable-qo7k5fvpda-uc.a.run.app/?collection=1e0f02d1-99cd-46bc-a739-bac0fde60710
```

Export:

- Clicked toolbar `Export collection` button.
- Network: `FarmTableService/ExportCollection => 200`
- Suggested filename: `default-2026-07-20.json`
- Saved verification copy: `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-20-deploy-4/web-export-default.json`
- Size: `3188` bytes
- md5: `2650eceb2eab3a8765d713cec176d664`
- JSON sections: `format_version=1`, `collection.name=default`, `tasks=4`, `comments=0`, `relationships=0`, `changes=0`, `users=0`
- Observed runtime: under 1 second; no visible slowness.

Import:

- Clicked toolbar `Import collection` button.
- Chose `web-export-default.json` through the dialog file input.
- Preview showed:
  - `Collection: "default"`
  - `Tasks: 4`
  - `Comments: 0`
  - `Relationships: 0`
- Set collection name to `farmtable-deploy4-web-202607200557`.
- Clicked dialog `Import`.
- Network: `FarmTableService/ImportCollection => 200`
- Landed on:

```text
https://farmtable-qo7k5fvpda-uc.a.run.app/?collection=2c78db91-d17f-4d8b-9d62-29a7b1d409fd
```

Result:

- PASS
- New collection ID: `2c78db91-d17f-4d8b-9d62-29a7b1d409fd`
- Board showed visible imported tasks in the expected columns, including:
  - `foobar`
  - `Test task from real client` with `Normal`
  - `smoke-test-1784467180`
  - `my-new-task` with labels `bar`, `foo`
- CLI cross-check returned `total_count=4` for the web-imported collection and matching task fields.
- Observed runtime: import completed and board loaded in roughly 1 second after the successful click; no visible slowness.

Web UI round-trip verdict: PASS.

## Screenshots and Artifacts

Directory:

```text
/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-20-deploy-4/
```

Files:

- `01-web-import-preview.png` (`6243f7226c8251ec28a82eb48d7eb4ce`, `55875` bytes): import dialog with selected JSON, sane preview counts, and edited collection name.
- `02-web-import-result.png` (`b19869c167fcb66ce8483d7b30d9beeb`, `37313` bytes): resulting imported collection board with visible tasks.
- `web-export-default.json` (`2650eceb2eab3a8765d713cec176d664`, `3188` bytes): real file downloaded via the web UI export button.

The two screenshots are distinct by md5.

## Issues / Notes

- Prescribed `gcloud builds submit --file=Dockerfile.server` is unsupported by the installed `gcloud`; fallback Cloud Build config succeeded.
- Cloud Build log streaming exited due to default logs bucket permissions; `gcloud builds describe` confirmed `SUCCESS`.
- Playwright CLI initially needed a browser install. Installed with `playwright-cli install-browser chrome-for-testing`, then opened with `--browser=chromium`.
- Browser console had one non-blocking favicon 404: `GET /favicon.ico => 404`.
- One Playwright selector attempt timed out trying to find the dialog import button by nested role. The dialog remained open and ready; clicking the concrete snapshot button ref succeeded. No application error was observed.

## Final Verdict

- Deploy: PASS, `farmtable-00009-826` live at 100% traffic.
- Baseline smoke tests: PASS.
- CLI export/import round trip: PASS.
- Web UI export/import round trip: PASS.
