# Farm Table Deploy Log — 2026-07-23 Deploy 42

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-42`)
- Commit deployed: `eef367b29fcca7b224c31184549bc545db3f71fe` — `feat(view): default to Dashboard view, reorder switcher, preserve task-deep-link UX (#146)`
- Previous revision: `farmtable-00048-zgz` (commit `65deb12`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`65deb12` / rev `farmtable-00048-zgz`)

```
eef367b feat(view): default to Dashboard view, reorder switcher, preserve task-deep-link UX (#146)
```

Key changes in this deploy (Feature 63, PR #146):
1. **Default view changed**: No `?view=` URL param now defaults to Dashboard (was Kanban).
2. **View switcher reordered**: Dashboard is now leftmost, before Kanban.
3. **Dashboard icon changed**: From `bar-chart-line` to `grid`.
4. **Task deep-link fallback**: When `?task=` is present but `?view=` is absent,
   defaults to Kanban (not Dashboard), since Dashboard doesn't support task
   centering/highlighting. This preserves Feature 62's deep-link UX.

Files changed: `web/src/components/ft-app.ts` and `web/src/components/ft-toolbar.ts`.

**LOW-RISK DEPLOY** — UI-only routing/default-view change, two review rounds
(one caught and fixed a real regression against Feature 62), with real verified
evidence from both the PR review and this live deployment verification.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-42.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-42.yaml \
  .
```

- Build ID: `2820899f-32b4-4970-94cb-1fcfaae1dd79`
- Status: **SUCCESS**
- Duration: ~3m 39s (23:47:42 to 23:51:21 UTC)
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:cb94ead645135f3f3d4f065d8842801cadc4db5e307ab00bb643bf1eac3a2583`

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --max-instances=4 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00049-4rl`**
- Traffic: **100%** to `farmtable-00049-4rl`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName,spec.template.metadata.annotations)'
```

Output:
```json
{
  "spec": {
    "template": {
      "metadata": {
        "annotations": {
          "autoscaling.knative.dev/maxScale": "4",
          "run.googleapis.com/cloudsql-instances": "deploy-demo-test:us-central1:scion-postgres-test",
          "run.googleapis.com/startup-cpu-boost": "true"
        }
      }
    }
  },
  "status": {
    "latestCreatedRevisionName": "farmtable-00049-4rl",
    "latestReadyRevisionName": "farmtable-00049-4rl",
    "traffic": [
      {
        "latestRevision": true,
        "percent": 100,
        "revisionName": "farmtable-00049-4rl"
      }
    ],
    "url": "https://farmtable-qo7k5fvpda-uc.a.run.app"
  }
}
```

Additional verification: `curl` returns HTTP 302 (IAP redirect) confirming
service is live and IAP is enforcing.

## Verification Checks

All checks performed via Playwright (headless Chromium) against the live deployed
instance at `https://farmtable-486315127503.us-central1.run.app`.

### Check (a): Default view is Dashboard (no ?view=, no ?task=)

- Navigated to `/?collection=<id>` with NO `?view=` and NO `?task=` params.
- `currentView` is `"dashboard"`. **PASS**
- `ft-dashboard-view` component present in shadow DOM. **PASS**
- URL has no `?view=` param (dashboard is the implicit default). **PASS**

**Result: PASS**

### Check (b): View switcher order and icon

- View switcher radio buttons inspected via shadow DOM.
- Order: Dashboard, Kanban, Tree, Dependencies, Ready Queue. **PASS**
- Dashboard is first (leftmost). **PASS**
- Dashboard icon is `grid` (not `bar-chart-line`). **PASS**
- Kanban is second (after Dashboard). **PASS**
- Dashboard is selected in the view switcher. **PASS**

**Result: PASS**

### Check (c): Task deep-link fallback to Kanban (CRITICAL — regression fix)

This is the specific regression that was caught during Feature 63 review and
fixed before merge. A URL with `?task=<id>` but NO `?view=` must land on
Kanban (not Dashboard), because Dashboard doesn't support task
centering/highlighting.

- Deep-link URL: `/?collection=<id>&task=1015aab2-d142-4c0e-ae25-8d874a1676ca`
- Opened in **FRESH browser context** (new session, new IAP token).
- `currentView` is `"kanban"` (not `"dashboard"`). **PASS**
- `ft-kanban-view` component present, `ft-dashboard-view` absent. **PASS**
- Task `1015aab2-d142-4c0e-ae25-8d874a1676ca` is selected. **PASS**
- Inspector is open with task content. **PASS**
- No dim overlay present. **PASS**
- View switcher shows Kanban as selected. **PASS**

**Result: PASS** — The regression fix is confirmed working live.

### Check (d): Explicit ?view= params

- `?view=kanban` → `currentView: "kanban"`. **PASS**
- `?view=tree` → `currentView: "tree"`. **PASS**
- `?view=dependencies` → `currentView: "dependencies"`. **PASS**
- `?view=dashboard` → `currentView: "dashboard"`. **PASS**
- `?view=dashboard&task=<id>` → stays on `"dashboard"` (explicit view overrides
  fallback logic). **PASS**

**Result: PASS** — All explicit view params work correctly.

### Check (e): Regression — normal features and Feature 62 deep-links

- **Tree View**: SVG renders, 37 nodes present. **PASS**
- **Task selection**: Click-to-select works, correct `selectedTaskId` set. **PASS**
- **URL sync**: Selecting task adds `?task=` to URL (Feature 62). **PASS**
- **Close Inspector**: `?task=` removed from URL (Feature 62). **PASS**
- **Deep-link round-trip**: Fresh context tree deep-link opens correct view,
  selects task, opens Inspector (Feature 62). **PASS**
- **Kanban View**: 8 columns render correctly. **PASS**
- **Dependency View**: 37 nodes render correctly. **PASS**
- **Console errors**: Zero relevant errors (11 filtered — all favicon 404 or
  gRPC stream closures from navigation, expected behavior). **PASS**

**Result: PASS** — No regression to normal browsing or Feature 62 deep-links.

## Evidence

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-23-deploy-42/`:

| File | Description |
|------|-------------|
| `verification-results.json` | JSON summary of all 23 verification checks |
| `console-errors.json` | Console errors captured during testing |
| `verify-deploy42.mjs` | Playwright verification script |
| `a-default-dashboard.png` | Default view (no params) → Dashboard loaded |
| `b-view-switcher.png` | View switcher showing Dashboard first with grid icon |
| `c1-task-deeplink-kanban.png` | Task deep-link (no ?view=) → Kanban with task selected, Inspector open |
| `d-explicit-kanban.png` | Explicit ?view=kanban |
| `d-explicit-tree.png` | Explicit ?view=tree |
| `d-explicit-dependencies.png` | Explicit ?view=dependencies |
| `d-explicit-dashboard.png` | Explicit ?view=dashboard |
| `d-explicit-dashboard-with-task.png` | ?view=dashboard&task=<id> stays on dashboard |
| `e-deeplink-tree-regression.png` | Feature 62 tree deep-link regression check |
| `e-regression-kanban.png` | Kanban view regression check |

## Final Verdict

| Check | Result | Detail |
|-------|--------|--------|
| Deploy | **PASS** | `farmtable-00049-4rl` live at 100% traffic, commit `eef367b` |
| max-instances=4 | **PASS** | `autoscaling.knative.dev/maxScale: "4"` confirmed |
| Baseline IAP | **PASS** | 302 without token, IAP enforcing |
| (a) Default view is Dashboard | **PASS** | `currentView: "dashboard"`, component present |
| (b) Dashboard first in switcher | **PASS** | Order: Dashboard, Kanban, Tree, Dependencies, Ready Queue |
| (b) Grid icon | **PASS** | Icon is `grid` (not `bar-chart-line`) |
| (b) Kanban second | **PASS** | Kanban is second after Dashboard |
| (b) Dashboard selected | **PASS** | View switcher value: `"dashboard"` |
| (c) Deep-link → Kanban | **PASS** | `?task=` without `?view=` → `currentView: "kanban"` |
| (c) Task selected | **PASS** | Correct task selected via deep-link |
| (c) Inspector open | **PASS** | Inspector visible with task content |
| (c) No dim overlay | **PASS** | No overlay interfering |
| (c) Switcher shows Kanban | **PASS** | View switcher value: `"kanban"` |
| (d) ?view=kanban | **PASS** | Loads correctly |
| (d) ?view=tree | **PASS** | Loads correctly |
| (d) ?view=dependencies | **PASS** | Loads correctly |
| (d) ?view=dashboard | **PASS** | Loads correctly |
| (d) ?view=dashboard&task= | **PASS** | Stays on dashboard (explicit view overrides) |
| (e) Tree renders | **PASS** | SVG + 37 nodes |
| (e) Task selection | **PASS** | Click-to-select works |
| (e) URL sync (Feature 62) | **PASS** | `?task=` added on select |
| (e) Close Inspector (Feature 62) | **PASS** | `?task=` removed on close |
| (e) Deep-link round-trip (Feature 62) | **PASS** | Fresh context deep-link works |
| (e) Kanban renders | **PASS** | 8 columns |
| (e) Dependency renders | **PASS** | 37 nodes |
| (e) Console errors | **PASS** | Zero relevant errors |

**ALL 23 CHECKS PASSED.**

Feature 63 (PR #146) is confirmed working live on revision `farmtable-00049-4rl`:
- Default view (no `?view=` param) is Dashboard ✓
- View switcher reordered: Dashboard first with grid icon ✓
- Task deep-links (`?task=` without `?view=`) correctly fall back to Kanban ✓
- All explicit `?view=` params work correctly ✓
- Feature 62 (task deep-linking) is not regressed ✓
- Normal view switching and navigation works ✓
- Zero console errors ✓
