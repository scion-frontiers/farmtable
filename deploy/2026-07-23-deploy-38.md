# Farm Table Deploy Log — 2026-07-23 Deploy 38

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-38`)
- Commit deployed: `0cf1f4b` — `feat(tree): add Solo (isolate) mode for tree view (#140)`
- Previous revision: `farmtable-00043-w5m` (commit `5c05b0d`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`5c05b0d` / rev `farmtable-00043-w5m`)

```
0cf1f4b feat(tree): add Solo (isolate) mode for tree view (#140)
5d197fe fix(auth): reuse existing session token in IAP middleware
```

Key changes in this deploy:
- **Feature 61 — Solo tree view mode** (commit `0cf1f4b`, PR #140):
  A "Solo" toggle button in the Tree View's hierarchy nav bar that filters
  the visible tree to show only the selected task and its descendants.
  Two review rounds passed; label renamed from "Isolate" to "Solo" per
  ptone's preference. Changes limited to two web component files:
  `ft-hierarchy-nav.ts` (76 lines) and `ft-tree-view.ts` (39 lines).

- **Session token reuse fix** (commit `5d197fe`):
  Minor auth fix — reuses existing session token in IAP middleware instead
  of creating a new one. Single file change in `internal/serverapp/unified.go`
  (9 lines added, 4 removed).

Files changed: `web/src/components/tree/ft-hierarchy-nav.ts`,
`web/src/components/tree/ft-tree-view.ts`,
`internal/serverapp/unified.go` (116 lines net added).

**LOW-RISK DEPLOY** — narrowly-scoped UI feature (Tree View only) + minor
auth optimization. No new routes, no schema changes, no backend behavior changes.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-38.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-38.yaml \
  .
```

- Build ID: `dab0cf3f-a06f-435c-ac78-990f55f27ca7`
- Status: **SUCCESS**
- Finished: `2026-07-23T15:39:45.336997Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:a9c3c769f8d2937aaf27319ef9a7384067f99ce7b8cd710f5dff8734f678fb39`

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00044-g8l`**
- Traffic: **100%** to `farmtable-00044-g8l`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output:
```json
{
  "status": {
    "latestCreatedRevisionName": "farmtable-00044-g8l",
    "latestReadyRevisionName": "farmtable-00044-g8l",
    "traffic": [
      {
        "latestRevision": true,
        "percent": 100,
        "revisionName": "farmtable-00044-g8l"
      }
    ],
    "url": "https://farmtable-qo7k5fvpda-uc.a.run.app"
  }
}
```

## Baseline IAP Check

```bash
curl -s -w '\n---HTTP_CODE:%{http_code}---' \
  https://farmtable-486315127503.us-central1.run.app/
```

- **302** with body "Invalid IAP credentials: empty token" — IAP is active and enforcing.
  **Result: PASS**

## Verification Checks

### Feature 61: Solo Tree View Mode (Playwright against live instance)

All checks executed via Playwright (`verify-deploy38.mjs`) using system Chromium
against the deployed service. Authentication via IAP token + session login.
Collection tested: `default` (37 tasks with multi-level hierarchy).

**Check (a): Solo button appears and is enabled for mid-hierarchy task**

- Selected mid-hierarchy task "foobar" (has 1 direct child, 1 total descendant,
  parent: `51dae6ed`).
- Solo button found in `ft-hierarchy-nav` shadow DOM, **enabled** (not disabled).
- Button text: "Solo" (not "Isolate").
- Screenshot: `a-solo-button-enabled.png`

**Result: PASS**

**Check (b): Solo click filters tree to selected task + descendants**

- Before Solo: **37 nodes** visible in tree.
- Clicked Solo button.
- After Solo: **2 nodes** visible — "foobar" (selected) + "my-new-task" (child).
- Expected: 1 (selected) + 1 (descendant) = 2. **Matches exactly.**
- Solo button state: active (blue highlight, `active` CSS class).
- Screenshot: `b-solo-active.png`

**Result: PASS** — Node count: 37 → 2 (expected 2)

**Check (c): Solo toggle off returns to full tree**

- Clicked Solo button again to deactivate.
- After toggle off: **37 nodes** visible — same as before Solo.
- Solo button state: inactive (no `active` class).
- Screenshot: `c-solo-off.png`

**Result: PASS** — Node count restored: 2 → 37

**Check (d): Button label and console errors**

- **d-label**: Button text reads "Solo" — confirmed no "Isolate" text present.
  **Result: PASS**
- **d-console**: Zero relevant console errors captured during all Solo interactions.
  **Result: PASS**

### Regression: Normal Tree View Browsing

**Check (e1): Tree view renders correctly (SVG + nodes + edges)**

- SVG canvas present, 37 nodes rendered, 5 hierarchy edges, valid viewBox.
- **Result: PASS**

**Check (e2): Minimap is present**

- `ft-minimap` element found in tree view shadow DOM.
- **Result: PASS**

**Check (e3): Node selection (highlight) works**

- Clicked "Test task from real client" node.
- `ft-tree-node[selected]` attribute correctly set on the clicked node.
- **Result: PASS**

**Check (e4): Zoom (mouse wheel) changes viewBox**

- Dispatched wheel event on SVG canvas.
- viewBox changed from `342.0...,-356.1...,1328.4...,854.5...` to
  `402.3...,-317.3...,1207.6...,776.8...` — zoom-in confirmed.
- **Result: PASS**

## Evidence

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-23-deploy-38/`:

| File | Description |
|------|-------------|
| `verification-results.json` | Full JSON results for all checks (a–e) |
| `console-errors.json` | Console errors captured during testing |
| `a-solo-button-enabled.png` | Tree view with task selected, Solo button enabled |
| `b-solo-active.png` | Tree filtered to 2 nodes in Solo mode |
| `c-solo-off.png` | Tree restored to full 37 nodes after Solo off |
| `e-regression-tree-view.png` | Regression smoke: tree view with zoom applied |
| `step1-after-login.png` | Collection picker after session login |
| `step2-tree-view.png` | Tree view loaded with 37 nodes |
| `verify-deploy38.mjs` | Playwright verification script (all checks) |

## Final Verdict

| Check | Result | Detail |
|-------|--------|--------|
| Deploy | **PASS** | `farmtable-00044-g8l` live at 100% traffic, commit `0cf1f4b` |
| Baseline IAP | **PASS** | 302 without token, IAP enforcing |
| (a) Solo button appears | **PASS** | Enabled for mid-hierarchy task "foobar" |
| (b) Solo filters tree | **PASS** | 37 → 2 nodes (matches 1 selected + 1 descendant) |
| (c) Solo toggle off | **PASS** | 2 → 37 nodes (full tree restored) |
| (d) Label "Solo" | **PASS** | Button reads "Solo", not "Isolate" |
| (d) No console errors | **PASS** | Zero errors during Solo interactions |
| (e1) Tree renders | **PASS** | SVG + 37 nodes + 5 edges + valid viewBox |
| (e2) Minimap | **PASS** | ft-minimap present |
| (e3) Node selection | **PASS** | Highlight works on click |
| (e4) Zoom | **PASS** | viewBox changes on wheel event |

**ALL CHECKS PASSED.**

Feature 61 (Solo tree view mode) is confirmed working live on revision
`farmtable-00044-g8l`:
- Solo button correctly appears with "Solo" label (not "Isolate")
- Filters tree to selected task + descendants with correct node count
- Toggle off restores full tree
- No console errors
- Prior tree view features (pan/zoom/minimap/selection) unaffected
