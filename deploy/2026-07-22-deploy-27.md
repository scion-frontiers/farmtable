# Farm Table Deploy Log — 2026-07-22 Deploy 27

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-27`)
- Commit deployed: `40f8d82` — `fix(web): remove redundant Relations section from Inspector General tab (#129)`
- Previous revision: `farmtable-00032-ksz` (commit `0596714`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`0596714` / rev `farmtable-00032-ksz`)

```
40f8d82 fix(web): remove redundant Relations section from Inspector General tab (#129)
```

Key change in this deploy:
- **Feature 53: Remove Redundant Relations Section** (PR #129):
  - Removed the old "Relations" section from the Inspector's General tab.
  - The dedicated Relationships tab remains the sole place where
    relationships are viewed and managed (Parent, Children, Blocked By,
    Blocks, Related, Duplicate Of).
  - No functional change to the Relationships tab itself — all relationship
    types, add/delete UI, and search-to-relate dialog are unaffected.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-27.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-27.yaml \
  .
```

- Build ID: `447a33b7-856b-436a-8837-ed10bb219e1d`
- Status: **SUCCESS**
- Finished: `2026-07-22T19:19:50.096415Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:e159840f5ff5dc4bc7a7ed8752352b65144368ddfa4e548a7bf1cd68d29c17f6`
- Note: Local log streaming exited with the known default logs bucket permission
  message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00033-sbg`**
- Traffic: **100%** to `farmtable-00033-sbg`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00033-sbg` as both `latestCreatedRevisionName` and
`latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' -L https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200 (via 302 redirect through IAP).

## Feature 53 Verification — Remove Redundant Relations Section (Playwright)

All verification performed with Playwright (Chromium headless, 1440x900 viewport)
against the live deployed URL `https://farmtable-486315127503.us-central1.run.app`.

IAP authentication was handled via a `Bearer` token obtained from
`gcloud auth print-identity-token` with the IAP OAuth client ID audience
(`486315127503-o94o6safp9v2865ddj9cupqftnst9jfn.apps.googleusercontent.com`),
passed as an `extraHTTPHeaders` `Authorization` header in the Playwright context.

Tests used the "default" collection, inspecting the task "my-new-task" (which has
labels `['bar', 'foo']`, description "Some new description", and a parent
relationship to "foobar") — providing full coverage of both the General tab
content and the Relationships tab functionality.

### V1 — General Tab: No "Relations" Section — **PASS**

1. Opened Inspector for "my-new-task" on the General tab.
2. General tab shows sections: **Properties** (Assignees, Labels, Start/Due
   date, Created/Updated), **Description**, **Comments**.
3. **No "Relations" section present anywhere** on the General tab — confirmed
   both visually and programmatically (searched all headings/labels for
   standalone "Relations" text).
4. Scrolled General tab to bottom — no hidden content below Comments.

**Key screenshots:** `f53-07-general-tab-my-new-task.png`, `f53-11-general-tab-bottom.png`

### V2 — Relationships Tab: Displays All Relationship Types — **PASS**

1. Clicked the "Relationships" tab on the same task's Inspector.
2. All six relationship categories are displayed:
   - **PARENT**: "foobar" (Working) — an existing relationship rendered correctly
   - **CHILDREN**: None
   - **BLOCKED BY**: None (with "+" add button)
   - **BLOCKS**: None (with "+" add button)
   - **RELATED**: None
   - **DUPLICATE OF**: None
3. Existing parent relationship "foobar" renders with a clickable link and
   stage badge ("Working"), confirming real data from the server.

**Key screenshot:** `f53-08-relationships-tab-my-new-task.png`

### V3 — Relationships Tab: Add Relationship (Blocked By) — **PASS**

1. Clicked the "+" button next to "BLOCKED BY" on the Relationships tab.
2. The "Search tasks to relate..." dialog appeared with:
   - TYPE filter chips: "Blocks" / **"Blocked by"** (highlighted/selected)
   - Search input field for finding tasks
   - Keyboard shortcuts: ↑↓ navigate, → add, ESC close
3. Dialog is fully functional and correctly pre-selects the "Blocked by"
   relationship type.

**Key screenshot:** `f53-09-add-blocked-by.png`

### V4 — Relationships Tab: Add Relationship (Blocks) — **PASS**

1. Clicked the "+" button next to "BLOCKS" on the Relationships tab.
2. Same search dialog appeared with TYPE filter: **"Blocks"** (highlighted)
   / "Blocked by".
3. Dialog correctly pre-selects the "Blocks" relationship type.

**Key screenshot:** `f53-10-add-blocks.png`

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-27/`:

| File | Description |
|------|-------------|
| `f53-00-app-loaded.png` | Landing page: collection selector loaded |
| `f53-00c-collection-loaded.png` | Default collection loaded in kanban view |
| `f53-07-general-tab-my-new-task.png` | **Key**: General tab for "my-new-task" — no "Relations" section |
| `f53-08-relationships-tab-my-new-task.png` | **Key**: Relationships tab showing parent "foobar" + all 6 categories |
| `f53-09-add-blocked-by.png` | **Key**: Add relationship dialog — "Blocked by" type pre-selected |
| `f53-10-add-blocks.png` | **Key**: Add relationship dialog — "Blocks" type pre-selected |
| `f53-11-general-tab-bottom.png` | General tab scrolled to bottom — no hidden Relations section |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00033-sbg` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 53: No Relations on General tab | **PASS** — section removed, only Properties/Description/Comments remain |
| Feature 53: Relationships tab displays all types | **PASS** — PARENT/CHILDREN/BLOCKED BY/BLOCKS/RELATED/DUPLICATE OF all shown |
| Feature 53: Existing relationship renders | **PASS** — "foobar" parent link with stage badge visible |
| Feature 53: Add "Blocked by" relationship | **PASS** — search dialog opens with correct type pre-selected |
| Feature 53: Add "Blocks" relationship | **PASS** — search dialog opens with correct type pre-selected |

**Feature 53 (PR #129) is CONFIRMED WORKING on the live deployed site.**
The redundant "Relations" section has been successfully removed from the
Inspector's General tab. The dedicated Relationships tab remains fully
functional — displaying all six relationship types, rendering existing
relationships with clickable links and stage badges, and providing working
add-relationship dialogs with type pre-selection and task search. All verified
using Playwright against the live deployment with real IAP authentication.
