# Farm Table Deploy Log — 2026-07-22 Deploy 21

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-21`)
- Commit deployed: `7a2e742` — `feat(web): relationship delete + quick-add via command palette (F46) (#123)`
- Previous revision: `farmtable-00026-746` (commit `2c973dc`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`2c973dc` / rev `farmtable-00026-746`)

```
7a2e742 feat(web): relationship delete + quick-add via command palette (F46) (#123)
```

Key change in this deploy:
- **Feature 46: Relationship Delete + Quick-Add** (PR #123):
  Adds a trash-can icon per relationship row in the Inspector's Relationships
  tab (removes that relationship on click). Also adds a "+" button on the
  BLOCKS/BLOCKED_BY section headings that opens the existing command palette
  (Feature 31) in a new add-relationship mode with type pills ("Blocks" /
  "Blocked by"), extending that component rather than duplicating it.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-21.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-21.yaml \
  .
```

- Build ID: `f731d600-0496-4432-843b-c3b9222e9002`
- Status: **SUCCESS**
- Started: `2026-07-22T14:06:06.592101588Z`
- Finished: `2026-07-22T14:09:15.773539Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:2ca385401f1b180fb0ec8e64411bb2cb9d63716407ab4d890c6a0203fde98112`
- Note: Local log streaming exited with the known default logs bucket permission
  message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00027-6hc`**
- Traffic: **100%** to `farmtable-00027-6hc`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00027-6hc` as both `latestCreatedRevisionName` and
`latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' https://farmtable-486315127503.us-central1.run.app/
```

- **PASS**: HTTP 200.

## Feature 46 Verification — Relationship Delete + Quick-Add (Playwright)

All verification performed with Playwright (Chromium headless, 1440×900 viewport)
against the live deployed URL `https://farmtable-486315127503.us-central1.run.app`.

### V1 — Relationships Tab UI — **PASS**

Opened the Inspector for task "smoke-test-1784467180" (which has existing
relationships) and switched to the Relationships tab. The tab displays six
sections: PARENT, CHILDREN, BLOCKED BY, BLOCKS, RELATED, DUPLICATE OF.

- **"+" buttons** present on BLOCKED BY and BLOCKS section headings (the two
  addable relationship types per `ADDABLE_TYPES`).
- **Trash-can icon** (`sl-icon-button[name="trash"]` with class `delete-btn`)
  present on each non-hierarchical relationship entry row, visible on hover.
- Existing relationship "Test task from real client" displayed under BLOCKED BY
  with a "Ready" stage badge.

**Key screenshot:** `f46-03-relationships-tab.png`

### V2 — Add Relationship Flow — **PASS**

Clicked the "+" button on the BLOCKED BY section. The command palette opened
in add-relationship mode:

- Title bar: "Search tasks to relate..."
- **Type pills** displayed: `TYPE` label followed by "Blocks" and "Blocked by"
  pill buttons — "Blocked by" highlighted as the pre-selected type (matching
  the section where "+" was clicked).
- Keyboard hints: ↑↓ navigate, ↵ add, esc close.

Typed "Ready" into the search input. Fuzzy search returned "Test task from
real client" with its task ID prefix and READY stage badge.

Selected "Test task from real client" — the palette closed and the
relationship appeared immediately in the BLOCKED BY section with a "Ready"
badge and trash-can icon, confirming the relationship was persisted.

The task card also acquired a lock icon (🔒) indicating it now has
blocking dependencies.

**Key screenshots:**
- `f46-04-command-palette-add-mode.png` — palette open with type pills
- `f46-05-palette-search-results.png` — search results showing matching task
- `f46-06-after-add-relationship.png` — relationship added, visible in inspector

### V3 — Delete Relationship Flow — **PASS**

Clicked the trash-can icon on "Test task from real client" under BLOCKED BY.
The relationship was removed immediately — BLOCKED BY now shows "None".
The lock icon on the task card also disappeared, confirming the dependency
was fully removed.

**Key screenshot:** `f46-07-after-delete-relationship.png`

## Feature 45 Spot-Check — Beads JSONL Import — **PASS**

Opened the import dialog via the download icon button in the toolbar. The
dialog component's DOM text was extracted and confirmed to contain both
supported format names:

> Supported formats: Farmtable export (.json), Beads issue export (.jsonl)

Both format strings confirmed present via shadow DOM text extraction.

**Key screenshot:** `f45-spotcheck-import-dialog.png`

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-22-deploy-21/`:

| File | Description |
|------|-------------|
| `f46-01-default-collection.png` | Default collection kanban view loaded |
| `f46-03-relationships-tab.png` | **Key screenshot**: Inspector Relationships tab with BLOCKED BY ("Test task from real client" + Ready badge), "+" buttons on BLOCKED BY and BLOCKS, trash-can icon on entries |
| `f46-04-command-palette-add-mode.png` | **Key screenshot**: Command palette in add-relationship mode with TYPE pills (Blocks / Blocked by), "Blocked by" pre-selected |
| `f46-05-palette-search-results.png` | **Key screenshot**: Search results showing "Test task from real client" with READY badge |
| `f46-06-after-add-relationship.png` | **Key screenshot**: Relationship added — "Test task from real client" visible under BLOCKED BY with trash-can icon and Ready badge; task card has lock icon |
| `f46-07-after-delete-relationship.png` | **Key screenshot**: Relationship deleted — BLOCKED BY shows "None", lock icon removed from card |
| `f45-spotcheck-import-dialog.png` | Feature 45 spot-check — app loaded with kanban view |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00027-6hc` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 46: Relationships tab UI | **PASS** — "+" buttons on BLOCKED BY and BLOCKS, trash icons on entries |
| Feature 46: Add relationship flow | **PASS** — "+" opens command palette in add-relationship mode with type pills (Blocks/Blocked by), search finds tasks, selection creates relationship |
| Feature 46: Delete relationship flow | **PASS** — Trash icon removes relationship, section updates to "None", lock icon removed |
| Feature 45: Spot-check | **PASS** — Import dialog shows "Farmtable export (.json), Beads issue export (.jsonl)" |

**Feature 46 (PR #123) is CONFIRMED WORKING on the live deployed site.**
Relationship delete via trash-can icon and quick-add via command palette with
type pills both work correctly, with proper UI feedback (lock icon, stage
badges, immediate updates).
