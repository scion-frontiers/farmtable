# Farm Table Deploy Log — 2026-07-24 Deploy 48

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-48`)
- Commit deployed: `0533f0ab39fa2719cb27cc474a32c34a56cf5bd6` — `feat(tree): add TB/LR layout orientation toggle (#154)`
- Previous revision: `farmtable-00054-jrj` (commit `d1a061c`, deploy-47)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`d1a061c` / rev `farmtable-00054-jrj`)

```
0533f0a feat(tree): add TB/LR layout orientation toggle (#154)
2a21cd9 test(auth): add IAP middleware token-reuse guard tests
```

Key changes in this deploy:

1. **Feature 67 — Tree Layout Orientation Toggle (PR #154)**: Tree View (parent-child)
   now supports left-to-right (LR) layout in addition to the existing top-to-bottom (TB).
   A rotate-toggle button (CCW icon in TB mode, CW icon in LR mode) appears next to the
   Solo button in the hierarchy nav bar. Orientation persists to `?layoutdir=LR` URL param
   (omitted for default TB). Includes a follow-up fix: the Tree-view icon in the view
   switcher no longer rotates with orientation — both Tree (`diagram-3`) and Dependencies
   (`diagram-3` + `rotate(90deg)`) icons remain visually distinct in all states.
   Does NOT touch Dependency View.

2. **IAP middleware test coverage**: Additional unit tests for token-reuse guard in IAP
   authentication middleware. No production code changes.

Files changed: `web/src/components/ft-app.ts`, `web/src/components/ft-toolbar.ts`,
`web/src/components/tree/ft-tree-view.ts`, `web/src/components/tree/ft-hierarchy-nav.ts`,
`internal/serverapp/unified_test.go`.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-48.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-48.yaml \
  .
```

- Build ID: `8e8154ac-8374-4bb0-bec7-f6efcf3be3a1`
- Status: **SUCCESS**
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Image digest: `sha256:fbbefbb0b84f8296867da3cec5cabe4a52db6510e2c17112914b404c90154594`

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --max-instances=4 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00055-kxv`**
- Traffic: **100%** to `farmtable-00055-kxv`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName,spec.template.metadata.annotations)'
```

Output:
```json
{
  "spec": {
    "template": {
      "metadata": {
        "annotations": {
          "autoscaling.knative.dev/maxScale": "4",
          "run.googleapis.com/client-name": "gcloud",
          "run.googleapis.com/client-version": "575.0.0",
          "run.googleapis.com/cloudsql-instances": "deploy-demo-test:us-central1:scion-postgres-test",
          "run.googleapis.com/startup-cpu-boost": "true"
        }
      }
    }
  },
  "status": {
    "latestCreatedRevisionName": "farmtable-00055-kxv",
    "latestReadyRevisionName": "farmtable-00055-kxv",
    "traffic": [
      {
        "latestRevision": true,
        "percent": 100,
        "revisionName": "farmtable-00055-kxv"
      }
    ],
    "url": "https://farmtable-qo7k5fvpda-uc.a.run.app"
  }
}
```

Additional verification: `curl` returns HTTP 302 (IAP redirect) confirming service is live
and IAP is enforcing.

## LIVE Feature 67 Verification — Tree Layout Orientation Toggle

### Test Setup

- **Collection**: `default` (`1e0f02d1-99cd-46bc-a739-bac0fde60710`) — 37 tasks
- **Method**: Playwright headless Chromium with programmatic DOM inspection of Lit web
  components via shadow DOM traversal
- **Solo test task**: `smoke-test-1784467180` (3-node subtree: parent → foobar → my-new-task)

### Check 4(a): Default orientation is TB (top-to-bottom)

Navigated to Tree View with no URL params. Inspected `ft-tree-view.layoutOrientation`
and the orientation toggle button in `ft-hierarchy-nav`.

**Result: PASS**
- `layoutOrientation = 'TB'` (default)
- Toggle button present with `arrow-counterclockwise` icon (CCW)
- Tooltip: "Switch to left-to-right layout"
- Solo button also present (funnel icon)
- 37 nodes rendered in TB layout (xSpread=8060, ySpread=280 — wide spread, confirming
  top-to-bottom rank direction)
- Minimap visible in bottom-left corner

### Check 4(b): Toggle to LR — tree re-layouts, icon changes

Clicked the orientation toggle button. Waited for layout re-render.

**Result: PASS**
- `layoutOrientation = 'LR'`
- Icon changed to `arrow-clockwise` (CW)
- Toggle button now has `active` class (highlighted blue)
- Tooltip changed to "Switch to top-down layout"
- Layout spread changed dramatically: xSpread=560, ySpread=3720 — tall narrow layout,
  confirming left-to-right rank direction
- Screenshot clearly shows nodes arranged left-to-right with parent→child flowing
  horizontally

### Check 4(c): URL has ?layoutdir=LR and persists on reload

**Result: PASS**
- After toggling to LR, URL updated to:
  `https://farmtable-486315127503.us-central1.run.app/?collection=...&view=tree&layoutdir=LR`
- After full page reload (`page.reload()`):
  - URL still contains `layoutdir=LR`
  - `layoutOrientation` still `'LR'`
  - Icon still `arrow-clockwise`
  - Layout preserved in LR orientation

### Check 4(d): Toggle back to TB — URL param removed, layout reflows

**Result: PASS**
- After toggling back: `layoutOrientation = 'TB'`
- Icon reverted to `arrow-counterclockwise` (CCW)
- URL: `?collection=...&view=tree` — `layoutdir` param completely removed
- Layout spread returned to original: xSpread=8060, ySpread=280

### Check 4(e): View-switcher icons are visually DISTINCT (bug fix verification)

This was the specific follow-up fix: an earlier version rotated the Tree-view icon
in the view switcher with the layout orientation, making it visually identical to the
Dependencies icon when in LR mode.

**Result: PASS** (3 sub-checks)

| Mode | Tree Icon | Dependencies Icon | Distinct? |
|------|-----------|-------------------|-----------|
| TB   | `diagram-3`, no style | `diagram-3`, `transform: rotate(90deg)` | **YES** |
| LR   | `diagram-3`, no style | `diagram-3`, `transform: rotate(90deg)` | **YES** |

- **Tree icon is identical in both TB and LR modes** — `diagram-3` with no inline style
  (no rotation applied). This confirms the bug fix: the view-switcher icon is now stable
  and does NOT rotate with the tree layout orientation.
- Dependencies icon always has `rotate(90deg)`, remaining visually distinct from Tree.

### Check 4(f): Solo mode works correctly in both orientations

Used URL-based Solo activation (`?task=<id>&solo=1`) with and without `&layoutdir=LR`.

**Result: PASS** (both orientations)

| Orientation | Solo Active | Nodes Visible | Full Tree | Correct? |
|-------------|-------------|---------------|-----------|----------|
| TB          | true        | 3             | 37        | **YES** — filtered to selected subtree |
| LR          | true        | 3             | 37        | **YES** — same subtree, LR layout |

- Solo in TB: 3 nodes arranged vertically (smoke-test → foobar → my-new-task)
- Solo in LR: same 3 nodes arranged horizontally
- Solo button shows `fullscreen-exit` icon when active (correct)
- Minimap visible in both orientations during Solo

### Check 4(g): Minimap (Perf Phase 1) works in both orientations

**Result: PASS**
- `ft-minimap` custom element present in tree view shadow DOM in both TB and LR modes
- Minimap visible in bottom-left corner of all screenshots
- Minimap updates to reflect the layout orientation (vertical in TB, horizontal in LR)

## Regression Checks (Check 5)

| Check | Result | Detail |
|-------|--------|--------|
| Dependency View | **PASS** | 37 nodes rendered correctly |
| Dashboard | **PASS** | Loads correctly, `currentView='dashboard'` |
| Default-view routing (F63) | **PASS** | Defaults to dashboard when no `?view=` param |
| Tree View normal | **PASS** | 37 nodes, TB orientation by default |
| Console errors | **PASS** | 0 relevant errors (2 benign: favicon 404, initial auth 401) |

## Evidence

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-24-deploy-48/`:

| File | Description |
|------|-------------|
| `verification-results.json` | JSON summary of all 18 verification checks |
| `console-errors.json` | Console errors captured during testing |
| `4a-tree-default-TB.png` | Tree View in default TB orientation (37 nodes) |
| `4b-tree-LR-toggled.png` | Tree View after toggling to LR orientation |
| `4c-tree-LR-after-reload.png` | Tree View still in LR after page reload (persistence) |
| `4d-tree-TB-restored.png` | Tree View after toggling back to TB |
| `4e-view-switcher-icons-LR.png` | View switcher icons in LR mode (Tree and Deps distinct) |
| `4f-solo-TB.png` | Solo mode in TB orientation (3-node subtree vertical) |
| `4f-solo-LR.png` | Solo mode in LR orientation (3-node subtree horizontal) |
| `4g-tree-minimap-TB.png` | Minimap present in TB orientation |
| `4g-tree-minimap-LR.png` | Minimap present in LR orientation |
| `5-dependency-view.png` | Dependency View regression check (37 nodes) |
| `5-dashboard.png` | Dashboard regression check |
| `5-default-view.png` | Default-view routing regression check |
| `5-tree-view-normal.png` | Tree View normal load regression check |
| `verify-deploy48.mjs` | Playwright verification script |

## Final Verdict

| Check | Result | Detail |
|-------|--------|--------|
| Deploy | **PASS** | `farmtable-00055-kxv` live at 100% traffic, commit `0533f0a` |
| max-instances=4 | **PASS** | `autoscaling.knative.dev/maxScale: "4"` confirmed |
| Baseline IAP | **PASS** | 302 without token, IAP enforcing |
| (4a) Default TB | **PASS** | `layoutOrientation='TB'`, CCW icon, 37 nodes |
| (4a) Toggle button | **PASS** | Present next to Solo, correct icon and tooltip |
| (4b) Toggle to LR | **PASS** | Layout reflows to LR, icon changes to CW, button active |
| (4c) URL param set | **PASS** | `?layoutdir=LR` added to URL |
| (4c) Persists reload | **PASS** | Still LR after full page reload |
| (4d) Toggle back TB | **PASS** | Layout reflows to TB, `layoutdir` param removed from URL |
| (4e) Icons distinct TB | **PASS** | Tree=`diagram-3` (no rotate), Deps=`diagram-3` + `rotate(90deg)` |
| (4e) Icons distinct LR | **PASS** | Same icons in LR — Tree does NOT rotate with orientation |
| (4e) Tree icon stable | **PASS** | Tree icon identical in both TB and LR (bug fix verified) |
| (4f) Solo TB | **PASS** | 3 nodes (subtree), isolateMode=true, vertical layout |
| (4f) Solo LR | **PASS** | 3 nodes (subtree), isolateMode=true, horizontal layout |
| (4g) Minimap TB | **PASS** | `ft-minimap` present and visible |
| (4g) Minimap LR | **PASS** | `ft-minimap` present and visible in LR |
| (5) Dependency View | **PASS** | 37 nodes rendered |
| (5) Dashboard | **PASS** | Loads correctly |
| (5) Default routing | **PASS** | Defaults to dashboard |
| (5) Tree View normal | **PASS** | 37 nodes, default TB |
| (5) Console errors | **PASS** | Zero relevant errors |

**ALL 18 CHECKS PASSED.**

Feature 67 (Tree Layout Orientation Toggle) is **confirmed working** on revision
`farmtable-00055-kxv`:
- **Orientation toggle** works: CCW button (TB → LR), CW button (LR → TB)
- **URL persistence** works: `?layoutdir=LR` param added/removed correctly, survives reload
- **View-switcher icon fix** verified: Tree icon does NOT rotate with layout orientation —
  Tree and Dependencies icons remain visually distinct in all states
- **Solo mode** works correctly in both TB and LR orientations
- **Minimap** (Perf Phase 1) present and functional in both orientations
- **No regressions** in Dependency View, Dashboard, default-view routing, or Tree View
