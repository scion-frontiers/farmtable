# Farm Table Deploy Log — 2026-07-24 Deploy 44

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-44`)
- Commit deployed: `e5218539d576be5a8788da9d410aac4ef0b8a134` — `feat(dashboard): add top-level Ready item count (#148)`
- Previous revision: `farmtable-00050-n8x` (commit `b67ac9d`)
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`b67ac9d` / rev `farmtable-00050-n8x`)

```
e521853 feat(dashboard): add top-level Ready item count (#148)
```

Key changes in this deploy (Feature 65, PR #148):
1. **Ready count stat card**: Dashboard view now shows a top-level "Ready" item
   count, reusing the canonical `isReady()` utility from the Ready Queue view.
2. **Green accent styling**: The Ready card is styled with a green accent
   (success color), distinct from the phase stat cards.
3. **Clickable navigation**: The Ready card is clickable (role="link") and
   dispatches a `view-change` event to navigate directly to the Ready Queue view.
4. **Keyboard accessible**: Supports Enter/Space key activation.
5. **Reactive updates**: Count updates correctly on poll refresh via the existing
   TaskStoreController reactive pattern.

Files changed: `web/src/components/ft-dashboard-view.ts`, `web/src/components/ft-app.ts`.

**LOW-RISK DEPLOY** — small, additive UI change. Single stat card added to Dashboard.
No changes to data model, API, or existing views.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-44.yaml` (standard
`gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-44.yaml \
  .
```

- Build ID: `a6e3697e-2e15-4a57-88ea-a60ce563b733`
- Status: **SUCCESS**
- Duration: ~4m 31s (18:39:43 to 18:44:13 UTC)
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:937de272d79a8bd1655f72caedd670fb4bb06d6eef4b0946ec5053fe22d2ea27`

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --max-instances=4 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00051-l7d`**
- Traffic: **100%** to `farmtable-00051-l7d`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName,spec.template.metadata.annotations)'
```

Output:
```json
{
  "spec": {
    "template": {
      "metadata": {
        "annotations": {
          "autoscaling.knative.dev/maxScale": "4",
          "run.googleapis.com/cloudsql-instances": "deploy-demo-test:us-central1:scion-postgres-test",
          "run.googleapis.com/startup-cpu-boost": "true"
        }
      }
    }
  },
  "status": {
    "latestCreatedRevisionName": "farmtable-00051-l7d",
    "latestReadyRevisionName": "farmtable-00051-l7d",
    "traffic": [
      {
        "latestRevision": true,
        "percent": 100,
        "revisionName": "farmtable-00051-l7d"
      }
    ],
    "url": "https://farmtable-qo7k5fvpda-uc.a.run.app"
  }
}
```

Additional verification: `curl` returns HTTP 302 (IAP redirect) confirming
service is live and IAP is enforcing.

## Verification Checks

All checks performed via Playwright (headless Chromium) against the live deployed
instance at `https://farmtable-486315127503.us-central1.run.app`.

### Check (a): Dashboard Ready count appears and is plausible

- Navigated to `/?collection=<id>&view=dashboard`.
- Dashboard rendered with 6 stat cards: Open (29), In Progress (8), On Hold (0),
  Closed (0), Ready (21), Total (37). **PASS**
- Ready card found with class `ready`, role="link", green accent styling. **PASS**
- Ready count is 21, which is a non-negative integer ≤ total (37). **PASS**

**Result: PASS** — Ready count card exists and shows a plausible count (21).

### Check (b): Cross-check with Ready Queue view

- Navigated to `/?collection=<id>&view=ready-queue`.
- Ready Queue renders 21 `.queue-row` elements (DOM count: 21). **PASS**
- Ready Queue header shows "Ready Queue (21)" (header count: 21). **PASS**
- Dashboard Ready count (21) === Ready Queue item count (21). **PASS**

**Result: PASS** — Counts match exactly (21 = 21).

### Check (c): Click Ready card → navigates to Ready Queue

- Returned to Dashboard view (confirmed view: `dashboard`).
- Clicked the Ready stat card element.
- View changed to `ready-queue`. **PASS**

**Result: PASS** — Clicking the Ready card navigates to Ready Queue view.

### Check (d): No console errors, no regression to other Dashboard stats

- Returned to Dashboard to verify other stats are still present.
- Phase stat cards present: Open (29), In Progress (8), On Hold (0), Closed (0).
  All 4 phase cards have valid counts. **PASS**
- Total card present with count 37. **PASS**
- Console errors: 0 relevant errors (2 total, both filtered as expected gRPC
  stream closures from navigation). **PASS**

**Result: PASS** — Other Dashboard stats intact, zero console errors.

## Evidence

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-24-deploy-44/`:

| File | Description |
|------|-------------|
| `verification-results.json` | JSON summary of all 6 verification checks |
| `console-errors.json` | Console errors captured during testing |
| `verify-deploy44.mjs` | Playwright verification script |
| `a-dashboard-ready-count.png` | Dashboard view with Ready count card visible |
| `b-ready-queue-view.png` | Ready Queue view showing 21 items |
| `c-after-ready-click.png` | View after clicking Ready card (navigated to Ready Queue) |
| `d-dashboard-regression.png` | Dashboard with all stats intact (regression check) |

## Final Verdict

| Check | Result | Detail |
|-------|--------|--------|
| Deploy | **PASS** | `farmtable-00051-l7d` live at 100% traffic, commit `e521853` |
| max-instances=4 | **PASS** | `autoscaling.knative.dev/maxScale: "4"` confirmed |
| Baseline IAP | **PASS** | 302 without token, IAP enforcing |
| (a) Ready card exists | **PASS** | Green accent card with "Ready" label on Dashboard |
| (a) Ready count plausible | **PASS** | Count 21, total 37 (21 ≤ 37) |
| (b) Count cross-check | **PASS** | Dashboard 21 = Ready Queue 21 |
| (c) Click → Ready Queue | **PASS** | View changes from dashboard to ready-queue |
| (d) Other stats intact | **PASS** | 4 phase cards + Total present with valid counts |
| (d) Console errors | **PASS** | Zero relevant errors (2 filtered) |

**ALL 6 CHECKS PASSED.**

Feature 65 (PR #148) is confirmed working live on revision `farmtable-00051-l7d`:
- Dashboard shows Ready count card with green accent styling
- Count (21) matches Ready Queue view exactly
- Card click navigates to Ready Queue
- No regression to existing Dashboard stats (Open, In Progress, On Hold, Closed, Total)
- Zero console errors
