# Farm Table Deploy Log — 2026-07-21 Deploy 9

## Source

- Repo: `https://github.com/scion-frontiers/farmtable`
- Branch: `main` (via dedicated worktree at `/workspace/farmtable-deploy-9`)
- Commit deployed: `798efc5` — `feat: add decomposer binary for LLM-driven task DAG creation (#110)`
- Previous revision: `farmtable-00014-jfd`
- No code changes were made; build+deploy+verify only.

### Commits included since last deploy (`6aeed20` / rev `farmtable-00014-jfd`)

```
798efc5 feat: add decomposer binary for LLM-driven task DAG creation (#110)
50b51ba fix(web): fix app shell flex layout so main scrolls independently (#109)
7831bd3 feat(web): scroll/frame-to-item on task navigation with dim overlay (#108)
```

Key features in this deploy:
- **Feature 35** (PR #105, already on main): Task title constant above inspector tabs.
- **Feature 36** (PR #106, already on main): Independent vertical scroll (first pass).
- **Feature 37** (PR #108): Scroll/frame-to-item on navigation + dim overlay if task not in view.
- **Feature 38** (PR #109): Truly independent main-content scroll (flex layout fix — the real fix).
- **PR #110**: Decomposer binary (`cmd/decomposer/`, `internal/decomposer/`) — standalone Go tool, does not affect web server.

## Build

Cloud Build config: `/tmp/farmtable-cloudbuild-server-deploy-9.yaml` (standard `gcr.io/cloud-builders/docker` step with `-f Dockerfile.server`).

```bash
gcloud builds submit \
  --project=deploy-demo-test \
  --config=/tmp/farmtable-cloudbuild-server-deploy-9.yaml \
  .
```

- Build ID: `1adffe4f-868e-4253-af67-6793bedc4482`
- Status: **SUCCESS**
- Started: `2026-07-21T21:25:10.645731403Z`
- Finished: `2026-07-21T21:29:36.782713Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:86894f66b2db9d98b4b1fbd2a0a9c01237a74085b356df409231def1199e2635`
- Note: Local log streaming exited with the known default logs bucket permission message; `gcloud builds describe` confirmed SUCCESS.

## Deployment

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

- Service: `farmtable`
- Region: `us-central1`
- **Revision: `farmtable-00015-65p`**
- Traffic: **100%** to `farmtable-00015-65p`
- Service URL: `https://farmtable-486315127503.us-central1.run.app`
- Primary URL: `https://farmtable-qo7k5fvpda-uc.a.run.app`

Confirmed with:

```bash
gcloud run services describe farmtable \
  --region=us-central1 \
  --project=deploy-demo-test \
  --format='json(status.url,status.traffic,status.latestReadyRevisionName,status.latestCreatedRevisionName)'
```

Output confirmed `farmtable-00015-65p` as both `latestCreatedRevisionName` and `latestReadyRevisionName` with 100% traffic.

## Baseline Smoke Test

```bash
curl -s -o /dev/null -w '%{http_code}' https://farmtable-qo7k5fvpda-uc.a.run.app/
```

- **PASS**: HTTP 200.

## Feature Verification (Playwright)

All verification performed with Playwright (Chromium headless) against the live deployed URL.

### Feature 38 — Independent Vertical Scroll (PR #109) — **PASS**

The primary feature the user asked about. Verified by:
1. Loaded the board view with 30 Ready tasks (scrollable column, scrollHeight=3191 vs clientHeight=753).
2. Opened the Inspector panel on a task.
3. Captured bounding box metrics **before** scrolling: toolbar top=0, bottom=65; inspector top=65, left=1007.
4. Scrolled the Ready column's `.cards` container down by 500px.
5. Captured bounding box metrics **after** scrolling: toolbar top=0, bottom=65; inspector top=65, left=1007.

**Result**: Toolbar and Inspector panel remained at the exact same pixel positions while the kanban column content scrolled independently. The flex layout fix in PR #109 (`:host` overflow:hidden, `.content` flex with min-height:0, `.main` overflow:auto) works correctly.

Screenshots:
- `02-scroll-position-1-top.png` — Board at scroll position 0 (top).
- `03-scroll-position-2-scrolled.png` — Board after scrolling Ready column down 500px. Header/Inspector pixel-identical.

### Feature 35 — Task Title Above Inspector Tabs (PR #105) — **PASS**

Inspector panel shows: "INSPECTOR" header label (top=91), task title "Ready Task 1" with phase/stage/priority badges (top=125), then "General | Relationships" tab group (top=189). Title is constant above the tabs as designed.

Screenshot: `04-inspector-title-above-tabs.png`

### Feature 37 — Scroll/Frame-to-Item + Dim Overlay (PR #108) — **PASS**

**Scroll-to-item**: Navigated to `ready-25` via `onTaskSelect`. The Ready column scrolled from scrollTop=500 to scrollTop=426 to bring the card into view. The card appeared highlighted at the top of the column with Inspector showing its details.

Screenshot: `05-scroll-to-item.png`

**Dim overlay**: Switched to Dashboard view and navigated to `ready-5` (not visible in Dashboard). A semi-transparent dark overlay appeared over the main content area, signaling the task is not in the current view. `getComputedStyle` confirmed display!='none' and opacity > 0.

Screenshot: `06-dim-overlay.png`

## Screenshots

All saved under `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-21-deploy-9/`:

| File | Description |
|------|-------------|
| `00-app-loaded.png` | Initial app load (collection picker) |
| `01-collection-board.png` | Board view after selecting "default" collection |
| `02-scroll-position-1-top.png` | Feature 38: Scroll position 1 (top) |
| `03-scroll-position-2-scrolled.png` | Feature 38: Scroll position 2 (scrolled, header/inspector fixed) |
| `04-inspector-title-above-tabs.png` | Feature 35: Task title above inspector tabs |
| `05-scroll-to-item.png` | Feature 37: Scroll-to-item on navigation |
| `06-dim-overlay.png` | Feature 37: Dim overlay when task not in view |

## Final Verdict

| Item | Result |
|------|--------|
| Deploy | **PASS** — `farmtable-00015-65p` live at 100% traffic |
| Baseline smoke test | **PASS** — HTTP 200 |
| Feature 38 (independent scroll) | **PASS** — Column scrolls independently, toolbar/inspector pixel-identical |
| Feature 35 (title above tabs) | **PASS** — Task title constant above inspector tabs |
| Feature 37 (scroll-to-item) | **PASS** — Column auto-scrolls to selected card |
| Feature 37 (dim overlay) | **PASS** — Overlay appears when task not visible in current view |
