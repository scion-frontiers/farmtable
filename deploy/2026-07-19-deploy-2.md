# Farm Table Deploy Log - 2026-07-19 Deploy 2

## Source

- Repo: `/workspace/farmtable`
- Branch: `main`
- Commit deployed: `f50c584c88f76d3d4c6dc5862c49536ca7564598`
- `git pull origin main`: fast-forwarded local `main` from `18657ee` to `f50c584`
- Pre-existing untracked workspace files were left untouched.

## Commands Run

```bash
gcloud auth list --filter=status:ACTIVE --format='value(account)'
gcloud config get-value project
git fetch origin main
git switch main
git pull origin main
```

The prescribed build command was attempted:

```bash
gcloud builds submit \
  --tag us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --project=deploy-demo-test \
  --file=Dockerfile.server \
  .
```

This installed `gcloud` rejected `--file=Dockerfile.server`, same as deploy 1:

```text
ERROR: (gcloud.builds.submit) unrecognized arguments: --file=Dockerfile.server
```

Used the same fallback pattern as deploy 1: temporary Cloud Build config outside the repo running Docker with `-f Dockerfile.server`.

```bash
gcloud builds submit --project=deploy-demo-test --config=/tmp/farmtable-cloudbuild-server.yaml .
gcloud builds describe 51f044fa-eff2-4630-b055-0c05a61b0994 \
  --project=deploy-demo-test \
  --format='json(status,finishTime,images,results.images)'
```

Deploy:

```bash
gcloud run deploy farmtable \
  --image=us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest \
  --region=us-central1 \
  --platform=managed \
  --use-http2 \
  --add-cloudsql-instances=deploy-demo-test:us-central1:scion-postgres-test \
  --service-account=scion-my-grove@deploy-demo-test.iam.gserviceaccount.com \
  --project=deploy-demo-test
```

Smoke tests:

```bash
curl -i https://farmtable-qo7k5fvpda-uc.a.run.app/
TOKEN=$(gcloud secrets versions access latest --secret=farmtable-token --project=deploy-demo-test)
export PATH=/workspace/.farmtable/bin:$PATH
export FARMTABLE_SERVER=farmtable-qo7k5fvpda-uc.a.run.app:443
ft task list --token "$TOKEN"
```

Browser verification used `playwright-cli` against `https://farmtable-qo7k5fvpda-uc.a.run.app/` with real navigation, clicks, and form fill; no `page.evaluate()` was used.

## Build Result

- Build ID: `51f044fa-eff2-4630-b055-0c05a61b0994`
- Status: `SUCCESS`
- Finished: `2026-07-19T16:41:31.328571Z`
- Image: `us-central1-docker.pkg.dev/deploy-demo-test/farmtable/farmtable-server:latest`
- Digest: `sha256:a861762104219023fbb77a3cb8e11d8ac2ac8be61aff9cde3e4dfe1568c1a8b9`

## Deployment Result

- Service: `farmtable`
- Region: `us-central1`
- Revision: `farmtable-00007-w87`
- Traffic: 100% to `farmtable-00007-w87`
- Primary URL: `https://farmtable-qo7k5fvpda-uc.a.run.app`
- Deploy output URL: `https://farmtable-486315127503.us-central1.run.app`

## Smoke Tests

- Dashboard HTTP check: PASS, `HTTP/2 200` from `https://farmtable-qo7k5fvpda-uc.a.run.app/`
- Native gRPC `ft task list --token "$TOKEN"`: PASS, returned 4 tasks from collection `1e0f02d1-99cd-46bc-a739-bac0fde60710`
- Feature 18 landing list: PASS. Bare URL showed `Select a collection` with `default` collection.
- Feature 18 select collection route: PASS. Clicking `default Farm Table` changed URL to `?collection=1e0f02d1-99cd-46bc-a739-bac0fde60710` and loaded the board.
- Feature 19 persistent picker: PASS. Top-left picker showed `default`; dropdown listed `default Farm Table`.
- Feature 20 new collection modal/create/switch: PASS. New collection button opened `New Collection` modal; created `smoke-test-1784479392`; app switched to `?collection=b638f6c0-1a56-4c00-9061-bd94f2731b49`; picker showed `smoke-test-1784479392`; board columns were empty.
- Feature 18 direct navigation: PASS. Direct URL `https://farmtable-qo7k5fvpda-uc.a.run.app/?collection=1e0f02d1-99cd-46bc-a739-bac0fde60710` loaded the `default` board without showing the landing list.

## Screenshots

All screenshots are in `/scion-volumes/scratchpad/projects/farmtable/deploy/2026-07-19-deploy-2/` and have distinct hashes:

- `01-landing-collection-list.png` (`5e55847b3235fa7ba88660c86547ebd6`): bare URL landing list with `Select a collection` and `default Farm Table`.
- `02-selected-board-picker.png` (`66a68feb2949ef4dba32c33d9f19bcac`): selected default collection board, URL contains default collection ID, top-left picker shows `default`.
- `03-picker-dropdown.png` (`78e2bba01b78fecac3eeae16d1d8b3d5`): picker dropdown expanded and listing `default Farm Table`.
- `04-new-collection-modal.png` (`dafeadfa3c50f09ceda074eb9208ad4e`): `New Collection` modal opened from the toolbar button.
- `05-new-empty-collection-board.png` (`7dce3dea37692c419f9e31e601742fa3`): created `smoke-test-1784479392`, switched to new collection ID, empty board visible.
- `06-direct-nav-existing-collection.png` (`052566795847d6f0c0c8a943e62f8b9c`): direct navigation to existing default collection ID loaded board immediately.

## Issues Encountered

- The handoff build command used `--file=Dockerfile.server`, which this `gcloud builds submit` version rejects. Resolved by using the deploy-1 fallback: a temporary Cloud Build config outside the repo that runs `docker build -f Dockerfile.server`.
- Cloud Build log streaming failed locally due to default logs bucket permissions for the service account. The build completed successfully and was verified with `gcloud builds describe`.
- Playwright CLI initially had no `chrome-for-testing` browser installed. Installed it with `playwright-cli install-browser chrome-for-testing`.
- Browser console showed one non-blocking 404 for `/favicon.ico`; the required UI flows passed.
