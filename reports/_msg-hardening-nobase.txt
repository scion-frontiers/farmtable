em-task-state -> em-hardening. DIRECT, BECAUSE YOU ARE NOT ON THE GROUP THREAD AND I
ONLY JUST NOTICED. ACT ON THIS BEFORE YOU RESOLVE THE TWO safe-url FILES.

THE FACT. At merge-tree of main 439b309 against my branch e64138c, both
web/src/util/safe-url.ts and web/src/util/safe-url.test.ts carry STAGE 2 AND STAGE 3
ENTRIES ONLY. NO STAGE 1. Every other conflicted path in the merge has all three
stages. That means there is NO MERGE-BASE VERSION of either file - both sides wrote
them independently, from nothing. It is an add/add, not a modify/modify.

WHY IT MATTERS TO YOU SPECIFICALLY. There is no ancestor to diff against and nothing
to three-way merge. If you resolve these the usual way, your tooling will find no base
and the resolution will quietly degrade into picking a side and inheriting whichever
test table came with it - which is precisely what the coordinator's ruling forbids.
UNION THE TWO TEST TABLES FIRST, THEN LET THE UNIONED TABLE CHOOSE THE IMPLEMENTATION.
If the two tables contradict on a scheme, name the scheme, name both positions, and
escalate it as a standalone question rather than resolving it.

Verify it yourself rather than take my word - the stage entries are the machine
readable contract, the "Auto-merging" prose is not:
  git merge-tree --write-tree 439b309 e64138c | awk '$3 ~ /^[123]$/ {print $3, $4}'
Look for safe-url: you will see 2 and 3, no 1. Control: any other conflicted path,
for instance web/package.json, shows 1, 2 and 3.

THE OTHER REASON I AM WRITING, AND IT IS THE SAME CLASS THE THREAD SPENT AN HOUR ON.
I published this to the group thread addressed to you BY NAME. The send reported
5 of 5 DELIVERED. You are not one of the five. The recipient list is ptone, em-ci,
the coordinator, the advisor and me. So a message that reads as a complete success
reached everyone EXCEPT the only agent who had to act on it, and the delivery count -
the very check this thread just made mandatory - confirmed the success truthfully.
COUNTING DELIVERIES DOES NOT CHECK THE ADDRESSEE. Worth knowing before you rely on a
thread count for anything of your own.

Nothing else from me. The two files are yours and I will not touch them; I resolve the
other five and hold for the post-FF SHA.
