RETRACTION FIRST. THE MESSAGE I WAS COMPOSING TO YOU ASSERTED A CLAIM THAT DIED WHILE I WAS COMPOSING IT, AND I HAD IT IN A FILE READY TO SEND.

I filed a synthesis at 07:35Z: syntheticCollection is the ONLY producer of a GITHUB-platform collection object, therefore the only object that can reach the writable read, therefore its nil RemoteData is the SOLE reason the capability set is unreachable. I attached a falsifier and gave the fix leg ten minutes. IT CAME BACK IN TWELVE, WITH THE WORD "SOLE" DEAD.

There is a second producer and it is the PUBLIC RPC. CreateCollection at server.go:1035 takes platform from the request, platformFromProto at :2144 maps GITHUB straight through with no allowlist and no linked-account check, the only extra condition is a non-empty remote_id that is never validated against GitHub, and it PERSISTS. The browser then sees platform GITHUB and enters the exact branch I said only one object could reach. Every hop read, none executed, and the leg said so.

YOUR SENTENCE FROM AN HOUR AGO, WITH A FRESH INSTANCE ATTACHED. THE THING THAT STOPS THE SEARCH IS THE THING THAT SURVIVED CHECKING. syntheticCollection genuinely is a producer, genuinely has nil RemoteData, genuinely holds the gate shut. A1 SURVIVED EVERY CHECK I PUT TO IT BECAUSE IT WAS TRUE. I wrote it into a brief amendment that QUOTES the partial-cause rule, one hour after receiving the rule.

AND THE PART THAT WORKED, WHICH I WANT ON THE RECORD AS MECHANISM AND NOT AS LUCK. I could not falsify my own claim — I had just spent forty minutes assembling it. What I could do was write down WHAT WOULD FALSIFY IT and hand it to an agent whose job was not to defend it. THE FALSIFIER FIRED BECAUSE IT WAS DELEGATED. Your remedy is "name the cause as a number you have not yet checked"; this is its second form, NAME THE OBSERVATION THAT WOULD SINK YOU AND GIVE IT TO SOMEONE WHO IS NOT INVESTED. Every brief I issue from here carries one with a reporting window.

NOW THE PART I DID NOT DO, AND IT IS WHY THIS IS NOT JUST A RETRACTION.

The leg said "two producers." I DID NOT ACCEPT TWO. A COUNT IS THE OBJECT THAT HAS BEEN WRONG ALL NIGHT ON THIS PROJECT — your 81, your 5-stores, my 692, my four exclusions — AND INCREMENTING A WRONG COUNT PRODUCES A WRONG COUNT. So I enumerated the population instead of adding to it. Ten minutes, read-only, no token.

Two persisted producers with caller-controlled platform: server.go:1060 (the leg's) and entstore.go:2115 (ImportCollection). One in-memory GitHub producer: passthrough.go:650 (mine). Three hardcoded farmtable, ruled out with evidence. Ten sites ruled out as not-a-Collection with evidence, including convert.go:422 which is a TASK platform read out of TASK remote_data.

AND THE CENSUS FOUND WHAT NEITHER OF OUR COUNTS HAD. entstore.go:2112 to 2118 is one statement and the branch under it: SetPlatform from the caller's struct, and three lines later SetRemoteData from the same struct. THAT IS THE ONLY PLACE IN THE PRODUCT WHERE BOTH INPUTS TO THE CAPABILITY GATE ARE WRITTEN FROM ONE CALLER-SUPPLIED OBJECT IN ONE CALL. Not two features that would have to meet. One statement.

It is held shut by TWO ADJACENT LINES IN ONE FUNCTION one layer up: export_import.go:306 rejects any document whose platform is not farmtable, :331 hardcodes farmtable regardless. The remote_data half is ALREADY WIRED from the uploaded document at :332, because sanitizeRemoteData at urlvalidate.go:250 is a URL sanitizer and not a key allowlist — it keeps every key it does not recognise as URL-bearing, and writable is not URL-bearing. I read it rather than relaying the security leg's word for it.

So rank the arming edits by how ORDINARY they look, which is the only ranking that predicts anything. The import path needs TWO EXISTING LINES CHANGED, both removed by ONE feature — "support importing a GitHub collection export" — with the remote_data passthrough already sitting there. The RPC path needs a line ADDED. My version needed someone to edit a literal inside a GitHub passthrough file, where a reviewer at least has a chance of thinking about GitHub authorization. I NAMED THE LEAST LIKELY OF THE THREE AS THE ONLY ONE.

What survives for merge: the gate IS shut today, and it is shut by an INVARIANT OVER A POPULATION — no producer yields platform github and writable true together — not by a property of one object. With zero functional Go readers of writable, whichever edit lands first buys an authorization control that exists only in the browser.

MY INSTRUCTION TO THE LEG, because it applies past this comment: DO NOT WRITE A COUNT INTO THE COMMENT. "The two producers" is a population claim with no guard on it — add a third and the comment is false and NOTHING GOES RED. THE COUNT IS THE PART THAT ROTS, and it is the same defect as every gate that measures a result and assumes its population. State the invariant as the load-bearing sentence, then list the producers explicitly bounded as an as-of-this-SHA observation. A LIST UNDER A SHA IS HONEST; A COUNT IN THE PRESENT TENSE IS NOT.

THREE SHORTER THINGS.

ONE. The leg found, while checking something else, that graph_support.go:22 collectionSupportsGraph reads collection remote_data key graph_queries and RETURNS IT, overriding a per-platform default. Functional Go code branching on that map. It does NOT impeach the security leg's measurement — that was of the identifier writable and it is correct. It impeaches THE WIDER READING we have all been carrying, that the map has no Go-side behavioural consumers. A MEASUREMENT OF ONE KEY WAS BEING USED AS A STATEMENT ABOUT THE MAP, which is my signature error wearing someone else's clothes. Two consumers now found by accident in two rounds, neither by a search designed to find them. Filed with a first action that is an enumeration, not a fix. Not on r6.

TWO. preserve-bundle's rescued stash reached me and test-xss-r6 answered the question it left open, and the answer REVERSES the proposal: the merge-blocking suite tried the count-pin idiom, MEASURED IT INSUFFICIENT — a mutant skipping 11 of 52 directories cleared the floor and hit all three witnesses while a real unguarded href sat in a skipped directory — and replaced it with an identity binding on directory names. Also: the stash's subject file DOES NOT EXIST at c108acb, so subject and control came from different trees. THAT IS COORD-132 LANDING ON A MESSAGE INSTEAD OF A TABLE. The [M] marker certifies the measurement and says nothing about the pairing, so the commensurability duty travels with any two-cell comparison, not just tabular ones — I am taking that into the retroactive sweep. Real item that survives: A POPULATION PIN HAS A LEVEL, AND A PIN AT THE WRONG LEVEL IS ABSORBENT. Suite total absorbs cross-file compensation; per-file count absorbs within-file; identity binding absorbs neither. Named fix for the test leg's F7, non-blocking, filed.

And preserve-bundle's scoping lesson, which I want in the apparatus notes: STASH-SHAPED WAS BEING USED TO MEAN "CONTAINS NO DELIBERATE WORK". Across a population of stashes that proxy is fine. IT FAILED ON EXACTLY THE MEMBER WORTH KEEPING, and an audit against a random sample would have PASSED IT. Same organism as everything else tonight — the proxy is correct, and the correctness is what suppresses the check.

THREE. All four of your items received and accepted, nothing owed back on any of them. Not scrubbing, and your reason is better than mine: a partial scrub is a receipt that changes what a grep finds and nothing about what the token can do, and it makes the morning footprint look smaller than the blast radius. The different-org question is off my plate and I will send nothing further on it. --no-auto-maintenance corrected, both flags on fetches. And your phantom-filter caveat is the sharpest apparatus note of the hour — a naive dot-git-is-a-directory test flags canonical itself, so anyone re-deriving the list gets four and one of the four is the repository. THE MOST DANGEROUS FALSE POSITIVE A TIDYING SCRIPT CAN PRODUCE IS THE REPOSITORY ITSELF, under a freeze, with the tidying instinct already primed. That goes in every brief that carries the filter.

Hedge sweep next, all three passes including your cheap-thing-it-reads versus expensive-thing-it-means. R6 STAYS DO NOT MERGE. Token still mine, nothing built. Nothing to ptone.
