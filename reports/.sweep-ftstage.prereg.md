# PRE-REGISTRATION — sweep-ftstage
Written BEFORE any search of the corpus. Timestamp below is machine-generated.

## Prediction under test (from brief §4, coordinator, DERIVED+UNMEASURED)
"Docs promise auto-creation 3x and AutoCreateLabels defaults true, therefore operators had every
reason NOT to provision ft:stage/* labels, therefore the unprovisioned population is the DEFAULT
rather than the corner case."

## FALSIFICATION SENTENCE (pre-registered)
The prediction is FALSIFIED if I find ANY ONE of the following:
 (F1) Any reachable code path that calls a GitHub label-creation API — GraphQL `createLabel`
      mutation, REST `POST /repos/{owner}/{repo}/labels` (go-github `Issues.CreateLabel`),
      or a shelled-out `gh label create` — whose label-name input can be an ft:stage/* value,
      whether by literal, constant, template, or loop over a stage enum.
 (F2) Any non-test provisioning artifact that creates those labels: migration, seed, fixture,
      terraform, helm, k8s manifest, CI workflow, Makefile target, shell script, CLI subcommand
      under bootstrap/provision/init/setup.
 (F3) Any operator-facing instruction (README, install/setup/getting-started doc, runbook) that
      tells a human to create the ft:stage/* labels by hand or by command.
 (F4) Evidence that AutoCreateLabels IS implemented — i.e. the flag is READ by non-test code and
      that read reaches a label-creation call.

## DECISION RULE (pre-registered, before looking)
- Any of F1..F4 satisfied  -> answer is PROVISIONING EXISTS; escalation of finding 1B dies.
- None satisfied AND every negative carries a same-shape same-corpus positive control that returned
  hits -> answer is MEASURED ABSENT.
- Any surface I cannot reach (e.g. requires executing a build, or lives outside the repo) ->
  that surface is reported UNMEASURED, named, and addressed to the eng-manager by name.

## What I will NOT do
I will not treat "no hits for the string ft:stage" as evidence of anything. The chokepoint is the
label-creation client call, and I enumerate there.
generated 2026-07-29T03:46:23Z
