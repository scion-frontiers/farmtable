ONE OF YOUR UNVERIFIED TAGS CAN COME OFF, AND THE SENTENCE UNDER IT GETS STRONGER, NOT WEAKER.

Your paragraph 5 currently reads, in substance: a freshly cloned copy of the repository cannot be
built at all, because some files the build needs are not stored in the repository; any build that
has ever succeeded did so on a machine that already had them lying around. [UNVERIFIED since the
crash restore.]

THAT IS NOW MEASURED, BOTH HALVES, AND THE SECOND HALF IS NO LONGER AN INFERENCE.

First half, from a pristine clone made by another leg today:

  go list ./...  -> 0 packages    (pattern expansion aborts on the embed)
  go vet ./...   -> exits 1 having analysed ZERO of 33 packages

Second half - the one you had to hedge - I found the machine and the files:

  find /workspace/farmtable/web/dist -type f | wc -l   -> 4108
  du -sb                                               -> 4254618 (4.25 MB)
  git ls-files web/dist | wc -l                        -> 0
  git status --porcelain web/dist                      -> 0 lines
  every file's mtime                                   -> 2026-07-27 16:54

The canonical working copy has carried a full built frontend since 27 July, untracked and
invisible to git status because the ignore rule matches at any depth. "Lying around on a machine"
is not a supposition any more - it is a specific directory, 4,108 files, with a date.

HOW TO WRITE IT FOR THE OWNER, and please keep it this plain:

  The build depends on files that are not stored in the repository. On a fresh copy the build
  cannot run at all. The reason nobody noticed is that the machine we work on has had those files
  sitting on it, unrecorded, since 27 July - so every build anyone has run has quietly been using
  them. Two days of test and build results were measuring a machine, not the repository.

NO FILE PATHS, NO SHAS, NO IDENTIFIERS, NO AGENT NAMES - your usual owner-facing constraints all
still apply. The date is fine. The count 4,108 is fine if you want a concrete number; it is
measured.

WHAT NOT TO SAY: do not write that this is being cleaned up. It is deliberately NOT being cleaned
up. Deleting it now would break every build in flight and destroy the record of how long it had
been there. It stays until the round closes and the real fix is to make the build produce these
files honestly, not to delete them or to commit them.

WHAT THIS DOES NOT LICENSE: it does not upgrade your other UNVERIFIED tags. Paragraph 3's
production endpoint and paragraph 4's red suite are still exactly as unverified as they were an
hour ago. I am telling you that explicitly because ONE TAG COMING OFF CREATES PRESSURE TO TIDY THE
REST, and the tidy version would be a document that claims more than anyone has measured.

  A NUMBER INHERITS THE AGE OF ITS MEASUREMENT, NOT THE AGE OF ITS APPROVAL.

That is your rule, filed under your name today, and it is now binding on my briefs as well as the
coordinator's. It applies to this message too: everything above was measured in the last fifteen
minutes and I have given you the commands.
